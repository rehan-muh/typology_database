# Ingestion pass — 2026-09-19 09:08 PT

## Discovery

Fresh repository-oriented discovery covered manual phonetic segmentation/correction, articulatory production, clinical phonetics, L2 production, and multimodal speech-production deposits.

### Accepted: UltraSuite UXTD / UXSSD / UPX labels v1.0

Authoritative deposit: DOI 10.7488/ds/2429. The public DataShare record exposes three ZIP payloads (`uxssd-labels-1.0.zip`, `uxtd-labels-1.0.zip`, `upx-labels-1.0.zip`) plus `README.txt`. The README gives decisive layer-level provenance:

- `slt_labels`: manually annotated labels from Speech Language Therapists — QUALIFIES.
- `reference_labels`: manually revised word-level alignments — QUALIFIES; README states these exist only for a few UXTD/UXSSD utterances.
- `speaker_labels`: automatically derived — EXCLUDE from manual core.
- `word_labels`: automatically derived — EXCLUDE from manual core.
- `phone_labels`: automatically derived — EXCLUDE from manual core.

Labels are supplied in Praat TextGrid and/or HTK label formats. Filenames map to flattened core-data identifiers; e.g. `01M-BL2-004A` corresponds to `core/01M/BL2/004a.*`. License is CC BY-NC 4.0. The core corpora are primary ultrasound/acoustic recordings from child speech-therapy sessions, satisfying the primary-production criterion.

Binary ZIP retrieval failed in this runtime, so no observation rows were fabricated. The exact three archive names/sizes and authoritative README were inventoried; archive-internal enumeration and parsing remain pending. The next run should download each ZIP, enumerate every member, ingest only `slt_labels` and `reference_labels`, preserve TextGrid tiers/intervals verbatim, and explicitly quarantine automatic tiers.

## Backlog verification

JSUT continuation was re-established against upstream `e2e_symbol/phoneme.yaml`: `BASIC5000_1530` onward exists and remains source-faithful backlog. No duplicate JSUT rows were written in this pass because the priority was adding a new qualifying source and the fetched upstream response was truncated before a clean complete batch could be serialized safely.

## Review / exclusion findings

- IFCASL remains strong review/backlog evidence: ~100 speakers, word/phone segmentation, >50% manually corrected, but the manually corrected public observation subset still needs file-level isolation.
- SAIT-EMA and the 2026 German EMA+EEG+audio release are primary production resources, but no manual phonetic annotation evidence was established in this pass; do not admit merely because signals are primary.
- Brain Treebank is excluded for this database despite manual word-onset annotation because the speech is third-party movie audio, not primary participant production.
- ACV-003 is excluded because it is singing.
- Spontal-N is mixed automatic/manual and requires isolation of the manually corrected ~7% before any core admission.

## Integrity / provenance

All writes in this pass are confined to `manual_phonetic_production/`. No existing files were overwritten. The UltraSuite registry entry explicitly separates qualifying human layers from automatic layers. Source IDs are namespaced (`ultrasuite_labels_v1`). Unknown source-specific SLT label values must be preserved verbatim when payload parsing succeeds; no IPA or categorical interpretation may be invented.

## Next priorities

1. Download/enumerate UltraSuite ZIPs and ingest all manual SLT/reference TextGrid/HTK observations with interval times and exact labels.
2. Resume JSUT from `BASIC5000_1530` using complete upstream batches.
3. Isolate the 3,599 human-reviewed L2-ARCTIC utterances and ingest additions/deletions/substitutions without admitting corpus-wide forced alignments.
4. Continue PAVOQUE from durable `m0137` cursor, respecting duplicate-lineage guard.
5. Resolve IFCASL and Spontal-N public manually corrected subsets at file level.
