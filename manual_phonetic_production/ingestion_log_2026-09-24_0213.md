# Ingestion log — 2026-09-24 02:13 PT

## Discovery
- Admitted `bas_phondat2_manual` (PhonDat 2) at **manual-layer scope only**.
- Authoritative BAS catalogue: 16 speakers, 3,200 read-speech utterances; PD2 is listed among corpora with partial hand segmentation and prosodic labeling.
- Historical BAS report states a subcorpus of 64 sentences per speaker was manually segmented and labelled into SAM-PA segments; whole corpus was also automatically segmented with MAUS. The MAUS layer is explicitly excluded from the manual core.
- Historical report also states prosodic annotation/segmentation for 8 speakers / 8,000 utterances, which conflicts with the current 3,200-utterance catalogue description. Preserved as a release/version ambiguity pending file-level reconciliation rather than silently normalizing counts.

## Backlog / ingestion work
- Added source-level registry record with tier-level eligibility and recording-lineage deduplication rule.
- Added 5-class manifest: primary audio, manual segment annotations, prosodic annotations, excluded MAUS layer, documentation.
- Added 6 source-faithful dictionary concepts: segment label/start/end, prosodic label, speaker ID, utterance ID.
- No observation rows fabricated: distributed annotation objects were not accessible in this pass, so exact file names/checksums and interval observations remain pending.

## Screening
- Re-screened CORPRES (manual 40% but no verified downloadable machine-readable deposit), MRPL2 (restricted), RescueSpeech and L2-ARCTIC (already represented), ROG (secondary reuse of GOS2/SST; requires isolation of genuinely new manual production-relevant layers), and BAS VM2/PD1/SC10 candidates.
- Singing-only Jingju collection rejected by scope despite manual phoneme TextGrids.

## QC / next priorities
1. Materialize PD2 manual annotation subset from BAS/CLARIN if connector/access permits; enumerate every object and checksum.
2. Reconcile PD2 historical/current release counts before harmonized observation ingest.
3. Audit BAS PD1, VM2 and SC10 manual partitions at file/tier level, avoiding MAUS layers.
4. Continue repository-first searches in JASA/JSLHR/JPhon/LabPhon supplements for downloadable token tables and trajectories.
