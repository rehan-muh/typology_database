# Ingestion log — 2026-09-14 11:11 PT

## Backlog ingestion

### PAVOQUE (`pavoque_expressive_german` / harmonized source id `pavoque`)

Authoritative source: `marytts/pavoque-data`, especially `pavoque-angry.yaml`. Repository documentation states that the phone segment arrays are manually corrected and gives labels plus segment end times relative to utterance onset.

Committed this run:

- `data/source_faithful/pavoque_angry_utterances_0001_0005.csv`: 5 source-faithful utterance records (`spike0001`–`spike0005`) retaining original prompt, German text, style, absolute source-FLAC start/end, and duration.
- `data/harmonized/pavoque_angry_segments_0001_0002.csv`: 41 manually corrected segment rows for `spike0001` and `spike0002`.
- `data/registry/variable_dictionary_increment_2026-09-14_1111.csv`: 7 source-variable mappings covering prompt, text, style, utterance boundaries, segment labels, and relative segment end times.

Segment treatment:

- Source labels are retained verbatim in `label_original_sampa`.
- `normalized_ipa` is deliberately blank; no IPA mapping was invented.
- Original source segment end times are retained unchanged.
- Segment starts are derived only as the preceding segment's end (0 for the first segment), which is warranted by the ordered contiguous annotation representation.
- Absolute segment times are derived by adding the source utterance onset to relative segment times.
- Silence/boundary labels (`_`, `H#`, etc.) remain observations rather than being silently dropped.

Validation performed: collision-safe utterance and segment identifiers; monotonically increasing segment boundaries within the committed utterances; segment durations non-negative; source utterance lineage remains the original PAVOQUE FLAC stream rather than creating duplicate pseudo-recordings.

Next PAVOQUE backlog: continue through `spike0003+`, then parse the complete angry YAML and all happy/neutral/poker/sad/outtakes YAMLs. Preserve every utterance even where the optional corrected segment array is absent, but admit segment rows to the manual core only where the corrected segment array is actually present. Add style/recording/equipment relations and full file-level row counts after complete parse.

## Discovery

### Phonetic Corpus of Estonian Spontaneous Speech v1.3 — HIGH review

Authoritative DOI: `10.23673/re-438`.

Repository metadata reports a primary spontaneous-speech corpus with manual word and phoneme transcription, 207 speakers, 135 hours of recorded speech, and 106 hours carrying word/phoneme-level annotation. The record also advertises detailed phonetic segmentation and additional multimodal/voice-quality resources.

Decision: **review, not core yet**. The legacy `datadoi.ee` record currently redirects to a repository-migration notice stating that the service has moved to Dataverse. I therefore did not assume file paths, checksums, redistribution terms, or annotation-tier provenance from stale URLs. v1.3 must also supersede rather than duplicate v1.2 lineage.

Next action: resolve the DOI against the current Dataverse record; enumerate every annotation, metadata, respiratory, multimodal, and audio archive; verify license/terms and manual-vs-automatic provenance tier by tier; ingest all human-created word/phone intervals and any human-created voice-quality/respiratory annotations; retain large media by authoritative manifest/checksum rather than committing bulk audio.

## Reconciliation

After this run: 205 tracked sources = 50 accepted + 31 excluded + 124 review; 49 materially ingested sources; 1,362,292 promoted/source-faithful rows across 135 logical tables; 1,194 inventory records; 1,236 variable-dictionary records.

All writes in this run were confined to `manual_phonetic_production/`.
