# Ingestion pass — 2026-09-17 00:10 PT

## Backlog ingestion / integrity audit

Continued the accepted PAVOQUE source (`marytts/pavoque-data`, `pavoque-angry.yaml`, blob SHA `bff79d978f740f6c2b09bc595ade03b6bdf33423`) from the durable checkpoint after `spike0009`.

Verified complete source records for `spike0010`–`spike0012` and the beginning of `spike0013` directly against the current upstream YAML. Complete records contain 35, 32, and 27 sequential segment/silence endpoint observations respectively (94 total candidate interval rows), with original German text, `style=angry`, utterance start/end, original SAMPA-like `lab`, and source-relative segment endpoint. These records are queued for the next structured interval-table write; they are not counted as newly ingested rows in this pass because the connector response for the source was range-truncated and I will not manufacture the missing source text/fields or claim a write that did not occur.

Important integrity finding: the current harmonized tree contains overlapping PAVOQUE chunks (`pavoque_angry_segments_spike0004_0006.csv` and `pavoque_angry_segments_spike0005_0006.csv`). This is a potential duplicate-lineage problem. Do NOT concatenate PAVOQUE chunks blindly. Before warehouse rebuild, deduplicate on namespaced source + prompt + segment ordinal (or exact source-relative boundary tuple) and preserve both chunk provenance records. The current tree also has `0007_0008` but no obvious `0009` chunk in the harmonized directory listing, despite the prior run's checkpoint claim. Therefore `spike0009` must be reconciled before claiming continuous coverage.

Source validation: `spike0010` utterance 31.44–35.52 s, final relative endpoint 4.075 s; `spike0011` 35.52–39.36 s, final endpoint 3.835 s; `spike0012` 39.36–42.78 s, final endpoint 3.415 s. In each case the final endpoint is 0.005 s shorter than nominal utterance duration, consistent with earlier PAVOQUE records. Endpoints are monotonically increasing in the inspected records. No IPA normalization added.

## Discovery

Fresh repository/literature search screened additional manual-production candidates.

* **IFA Dialog Video corpus** (Zenodo record 14906857): annotated primary face-to-face Dutch dialog recordings with named annotators and open GNU licensing. REVIEW pending inspection of tier granularity and confirmation of which temporal/phonetic layers are human-created rather than automatically generated.
* **AusKidTalk orthographic annotation workflow** (Language Resources and Evaluation, 2026): expert pre-screening plus research-assistant hand-correction of transcription and time alignment in Praat. REVIEW; only hand-corrected files are eligible, and phonetic granularity/public deposit must be resolved before core admission.
* **Friedrichs et al. 2026 multimodal German speech-production dataset**: synchronized EMA, EEG and audio from 29 adult native German speakers, with anatomical measures for a subset. Remains REVIEW: valuable primary production data, but a qualifying manual landmark/annotation layer still needs isolation before observation-level admission.

Explicit exclusion reaffirmed: Jingju a Cappella Recordings Collection is singing/music production despite excellent manual phrase/syllable/phoneme TextGrids; it remains outside scope.

## Next backlog priorities

1. Reconcile PAVOQUE overlapping chunks and missing/unclear `spike0009`, then write `spike0010` onward without duplicates.
2. Russian Fricatives: enumerate/download TextGrids and ingest manually corrected first-session target boundaries and all supplied measurements.
3. TaL manual segments.
4. L2-ARCTIC isolated human `/annotation` subset only.
5. CCOST corrected word/syllable/phone/toneme TextGrid tiers.
6. Resolve IFA Dialog Video and AusKidTalk public annotation manifests.

No unrelated repository paths modified.