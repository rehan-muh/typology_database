# Ingestion log — 2026-09-22 12:11 PT

## Backlog ingestion

Advanced `zenodo_10080490` (PxCorpus v4) from accepted/pending toward source-faithful ingestion using the authoritative Zenodo record and embedded project documentation.

- Manifested the sole top-level Zenodo object: `PxCorpus.zip`, 375.7 MB, MD5 `97247fc5278cf4e3ab147aab4fc72f14`, open, CC BY 4.0.
- Recorded documented internal PxSLU objects: `metadata.txt`, `paths.txt`, `PxSLU_conll.txt`, `seq.in`, `seq.label`, `seq.out`, `recordings/`, `Demo.ipynb`, `verifications.py`, and data-split files.
- Recorded documented PxDialogue objects: `events.txt` and `dialogue_annotations.txt`.
- Added 14 source-faithful variable-dictionary entries covering participant ID, age range, gender, expertise category, recording path, intent, human-verified transcript, BIO semantic labels, manual dialogue correctness, and event-log fields.
- Preserved the source discrepancy: prose says 2,067 recordings; overview table totals 1,981 recordings (903 sessions, 262.27 min). No count was silently reconciled.
- Recording format documented by authors: mono WAV, 16 kHz, 16-bit signed PCM.
- Human provenance: automatic transcripts were verified by human effort; record also calls the corpus manually transcribed and semantically annotated. `dialogue_annotations.txt` is explicitly manual.
- Safety boundary: application TTS/system events are retained as lineage/context only and must not be admitted as human production observations. Event timestamps are not treated as manual phonetic landmarks.
- No observation rows added because the 375.7 MB archive itself was not parsed in this run; no internal values were invented from examples in the README.

## Discovery

Ran fresh repository-focused searches for manually annotated/corrected production corpora, including TextGrid, VOT, formant, EMA, and manual segmentation terms. Search results were dominated by already-known sources, non-speech annotation, secondary corpora, and singing; no new source met the threshold for admission without weaker provenance. MRPL2 remains high priority but restricted. This run therefore favored deeper ingestion of an already accepted, openly deposited source over registry inflation.

## Validation

- All writes confined to `manual_phonetic_production/`.
- No unrelated repository content modified.
- No duplicate observation rows introduced.
- New manifest record has authoritative repository URL and checksum.
- Variable dictionary retains original variable/file semantics and distinguishes human annotation from application logs.

## Next priorities

1. Download/enumerate `PxCorpus.zip`; verify exact member list and byte sizes; parse all parallel PxSLU text tables and PxDialogue annotations into source-faithful tables.
2. Resolve 2,067-vs-1,981 recording count from actual `paths.txt`/archive members, not prose inference.
3. Materialize namespaced speaker/session/recording IDs and ingest all 1,981-or-2,067 transcript rows once archive evidence establishes the exact count.
4. Continue RescueSpeech and AphasiaBD archive enumeration; retain MRPL2 as restricted backlog.
