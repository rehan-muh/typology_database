# Ingestion pass — 2026-09-18 13:13 PT

## Backlog ingestion
- AutoVOT tutorial: parsed `cas7D_1054_32_1.TextGrid` directly from upstream GitHub. Admitted only the explicitly manual `vot` tier: 0.780001–0.86 s = 79.999 ms; contextual phone `B`, word `BIT`. Source blob SHA `39a46bfd915510ab218ffb934a6e038a839a15dc`. Phone/word tiers are context only.

## New accepted source: PAVOQUE
- Accepted the PAVOQUE Corpus of Expressive Speech as primary, non-singing German production data with manually corrected phonetic segmentation.
- Primary documentation: `marytts/pavoque-data` README, blob `3b10a17eaeba6ee0843dc68147c3b5d3e7b5ba6d`.
- One male native German speaker; spoken neutral and expressive acting styles. Professional baritone training is speaker background only; corpus task is speech, not singing.
- Audio documented as FLAC, 44.1 kHz, 16-bit, mono.
- Six annotation YAMLs inventoried with immutable Git blob SHAs and byte sizes: angry, happy, neutral, outtakes, poker, sad. Total surfaced YAML annotation bytes = 7,741,085.
- YAML schema: prompt, text, style, utterance start/end seconds, optional manually corrected segments with SAMPA-based `lab` and segment `end` relative to utterance start.
- Parsed `spike0001` from `pavoque-angry.yaml` into 12 linked segment rows. Original labels and original relative endpoints retained; derived segment starts and absolute endpoints are separate fields. No IPA normalization was invented.
- License: CC BY-NC-SA 4.0.

## Discovery / triage
- Danish infant-directed speech study: article reports 9,267 vowels manually segmented in Praat; high-priority repository/supplement hunt because production measurements are potentially broad but public token deposit not yet established in this pass.
- 2024 JSLHR French child cochlear-implant study: 1,037 primary sentences / 2,074 vowels manually segmented and annotated in Praat; high-priority deposit hunt for acoustic/perceptual production measures.
- BAS corpus index revalidated as a broad source family explicitly distinguishing manual from automatic segmentations; individual corpora require license/file-level triage before core admission.
- Speech-to-song-illusion production study surfaced with manually segmented speech, but retained for review because its relationship to a music/speech-to-song paradigm requires source-level scope checking against the non-singing rule.

## Validation
- All new IDs namespaced by source.
- PAVOQUE source label `H#` and SAMPA labels preserved verbatim; no unsupported IPA mapping.
- Segment starts are transparently derived from prior segment endpoints, not substituted for source endpoints.
- No raw audio committed.
- Writes confined to `manual_phonetic_production/`.

## Next priorities
1. Exhaustively stream/parse all six PAVOQUE YAMLs into source-faithful utterance and segment tables; retain every utterance even where `segments` is absent and explicitly count annotation coverage.
2. Continue AutoVOT `32_2`, `32_3`, then later voiced/voiceless backlog without reprocessing completed files.
3. Locate public deposits/supplements for the Danish IDS 9,267-vowel dataset and the French CI 2,074-vowel dataset; admit only if observation-level public data are available.
4. Continue L2-ARCTIC licensed manual-subset file acquisition if access becomes available; never merge forced-aligned remainder into manual core.
