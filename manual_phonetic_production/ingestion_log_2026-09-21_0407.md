# Ingestion pass — 2026-09-21 04:07 PT

## Discovery
Searched public repository/deposit records for manually segmented/corrected phonetic production material, including Zenodo/OSF/GitHub and learner/clinical/articulatory/voice targets. Rechecked IFCASL (>50% manually corrected word/phone segmentation) and HAMATAC (primary L2 German map-task speech with manual disfluency annotation), but did not promote inaccessible/non-phonetic layers.

## Newly accepted: VoXAngeles
Accepted `voxangeles` under the explicit exception for secondary recordings with genuinely new, isolatable manual annotation. The upstream repository states that `data/audited_aligned` contains manually corrected phone-level alignments and audited phonetic transcriptions, frequently checked against original field notes. The underlying recordings originate in the UCLA Phonetics Lab Archive; therefore lineage must retain that parent and only the new VoXAngeles layer is admissible. `data/unaudited` is explicitly force-aligned and not manually corrected and is excluded.

## File-level backlog ingestion
Enumerated six public phonetic-measurement TSVs and recorded immutable Git blob SHAs and byte sizes in `data/registry/file_manifest_increment_2026-09-21_0407.csv`: duration; f0 deciles; f0 quartiles; formant deciles; formant quartiles; and recording mean-f0. The trajectory products must remain trajectories (all source sample points retained), not be collapsed to midpoint values. Decile/quartile products overlap scientifically and must be linked rather than counted as independent tokens.

Large TSV contents were not safely returned by the connector in this pass, so they remain `pending_parse`; row counts and variable-dictionary counts were deliberately not fabricated or incremented. Next priority is direct source-faithful parsing of these six TSVs, then enumeration/parsing of `audited_aligned` annotation files with namespaced IDs and explicit lineage to UCLA.

## Integrity / deduplication
- No automatic-only observations admitted.
- No raw audio committed.
- No inferred IPA/context.
- No parsed-row count increment without observation parsing.
- Existing canonical parsed total remains 1,364,267 rows / 205 logical tables.
- Registry counts updated only for the newly adjudicated source and six manifest records.

## Commits
- accepted source registry: `b9e668c1b45d8ac637c1f577b7ae9f212334e98b`
- file manifest: `987373c393b508064b72184d63f58525e590adf3`
- canonical status: `74b5b8c55b6103a201a8d4acf9bc8d6db8af02ac`
