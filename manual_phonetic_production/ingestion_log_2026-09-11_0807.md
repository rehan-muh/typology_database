# Manually Annotated Phonetic Production Database — ingestion pass 2026-09-11 08:07 PT

## Backlog ingestion

- Continued PAVOQUE from the recorded continuation point by inspecting upstream `pavoque-angry.yaml` lines 8521–8920 against immutable Git blob `bff79d978f740f6c2b09bc595ade03b6bdf33423`.
- The inspected window closes the prior partial `m0179`, contains complete `m0180`–`m0189`, and enters `m0190`. This run did **not** claim that window as a durable contiguous mirror; the exact mirrored prefix remains lines 1–8520 until the missing raw line chunks themselves are committed.
- Promoted complete utterance `pavoque::m0180` (`Es ist die Enterprise...`, angry, 878.52–881.46 s).
- Promoted all 20 source-labelled manually corrected intervals for `m0180` into `data/warehouse/pavoque_segment_intervals_increment_2026-09-11_0807.csv`.
- Preserved original German SAMPA/silence labels and original relative endpoints. Derived interval starts, durations and absolute times are stored separately. `normalized_ipa` is deliberately blank.
- Added a source-faithful endpoint table for the same 20 annotations with immutable upstream provenance.
- Validation: interval endpoints are monotonic; final annotated endpoint is 2.935 s relative / 881.455 s absolute, leaving the source-recorded 5 ms residual to the utterance end at 881.460 s rather than silently extending the final interval.
- Net promoted observations this pass: **21** (1 utterance + 20 intervals).

## Discovery and screening

Repository/web discovery was run across Zenodo, OSF and GitHub using manual-annotation/manual-segmentation/TextGrid/phonetic-production queries.

- **PxCorpus (French spoken drug-prescription corpus; Zenodo 10080490): screened, not admitted.** It is primary production and its automatic transcripts were human verified, with semantic annotation, but the public record does not establish a relevant manually created/corrected phonetic interval, landmark, acoustic/articulatory measurement, or exhaustive human verification of such a layer. Transcript-level verification alone is insufficient for core inclusion under the current protocol.
- **AphasiaBD:** re-encountered; already present in REVIEW. Public documentation establishes manual sentence-level segmentation, not an isolable manual phonetic layer.
- **Jingju manually segmented corpora:** re-encountered and remain outside scope because the productions are singing, despite high-quality phoneme-level manual TextGrids.
- Search results dominated non-phonetic uses of “manual segmentation”; none were promoted without direct evidence satisfying the production + relevant-human-annotation criteria.

## Reconciled state

- Sources tracked: **143**
- Accepted: **45**
- Excluded: **25**
- Review: **73**
- Materially ingested sources: **44**
- Promoted/parsed rows: **1,361,546**
- Logical tables: **86**
- File-manifest records: **958**
- Variable-dictionary records: **1,148**
- PAVOQUE promoted utterance rows: **27**
- PAVOQUE promoted interval rows: **110**

## Next backlog priorities

1. Commit exact source-faithful PAVOQUE raw line chunks beginning at line 8521 so the durable contiguous mirror can advance, then promote every complete utterance/interval in the inspected window rather than sampling one.
2. Scale relational promotion across the 220 already mirrored PAVOQUE utterances.
3. Resume direct observation-level parsing of accepted Chenghai Southern Min TextGrid/XLSX material.
4. Retry/parse the accepted real-time MRI archive so its 392 manually labelled articulator masks and 392 velopharyngeal-closure judgments become linked articulatory observations.
5. Resolve IFCASL/Bangla/AusTalk review cases only when an isolable human-corrected observation layer and redistribution provenance can be established.

All writes in this pass were confined to `manual_phonetic_production/`.
