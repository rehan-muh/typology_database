# Manually Annotated Phonetic Production ingestion log — 2026-09-12 21:12 PDT

## Backlog ingestion

### PAVOQUE — angry subset, durable chunk 038

- Source: `marytts/pavoque-data`, `pavoque-angry.yaml`.
- Immutable upstream blob: `bff79d978f740f6c2b09bc595ade03b6bdf33423`.
- License: CC BY-NC-SA 4.0.
- Literal upstream records ingested: `m0082`, `m0083`, `m0084`, `m0085`.
- New durable utterance rows: **4**.
- New durable manually corrected segment/silence intervals: **131** (`20 + 24 + 52 + 35`).
- Durable PAVOQUE angry total after this pass: **133 utterances / 4,091 intervals / 38 chunks**.
- Next verified literal upstream record: `m0086`.

Files created:

- `data/source_faithful/pavoque_angry_utterances_chunk038.csv`
- `data/source_faithful/pavoque_angry_segments_chunk038.csv`
- `data/source_faithful/pavoque_angry_chunk038_manifest.json`

Canonical status updated:

- `data/source_faithful/pavoque_angry_ingestion_status.json`

Validation performed:

- namespaced utterance IDs use the `pavoque::source_id` convention;
- all original German-SAMPA/silence labels retained without substitution;
- every source cumulative endpoint retained in `endpoint_original_s`;
- interval starts and durations are deterministic consecutive-endpoint derivatives only;
- no IPA values were generated or inferred;
- literal upstream record order was preserved;
- `m0082`, `m0083`, `m0084`, and `m0085` each retain the source-native 0.005-s terminal residual rather than being timing-repaired;
- all 131 derived interval durations are non-negative and link consecutively within utterance;
- global scientific-observation count was **not** incremented because the validated full PAVOQUE lineage is already represented elsewhere in the warehouse; these durable chunks are source-faithful materialization, not independent new recordings.

## Discovery and repository screening

### Belz, Rasskazova, Krivokapić & Mooshammer — German vowel tenseness / phrasal structure acoustic + EMA data

Added to **HIGH review** as `belz_rasskazova_krivokapic_mooshammer_2021`.

Evidence established from the open Zenodo deposit and associated Language and Speech article:

- primary controlled speech production from **10 native German speakers** recorded using electromagnetic articulography;
- **800 monosyllabic + 400 disyllabic target-word presentations** were designed across phrasal conditions and repetitions;
- recordings were pre-aligned with WebMAUS, but the transcription/alignment was explicitly **checked and corrected in Praat**;
- every target-word segment was annotated on an interval tier, including stop closure/VOT, vowels, fricatives and nasals;
- prosodic-boundary realization and pitch accent were audio-visually inspected by a trained research assistant;
- the open Zenodo record exposes `mono.txt`, `di.txt`, `mono.art.txt`, `di.art.txt`, `meta.speech.dur.txt`, `analysis.R`, and `analysis.html`;
- repository checksums are exposed for all public files; the five data files contain source-level acoustic and/or articulatory observations rather than paper-only summaries;
- preview of `mono.art.txt` confirms rich original columns for gesture landmarks/times, X/Z coordinates, velocities, subject, sex, phrasal position, word, iteration, plateau/opening/closing durations, displacements, and tenseness.

Eligibility decision:

- the acoustic segmentation/annotation layer clearly meets the database human-corrected inclusion criterion and should be promoted after complete table parsing;
- articulatory landmarks were produced in `mview`, described as semi-automatic. They remain in review until the extent of human inspection/correction is documented sufficiently to satisfy the manual/human-verified criterion;
- no automatic-only layer was promoted merely because it is distributed alongside eligible data.

Next ingestion action for this source:

1. parse `mono.txt` and `di.txt` in full and preserve every original column/value;
2. create a complete variable dictionary including original units and segment/constituent timing semantics;
3. inventory/checksum all seven deposit files from Zenodo;
4. parse `mono.art.txt` and `di.art.txt` source-faithfully but keep their promotion status separate until manual verification of mview landmarks is established;
5. build linked speaker, stimulus, condition, acoustic-duration, interval/segment, and articulatory-measurement tables using namespaced IDs.

## Reconciled database state

- tracked sources: **164**
- accepted: **47**
- excluded: **26**
- review: **91**
- materially ingested sources: **44**
- distinct promoted/parsed scientific observations: **1,361,571**
- logical tables: **86**
- inventory-file records: **1,100**
- variable-dictionary records: **1,156**

## Next backlog priorities

1. Continue PAVOQUE from literal upstream record `m0086`.
2. Fully parse the open Belz et al. acoustic tables and build their source-faithful variable dictionary; separately resolve manual-verification status for EMA landmarks.
3. Retrieve and unpack the public ROG annotated archive and isolate genuinely new ROG-Art human prosodic/disfluency/dialogue-act tiers without duplicating inherited GOS recordings.
4. Continue direct-release attempts for L2-ARCTIC manual `/annotation` TextGrids and its human-checked suitcase subset.
5. Resolve high-value open token tables already in backlog, prioritizing manually corrected trajectories and production annotations over paper-level metadata.

All repository writes in this pass were confined to `manual_phonetic_production/`.
