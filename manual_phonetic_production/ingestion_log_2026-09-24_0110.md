# Ingestion log — 2026-09-24 01:10 PT

## Discovery

Accepted `clips_mt_manual_bas_v1_1` (CLIPS_MT_MANUAL) at corrected manual-annotation scope. BAS documents 30 speakers / 30 map-task dialogues from 15 Italian locations, 3,228 inspected/repaired WAV turn files, 3,228 corrected CLIPS annotation sets, 3,228 BAS Partitur files, 3,228 EMU annotation sets, and 104,056 phonetic segments. Segmentation is explicitly manual, including partial closure+burst segmentation for plosives. Distribution is scientific/controlled and prohibits redistribution to third parties, so this run stores authoritative source references and an expected-object manifest rather than copying unavailable binaries.

Lineage rule: CLIPS_MT_MANUAL is a corrected edition of a subset of original CLIPS recordings. The corrected annotation layer is genuinely new/manual and eligible, but underlying recordings must not be double-counted if original CLIPS is later/elsewhere registered.

Screened/rescreened: MRPL2 (eligible annotations but restricted files); IFCASL (manual-correction evidence strong, exact public objects still unresolved); ROG (secondary reuse of GOS2/SST recordings, requires isolation of genuinely new production-relevant manual layers); ESTER (manual orthography/speaker/acoustic-condition annotations but phonetic alignments described as automatic); German-English Code-Switching Corpus (derivative of Spoken Wikipedia; lineage isolation required); UrduSpeech benchmark (manual correction claim, but third-party/curated-media lineage needs verification).

## Backlog ingestion

Reviewed canonical status before writes: 272 sources, 79 accepted, 36 excluded, 157 review; 55 observation-ingested; 1,365,086 parsed rows / 245 logical tables; 1,347 manifest and 1,435 dictionary records. Added CLIPS_MT_MANUAL registry, six object-class manifest records, eight dictionary concepts, and component-level ingestion status. No observation rows were fabricated because the scientific distribution is not directly accessible through the public metadata page.

## QC / provenance

- Exact original tier/column names are not claimed where the source files have not been opened; dictionary placeholders are explicitly marked pending.
- No IPA normalization was invented.
- No restricted WAV/annotation binaries were committed.
- Underlying CLIPS audio lineage flagged for deduplication.
- Writes confined to `manual_phonetic_production/`.

## Next priorities

1. Obtain/parse CLIPS_MT_MANUAL scientific distribution if connector-accessible; enumerate every actual object and checksum, then ingest all 104,056 manual segments plus metadata.
2. Continue extracting accessible accepted-source backlogs before adding registry-only sources.
3. Resolve IFCASL public object distribution and manually corrected subset identity.
4. Investigate primary experimental deposits in Journal of Phonetics / Laboratory Phonology / JASA / JSLHR supplements with token-level acoustic, articulatory, voice-quality, and trajectory measurements.
