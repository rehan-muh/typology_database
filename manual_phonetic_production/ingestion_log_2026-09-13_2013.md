# Manually Annotated Phonetic Production Database — ingestion pass 2026-09-13 20:13 PT

## Scope

This pass performed both required activities: (1) backlog ingestion/harmonization of already accepted primary production material and (2) fresh public-repository discovery. All writes are confined to `manual_phonetic_production/`.

## Backlog ingestion: CCOST

Source: `ccost_2026` (accepted; primary Standard Thai production with manually corrected phonetic annotations).

Processed source rows **109–128** from the already immutable source-faithful CCOST metadata layer. Rows 109–118 come from `CCOST_metadata_chunk003_rows089_118.csv` (blob `fc779dafdc47aee2cc55be6c26a6fbbf992adc4b`); rows 119–128 come from `CCOST_metadata_chunk004_rows119_148.csv` (blob `b758b920f4fff8676666030f079c8c60859c841e`).

Created:

- `data/warehouse/ccost_2026_recording_quality_conditions_rows109_128.csv` — 20 rows.
- `data/warehouse/ccost_2026_acoustic_summary_rows109_128.csv` — 20 rows.
- `data/warehouse/ccost_2026_rows109_128_harmonization_manifest.json` — explicit source/output lineage, transformation rules, validation, and double-counting guard.

Preserved in the recording/condition projection: source recording ID, namespaced recording ID, namespaced speaker ID, task parsed only from the explicit `INT`/`WL`/`ST` bundle component, interviewer gender, source quality grade, SNR, quality issue/comment, word-list order, pace, misread frequency, total duration, labeled duration, interval count, and exact source-row provenance.

Preserved in the acoustic projection: `MeanIntensity.dB`, `MaxIntensity.dB`, `MinIntensity.dB`, `StdDev.dB`, `DynamicRange.dB`, `PeakLevel.dBFS`, `PeakIntensity.dB`, `Threshold.dB`, `AvgSpeech.dB`, and `AvgNoise.dB`.

No low-quality rows were filtered: low-SNR, clipping, low-volume, and other source flags remain represented. No source value was overwritten. No normalization, imputation, unit conversion, IPA inference, or missing-context inference was performed.

Validation: 20 source rows -> 20 unique source recording IDs -> 20 unique namespaced recording IDs in each linked projection. Scientific-observation increment is **0** because these are analysis-ready projections of metadata observations already present in the source-faithful layer.

CCOST cumulative harmonization after this pass: **116 quality/condition rows + 116 acoustic-summary rows**. Source-faithful metadata remains **195/488** rows. Next warehouse harmonization row: **129**. Next external source-faithful metadata row: **196**.

## Fresh discovery: Open Oldenburg Conversation Corpus (OOCC)

Candidate ID: `oocc_2026`

Authoritative current-version record: <https://doi.org/10.5281/zenodo.21446419> (version 2, July 2026).

Disposition: **HIGH review**, not core.

Why promising: primary non-singing German dyadic-conversation recordings with synchronized audio/video/body-pose data; the Zenodo deposit separately exposes transcript and landmark archives for both 2-minute and 10-minute recordings, plus metadata and documentation. This structure could contain admissible human production annotations without requiring raw-media mirroring.

File-level inventory completed for all **18** visible Zenodo version-2 objects. `data/warehouse/oocc_2026_file_manifest.csv` records published sizes, MD5 checksums, roles, authoritative record, and per-file next action. Large audio/video archives are `manifest_only`; transcript, landmark, metadata, and documentation archives remain pending targeted parsing/provenance review.

Rights/lineage handling: Zenodo reports CC BY-NC-ND 4.0 plus participant-protection/use conditions and asks users not to redistribute substantial corpus portions outside official channels. Therefore the database retains authoritative references/checksums rather than mirroring large media. Version 2 is treated as the current lineage; version 1 is not counted as an independent source.

Core promotion is withheld because the surfaced record does not yet prove whether transcript boundaries, landmarks, or labels were manually created/corrected or exhaustively human-verified. Next pass should open `documentation.zip`, `README.md`, `metadata.zip`, and the small transcript archives first, then admit only demonstrably human-grounded layers; automatic pose/landmark products must remain separate.

## Reconciliation

- Sources: **190 total = 47 accepted + 31 excluded + 112 review**.
- Materially ingested sources: **45**.
- Distinct promoted scientific observations: **1,361,972** (unchanged; CCOST additions are non-additive projections).
- Logical tables: **102** (up from 100 via the two new CCOST projections).
- Inventory-file records: **1,128** (up from 1,110 via 18 OOCC records).
- Variable-dictionary records: **1,197** (unchanged; OOCC variables are not mapped before archive inspection).

## Errors / blockers

- No destructive or out-of-subtree writes were performed.
- OOCC manual-annotation provenance is unresolved; therefore no transcript/landmark observation was promoted prematurely.
- CCOST external source-faithful continuation beyond row 195 was not attempted from incomplete snippets; no row 196+ content was guessed.

## Next priorities

1. CCOST: harmonize rows 129–148 next, then continue 149–178; independently resume source-faithful external ingestion at row 196 when the authoritative endpoint is available.
2. OOCC: parse README/documentation/metadata and small transcript archives; establish manual-vs-automatic provenance tier-by-tier before any core admission.
3. Continue high-priority review backlog where downloadable TextGrid/EAF/CSV/XLSX/RDS/MAT objects can be converted into observation-level tables without mirroring large raw audio.
