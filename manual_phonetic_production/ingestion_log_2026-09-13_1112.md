# Ingestion log — 2026-09-13 11:12 PT

## Backlog ingestion

### PAVOQUE angry subset — chunk 047

Advanced the durable source-faithful mirror from source record `m0121` through `m0124`, following literal order in upstream `marytts/pavoque-data/pavoque-angry.yaml` (blob `bff79d978f740f6c2b09bc595ade03b6bdf33423`).

- New utterances: 4 (`m0121`, `m0122`, `m0123`, `m0124`)
- New manually corrected German-SAMPA/silence intervals: 157
- Durable mirror after this pass: 47 chunks, 170 utterances, 5,311 intervals
- Next verified literal source record: `m0125` (`Sein Nachfolger wurde George Russell.`)
- All four utterances preserve a 0.005 s source-native terminal residual between the last cumulative segment endpoint and the utterance end.
- Preserved source utterance IDs, orthographic text, style, utterance bounds, labels, and cumulative endpoints.
- Derived only interval starts and interval durations; no labels were normalized to IPA and no missing context was inferred.
- Created chunk-level provenance and a two-row append-only file-manifest increment with Git blob hashes and byte sizes.
- Double-counting guard retained: these rows materialize a PAVOQUE lineage already represented in the validated full parse, so `parsed_rows_total` was not incremented.

Validation checks passed: 4 unique source utterance IDs; 157 interval rows; strictly non-decreasing endpoints within utterance; interval starts equal the preceding endpoint (0 for the first interval); duration = end − start; four 0.005 s terminal residuals; namespaced IDs present.

## Fresh discovery

### IFCASL — HIGH review

Added `ifcasl_2016` to the review queue after a targeted search of phonetic learner-corpus and manually corrected TextGrid literature.

The published corpus documentation describes primary French and German native/non-native read speech from roughly 100 speakers. Forced phone/word alignment was followed by human correction: the non-native subcorpora were manually re-annotated to 80% each, native French to 60%, and native German to 25%. The `Real` TextGrid tier contains manually corrected actual phone labels and boundaries; annotators also marked insertions, deletions, substitutions, and incorrect (de)voicing, while word boundaries were manually corrected where necessary.

No authoritative public downloadable WAV/TextGrid deposit was located during this pass. Therefore the source remains HIGH review rather than accepted/core. If a repository is located, ingest only records with the manual `Real` layer (plus linked metadata); automatic-only `Align` observations remain out of scope.

## Discovery sweep notes

Searches also surfaced singing/music corpora and non-speech manually segmented datasets; these were not admitted because they fail the non-singing phonetic-production scope. Existing AphasiaBD material was recognized as already tracked and was not duplicated.

## Global status after reconciliation

- Tracked sources: 179
- Accepted: 47
- Excluded: 29
- Review: 103
- Materially ingested: 45
- Distinct promoted scientific observations: 1,361,972 (unchanged; PAVOQUE chunk double-count guard)
- Logical tables: 90
- Inventory-file records: 1,109
- Variable-dictionary records: 1,197

All repository writes in this pass were confined to `manual_phonetic_production/`.
