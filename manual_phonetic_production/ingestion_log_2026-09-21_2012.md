# Ingestion pass — 2026-09-21 20:12 PT

## Discovery
Screened fresh repository/web candidates for explicit human phonetic-production annotation. New accepted source: `RVD_MOCHATIMIT_ZHANG2024` (`YIXUANZ/rvd`). This is secondary Mocha-TIMIT reuse but qualifies under the database exception because the authors contribute a new, isolatable manual correction layer. The upstream README states that inaccuracies in Mocha-TIMIT labels were identified and that `manually_corrected_subset` contains manually corrected labels for specific utterances.

Rejected from core during screening: singing/choir resources; perception/non-production resources; transcript-only resources without an isolatable phonetic-production layer. HAMATAC was not promoted because surfaced manual additions are chiefly POS/lemma/disfluency/transcription annotations rather than a demonstrated phonetic measurement/landmark layer.

## Source-level ingestion
- Added accepted registry increment for `RVD_MOCHATIMIT_ZHANG2024`.
- Inventoried upstream README blob `0982ca42a9f8dbe3a42105823f4b8b8757981b10` (418 B), `filelist.lst` blob `4f2a220292cf5be024354a659daa26ad84a95012` (37,894 B), and corrected-label tree `62b8305d24ad7ff81b76f50e8aefd65c7eb84a11`.
- Verified first corrected member `ffes0_267_voicing.npy`, blob `63b732dc6ad180c31423d821b5b75b20a69a8416`, 3,720 B.
- Added a 3-row source-faithful variable dictionary. No NPY observation values were counted yet because binary array coding/frame timing has not been decoded from authoritative methods; this avoids inventing units or time bases.
- Deduplication: underlying recordings remain Mocha-TIMIT lineage; only the new RVD manual correction layer is core-admissible.

## Backlog audit
- L2-ARCTIC v5 manual annotation package remains behind its authorized/form-gated access flow; metadata and manual-layer documentation are already ingested, while forced-alignment-only tiers remain excluded.
- JSUT-label status still excludes Julius automatic timing from the manual core. Existing source-faithful slices extend beyond the stale status cursor, so no rows were duplicated this pass.

## Validation
Canonical source counts now 240 total / 60 accepted / 32 excluded / 148 review. Parsed scientific rows remain 1,364,617 across 222 logical tables. Inventory records: 1,247. Variable dictionary records: 1,272. No unrelated repository paths modified.

## Next priorities
1. Decode RVD corrected NPY arrays against paper methods and ingest every corrected frame/label with explicit time base only after verified.
2. Reconcile JSUT-label ingestion-status cursor against already committed later slices before adding more rows.
3. Continue PAVOQUE source-faithful promotion only after deduplicating existing chunk files against newer per-utterance promotion files.
4. Inspect IFCASL and the 2026 audiovisual monologue deposit for file-level separation of human-corrected versus automatic boundaries.
