# Manual phonetic production ingestion — 2026-09-13 00:13 PT

## Backlog ingestion

### PAVOQUE expressive German — angry subset
- Advanced durable source-faithful materialization from chunk040 through **chunk041**.
- Added source records in literal upstream order: `m0095`, `m0097`, `m0098`, `m0099`.
- Added **4 utterance rows** and **119 manually corrected German-SAMPA/silence interval rows**.
- Preserved original utterance IDs, texts, style labels, utterance bounds, segment labels, and cumulative segment endpoints from upstream blob `bff79d978f740f6c2b09bc595ade03b6bdf33423`.
- Derived only interval starts and durations from the preceding cumulative endpoint; no IPA or missing context was invented.
- All four utterances retain the native **0.005 s terminal residual**.
- Upstream IDs `m0096` and `m0100` are absent; these gaps were preserved rather than filled synthetically.
- Next verified literal source record: `m0101`.
- Double-counting guard retained: these rows mirror a lineage already represented in the validated full PAVOQUE parse, so the global distinct-scientific-observation count does not increase.

## Accepted-source backlog triage

### C-COST / Chulalongkorn Corpus of Spoken Thai
- Re-verified the public Zenodo deposit and corpus paper.
- Public `CCOST_metadata.csv` exposes **488 recording rows** and **37 source variables** including speaker demographics, task/order, quality flags, annotation comments, total/labeled durations, interval counts, intensity measures, SNR and legacy file identifiers.
- Corpus documentation confirms approximately **7 h spontaneous + 5 h controlled Standard Thai speech from 49 speakers**, with force-aligned annotations manually corrected by trained native Thai speakers.
- Direct byte-level retrieval of the complete CSV/ZIP was unavailable in this runtime, so no incomplete or reconstructed partial table was committed. The source remains an accepted, high-priority observation-level backlog target.

## Fresh discovery

### IFCASL — HIGH review
- Added `ifcasl_2016` to the review increment.
- Primary French-German native/non-native read-speech from about **100 speakers**.
- Corpus documentation states that **more than 50%** is manually corrected at phone/word level; project documentation confirms manual checking/annotation after forced alignment.
- The manually corrected `Real` tier described in associated documentation includes realized-phone labels and manually reset phone boundaries; word boundaries are also corrected where necessary.
- Kept in review because a current public corpus distribution endpoint and exact manually-corrected utterance/tier inventory still need to be located before automatic-only material can be excluded with certainty.

## Files committed
- `data/source_faithful/pavoque_angry_utterances_chunk041.csv`
- `data/source_faithful/pavoque_angry_segments_chunk041.csv`
- `data/source_faithful/pavoque_angry_chunk041_manifest.json`
- updated `data/source_faithful/pavoque_angry_ingestion_status.json`
- `data/registry/review_increment_2026-09-13_0013.csv`
- updated `STATUS.json`

## Reconciled status
- Sources: **169 total = 47 accepted + 29 excluded + 93 review**.
- Materially ingested sources: **44**.
- Distinct promoted scientific observations: **1,361,571** across **86 logical tables**.
- Inventory-file records: **1,109**.
- Variable-dictionary records: **1,156**.

## Next backlog priorities
1. Continue PAVOQUE angry from literal record `m0101` while preserving source ID gaps.
2. Materialize C-COST `CCOST_metadata.csv`, `wordlist.csv`, `sentences.txt`, then enumerate/parse TextGrid tiers without mirroring raw WAV audio.
3. Locate and inventory the IFCASL corpus distribution endpoint and isolate manually corrected `Real`/word tiers from automatic-only alignments.
4. Revisit SwehVd OSF enumeration and the open Belz et al. German acoustic tables for full source-faithful parsing.

All repository writes in this pass were confined to `manual_phonetic_production/`.
