# Ingestion pass — 2026-09-15 10:08 PT

## Backlog ingestion

Prioritized PAVOQUE rather than continuing only the JSUT sequence. Parsed `spike0003` from authoritative `marytts/pavoque-data/pavoque-angry.yaml`.

- Primary German studio speech; angry/aggressive expressive speaking style.
- Source utterance: `spike0003`, 5.94–11.94 s.
- Text preserved verbatim: `Na dann weißt du ja, dass ich Dummköpfen gegenüber nicht sehr tolerant bin.`
- 56 phone/silence segments ingested.
- Original SAMPA-like labels preserved verbatim; no IPA normalization invented.
- Segment end times preserved exactly as supplied; starts derived only from the preceding supplied endpoint, with both utterance-relative and recording-absolute times retained.
- Durations derived as end minus start and stored separately from raw boundaries.
- Annotation status recorded as `manually_corrected` based on corpus documentation.
- Namespaced IDs use `pavoque::spike0003::segNNN`.
- Source-faithful utterance metadata preserved separately from the harmonized segment table.

Files added:
- `data/source_faithful/pavoque_angry_utterance_0003.csv`
- `data/harmonized/pavoque_angry_segments_0003.csv`

## Discovery / verification

Fresh direct-repository and web discovery reconfirmed PAVOQUE as a qualifying primary production source. The public `marytts/pavoque-data` repository documents a single male native German speaker recorded in a studio across neutral, cheerful, depressed, aggressive, and poker styles. Its YAML files provide prompt, text, style, utterance start/end, and optional manually corrected phonetic segments with SAMPA-based labels and relative end times. Independent corpus documentation states that phone-level segmentation was manually checked/corrected by trained annotators. License remains CC BY-NC-SA 4.0.

No secondary speech collections or automatic-only annotations discovered in this pass were promoted to core.

## Validation

- 56 segment IDs unique.
- Segment indices contiguous 1–56.
- End times strictly nondecreasing.
- Derived relative durations nonnegative.
- Absolute boundaries equal utterance start + relative boundaries.
- Final supplied segment endpoint 5.995 s lies within the nominal 6.00 s utterance window (5.94–11.94 s), leaving 0.005 s unsegmented; source value retained rather than forcing closure.
- No invented IPA/context.
- Writes confined to `manual_phonetic_production/`.

## Next priorities

1. Continue PAVOQUE `spike0004+`, then other styles, preserving all utterance metadata and manually corrected phone boundaries.
2. Continue JSUT BASIC5000 from `0941`.
3. Retrieve and parse Chenghai `textgrid.zip` and `data analysis.xlsx` exhaustively.
4. Verify public Bangla Speech Corpora manual annotation files and license.
5. Continue DanPASS TextGrid and CLIPS_MT_MANUAL corrected-layer retrieval.
