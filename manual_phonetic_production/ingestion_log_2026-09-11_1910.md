# Manual phonetic production ingestion log — 2026-09-11 19:10 PT

## Backlog ingestion

Canonical PAVOQUE state was read before writing. Upstream `marytts/pavoque-data/pavoque-angry.yaml` was re-read directly; blob SHA remains `bff79d978f740f6c2b09bc595ade03b6bdf33423`.

Materialized source-faithful chunk 011:

- `spike0037` — `Ich verstehe nicht ganz – wieso bloß bist du so gut gelaunt?` — 45 manually corrected German-SAMPA/silence intervals; utterance 128.70–133.80 s; last annotation endpoint 5.095 s for a 5.100 s utterance.
- `spike0038` — `Bekommst du manchmal eine Wut auf die Welt?` — 34 manually corrected intervals; utterance 133.80–137.88 s; last annotation endpoint 4.075 s for a 4.080 s utterance.

Committed files:

- `data/source_faithful/pavoque_angry_utterances_chunk011.csv` — 2 utterance rows.
- `data/source_faithful/pavoque_angry_segments_chunk011.csv` — 79 interval rows.

Original labels and cumulative endpoints are retained. Interval starts/durations are deterministic derivatives. No IPA or missing segment context was invented. Both 5-ms source-native tail residuals were preserved.

Canonical PAVOQUE status now records 11 chunks, 36 utterances, and 1,138 intervals. `spike0039` is the next verified upstream record. Chunk011 was added to the canonical file manifest with exact repository blob SHAs and byte sizes.

These 81 durable rows do not increase the global scientific-observation count because the same annotation lineage is already represented in the validated full PAVOQUE CI parse.

## IFA backlog work

The accepted IFA Spoken Language Corpus deposit was reopened at Zenodo record 14904090. The deposit confirms that the corpus is hand-segmented Dutch speech and reports 1,000 hours of labeling, 8 phonemically segmented speakers, 19,867 segmented speech-seconds, 4,492 segmented sentences, 51,782 words, 74,702 syllables, and 187,544 phonemes.

File-level direct download URLs were resolved for the compact annotation archives, including:

- `Labels-chunks.zip`
- `Labels-validation.zip`
- `LabelProtocol.zip`

The direct Zenodo URL for `Labels-chunks.zip` was attempted in the execution environment but archive-byte retrieval failed. This is recorded as a transient retrieval failure, not as an absence of data. No IFA observation rows are claimed. The canonical IFA ingestion-status file now stores the verified direct URL templates so subsequent passes can retry without rediscovering them.

## Fresh discovery

Added a new HIGH REVIEW increment:

### ROG: A Multi-Layer Manually Annotated Corpus of Spoken Slovenian (2026)

The LREC 2026 resource contains ~75,000 words / ~10 hours and integrates manual prosodic, disfluency, interactional, morphosyntactic and dialogue layers; approximately half of the audio is reported freely available under CC-BY. It is not admitted directly to core because it builds on pre-existing SST/GOS 2 recordings. Under the project inclusion rules, only genuinely new manual production/prosodic annotation layers that can be isolated from reused recordings are potentially admissible. Next work is to locate the public repository, enumerate files, and verify that the new prosodic annotations carry usable time alignment/landmarks.

## Validation

- New PAVOQUE utterance IDs are namespaced as `pavoque::spike0037` and `pavoque::spike0038`.
- All 79 segment rows resolve to those utterances.
- Segment indices are unique and monotonically ordered within utterance.
- All derived durations are nonnegative endpoint differences.
- PAVOQUE upstream SHA is unchanged.
- No raw audio was committed.
- No IFA rows were counted after the download failure.
- ROG remains REVIEW rather than being admitted on publication-level evidence.
- All writes are confined to `manual_phonetic_production/`.

## Reconciled state

- 150 tracked sources
- 46 accepted
- 25 excluded
- 79 review
- 44 materially ingested sources
- 1,361,571 distinct promoted/parsed observations across 86 logical tables
- 1,017 inventory-file records
- 1,148 variable-dictionary records
- PAVOQUE durable mirror: 36 utterances / 1,138 intervals; next `spike0039`

## Next backlog priorities

1. Continue PAVOQUE at `spike0039`, retaining all original endpoints and labels.
2. Retry IFA compact archive retrieval using the now-persisted direct Zenodo URLs; parse TextGrids and validation annotations immediately if bytes are reachable.
3. Locate and enumerate the ROG repository and isolate genuinely new manual prosodic/time-aligned annotation layers from reused GOS 2/SST material.
4. Enumerate the English-child Zenodo archive and isolate its manually segmented free-speech subset.
5. Continue observation-level ingestion of Chenghai Southern Min, the manually labelled rt-MRI source, and the Bengali–English five-timepoint F1/F2 trajectory source.
