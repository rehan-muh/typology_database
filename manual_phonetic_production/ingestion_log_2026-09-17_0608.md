# Ingestion pass — 2026-09-17 06:08 PT

## Discovery

Screened repository-focused searches for public manually annotated speech-production deposits. Added **PxCorpus v4** (Zenodo DOI 10.5281/zenodo.10080490) to **review**, not accepted/core.

Evidence from the authoritative Zenodo record: primary French drug-prescription speech was collected experimentally from 55 participants; the PxSLU documentation reports 903 sessions and 1,981 synchronized recording rows (262.27 min). Recordings are mono 16 kHz / 16-bit signed PCM. Transcriptions were automatically generated then verified by humans / described as manually transcribed; semantic labels and dialogue OK/Fail annotations are manual. CC BY 4.0. Archive `PxCorpus.zip` is 375.7 MB with MD5 `97247fc5278cf4e3ab147aab4fc72f14`.

## File-level inventory

Manifested the public archive plus the documented internal objects: `PxSLU/recordings/`, `paths.txt`, `metadata.txt`, `seq.in`, `seq.label`, `seq.out`, `PxSLU_conll.txt`, `PxDialogue/events.txt`, and `PxDialogue/dialogue_annotations.txt`. Important lineage constraint: `PxSLU_conll.txt` is explicitly documented as not acoustically aligned to `paths.txt`; parallel row linkage should instead use `paths.txt` + `metadata.txt` + `seq.*`.

## Inclusion decision

**REVIEW ONLY.** The source clearly satisfies primary, non-singing speech and human verification at transcript/semantic level, but current public documentation does not establish manually created/corrected phonetic intervals, acoustic landmarks, or human-verified acoustic measurements. Under the database's strict inclusion rule, orthographic/semantic manual annotation alone is insufficient. No observation rows were promoted to core and no phonetic measurement counts were claimed.

## Backlog ingestion work

Checked the live review queue before writing to avoid duplicating known candidates. This pass therefore records a genuinely new candidate and an actionable file manifest rather than another paper-level citation. Archive parsing remains pending because the connected GitHub interface cannot ingest the 375.7 MB Zenodo binary directly in this run; authoritative checksum and internal object map are preserved for a later materialization/download-capable pass.

## Validation / provenance

- Changes restricted to `manual_phonetic_production/`.
- No unrelated repository content modified.
- No automatic transcription treated as manual phonetic alignment.
- No core observations added from a source lacking demonstrated manual phonetic annotation.
- Dataset/version lineage fixed to Zenodo v4 DOI 10.5281/zenodo.10080490; older v3 record 10.5281/zenodo.6524162 is the same evolving corpus and must not be counted separately.

## Next priorities

1. Continue actual observation-level ingestion from accepted backlog (L2-ARCTIC human TextGrids/error annotations; JSUT manual labels beyond BASIC5000_1430; VoxAngeles audited layer).
2. Reconcile/deduplicate PAVOQUE overlapping chunks before warehouse rebuild.
3. Materialize/download PxCorpus archive when a binary-capable path is available and inspect for any undocumented TextGrid/EAF/phonetic measurement layer; otherwise retain as review/exclude from phonetic core.
4. Continue repository discovery emphasizing manually corrected phone boundaries plus acoustic/articulatory measurements rather than transcription-only corpora.
