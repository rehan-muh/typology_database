# Ingestion pass — 2026-09-19 12:08 PT

## Discovery

High-rigor web/repository discovery was run for manually annotated/corrected primary speech-production resources, emphasizing repository records and layer-level qualification.

### New review candidates

1. **RescueSpeech (Zenodo 8077622, v2)** — primary German speech from simulated search-and-rescue exercises. Zenodo explicitly describes the recordings as manually annotated, but the landing page does not establish whether the human annotation is phonetic/temporal versus transcription-only. **REVIEW, not core.** Next: enumerate payload and inspect annotation schema/workflow.
2. **ROG (LREC 2026)** — 75k+ words / ~10 h Slovenian speech with manually annotated prosodic units on approximately half the corpus, plus disfluency/dialogue layers. Because it builds on pre-existing SST/GOS2 material, lineage and isolation of the new manual prosodic annotations must be established before core admission. **REVIEW.**
3. **IFCASL** — paper reports word/phone segmentation with >50% manually corrected material and inter-annotator agreement. This is a strong qualifying layer, but automatic and manually corrected observations must be separable. **REVIEW pending file-level isolation.**
4. **Spontal-N** — ~7% of automatically generated phoneme annotation was manually corrected and treated as gold standard. The corrected subset qualifies in principle; the remaining forced-aligned material does not. **REVIEW pending observation-level isolation.**

### Rejections / non-core decisions

- COVYT: manual segmentation exists, but source recordings are mined third-party social-media material; excluded by primary-recording criterion.
- Zebra finch vocalization dataset: non-human animal vocal production, outside intended human phonetic-production scope.
- AutoVOT generic outputs: automatic predictions remain excluded; only genuinely manually labelled training examples are admissible.
- ROG morphosyntax/dialogue-act layers alone are not sufficient; only a defensibly isolated manual production/prosodic layer may qualify.

## Backlog ingestion / audit

- Re-audited the durable source-faithful tree and registry structure before writes. Existing source-faithful assets include explicit source manifests/dictionaries and manually labelled AutoVOT tutorial observations; registry increments are maintained separately.
- Re-opened the authoritative JSUT `e2e_symbol/phoneme.yaml` upstream source. The GitHub response is too large and is truncated by the connector before the `BASIC5000_1570` cursor, and repository code search did not return the key. **No JSUT observations were written this run** because accepting guessed or partial sequences would violate source-faithful ingestion.
- Added a durable review increment with explicit qualifying/nonqualifying layers and next actions so future passes do not repeat discovery work.

## Files written

- `data/registry/review_increment_2026-09-19_1208.csv`
- `ingestion_log_2026-09-19_1208.md`

## Validation

- All writes are confined to `manual_phonetic_production/`.
- No automatic/forced-aligned observations were promoted to core.
- No third-party media collection was admitted.
- No unknown IPA, contexts, timings, counts, units, or tier semantics were invented.
- Review candidates explicitly identify the human layer that must be isolated before acceptance.

## Next backlog priorities

1. Obtain/inspect RescueSpeech v2 payload and determine whether its manual annotations contain timed phonetic/prosodic intervals.
2. Locate ROG public repository and isolate newly created manual prosodic units from inherited SST/GOS2 layers.
3. Resolve IFCASL distribution and per-observation/manual-correction provenance.
4. Locate Spontal-N corrected gold-standard phoneme subset and ingest only those observations.
5. Continue JSUT from `BASIC5000_1570` only after retrieving an authoritative non-truncated range.
6. Continue UltraSuite manual `slt_labels` / manually revised `reference_labels`, L2-ARCTIC 3,599 human-reviewed utterances, PAVOQUE durable cursor, SPIRE VCV manual boundaries/EMA, and Chenghai stop TextGrid/workbook payloads when accessible.
