# Manual phonetic production ingestion log — 2026-09-12 04:07 PDT

## Backlog ingestion

- Advanced PAVOQUE angry durable source-faithful materialization from `m0021` through `m0026`.
- Added `data/source_faithful/pavoque_angry_utterances_chunk021.csv`: 5 utterances (`m0022`–`m0026`) in literal upstream order.
- Added `data/source_faithful/pavoque_angry_segments_chunk021.csv`: 138 manually corrected German-SAMPA/silence intervals.
- Added `data/source_faithful/pavoque_angry_file_manifest_chunk021.csv` with exact repository blob SHAs and immutable upstream blob lineage.
- Updated `data/source_faithful/pavoque_angry_ingestion_status.json` to 21 durable chunks, 77 utterances, and 2,244 intervals. Next verified upstream record: `m0027`.

## Source-faithful validation

- Original utterance text, style, source times, German-SAMPA/silence labels, and cumulative segment endpoints retained.
- Namespaced IDs use `pavoque::source_id` conventions.
- Interval starts and durations are deterministic derivatives; originals were not overwritten.
- No IPA values were invented.
- Segment sequences are monotonic and positive-duration within each ingested utterance.
- Terminal source timing is retained exactly rather than regularized: `m0022`, `m0024`, `m0025`, and `m0026` end 0.005 s before the nominal utterance duration; `m0023`'s final manual annotation endpoint is 0.010687 s beyond the nominal utterance duration. This is flagged as a source-native timing inconsistency, not silently repaired.
- Global distinct scientific-observation count is unchanged because the PAVOQUE lineage is already represented in the validated full-corpus parse.

## Discovery and screening

### Consonant Challenge Corpus — re-verified high-priority manual subset

- Zenodo DOI: 10.5281/zenodo.3538223.
- Primary production corpus: 12 female + 12 male native-English talkers; 10,368 VCV tokens overall.
- `handsegm.91.mlf.txt` is explicitly documented as 91 hand-segmented VCVs in HTK format; published MD5 `6cb12316c67b4303760e91809c29bd90` and size 8.0 kB.
- `segmentation_training.mlf.txt` and `segmentation_testsets.zip` are explicitly automatic and therefore excluded from the manual core.
- Direct byte retrieval of the manual HTK file was not available through the current web/repository execution route, so no token/interval observations were claimed or fabricated. Priority remains to parse only the isolated manual file and link its VCV IDs back to the authoritative audio manifests.

### Additional screening

- Jingju a Cappella Recordings Collection exposes extensive manually segmented Praat TextGrids, including phoneme-level X-SAMPA tiers, but is singing and therefore remains excluded under the project rules.
- PxCorpus contains primary French prescription recordings with human-verified transcription and semantic annotation; it was not promoted on the current evidence because the relevant public annotation claim is not phonetic interval/landmark/acoustic annotation.

## Counts after reconciliation

- tracked sources: 153
- accepted: 46
- excluded: 25
- review: 82
- materially ingested: 44
- distinct promoted/parsed observations: 1,361,571
- logical tables: 86
- inventory-file records: 1,037
- variable-dictionary records: 1,148

## Next backlog priorities

1. Continue PAVOQUE from `m0027` in literal upstream order.
2. Retrieve and parse Consonant Challenge `handsegm.91.mlf.txt` only; explicitly exclude its automatic segmentation files.
3. IFA hand-segmented label archives.
4. Hu Tibetan trilingual English-vowel F1/F2 trajectories.
5. Bengali–English five-timepoint F1/F2 trajectory deposit.
6. Manually corrected L2-ARCTIC subset only.
7. Chenghai Southern Min production data.
8. Manually labelled rt-MRI material.

All repository writes in this pass were confined to `manual_phonetic_production/`.
