# Manual phonetic production ingestion — 2026-09-13 03:13 PT

## Backlog ingestion: CCOST

- Continued from the validated checkpoint at metadata source row 89.
- Opened the authoritative Zenodo `CCOST_metadata.csv` and ingested source rows **89–118** (30 recording records).
- Preserved all **37 original columns and values** in `data/source_faithful/ccost_2026/CCOST_metadata_chunk003_rows089_118.csv`.
- Retained quality flags, SNR/intensity statistics, duration and interval-count fields, task/order fields, original filenames, and free-text manual-correction/annotation notes without normalization or deletion.
- Added 30 collision-safe recording links in `data/warehouse/ccost_2026_recordings_chunk003.csv`.
- Added new speaker projections for S13–S17 in `data/warehouse/ccost_2026_speakers_chunk003.csv`; S12 was already present and was not duplicated.
- No IPA, phonological context, or missing metadata was inferred.
- Checkpoint advanced to **118/488 metadata rows**; next row is **119**.
- CCOST source-faithful total is now **324 rows** when the previously ingested 206-row wordlist is included.
- Large 6.2-GB audio/TextGrid archive remains URL/checksum referenced rather than mirrored; annotation-only extraction remains pending.

## Discovery and screening

- Ran fresh repository/literature searches for manually corrected TextGrid, formant, VOT, alignment, and production corpora.
- MRPL2 surfaced again, but repository deduplication showed it is already tracked; no duplicate candidate was created.
- Added **SUMM-RE** to HIGH review. It is primary non-singing French conversational speech released CC BY-SA 4.0. Public documentation distinguishes manually transcribed/aligned dev/test material (roughly 24–25 h) from an explicitly automatic train split (~70 h). Records expose meeting/speaker/audio IDs plus segment and word timestamps. Promotion is blocked until documentation establishes whether timing boundaries themselves were exhaustively human-verified rather than only transcript text. Automatic train alignments are out of scope regardless.

## Validation

- CCOST source row range is non-overlapping with chunks 001 (1–48) and 002 (49–88).
- All recording IDs are namespaced as `ccost_2026::source_recording_id`.
- All speaker IDs are namespaced as `ccost_2026::S#`.
- Original source columns remain unchanged in the source-faithful layer.
- Harmonized projections do not overwrite source values.
- No raw audio was committed.
- Root status updated to **171 tracked sources: 47 accepted, 29 excluded, 95 review, 45 materially ingested** and **1,361,895 parsed source observations**.

## Next priorities

1. Continue CCOST metadata at row 119 without repeating earlier rows.
2. Prioritize annotation-only extraction of CCOST TextGrid word/syllable/phone/toneme tiers without mirroring WAV audio.
3. Resolve exact manual-timing provenance for SUMM-RE dev/test before any core promotion.
4. Continue screening primary acoustic/articulatory production deposits, emphasizing observation-level public files rather than paper-only registrations.
