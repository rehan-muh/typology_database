# Manually Annotated Phonetic Production ingestion pass — 2026-09-13 15:13 PT

## Scope

Aggressive combined backlog-ingestion + discovery pass. Writes were restricted to `manual_phonetic_production/`.

## Backlog ingestion / harmonization

### CCOST (`ccost_2026`)

Source-faithful metadata remains committed through authoritative `CCOST_metadata.csv` source row 195 of 488. This pass did not fabricate rows 196+ after the Zenodo record/file endpoint timed out during retrieval.

Materially harmonized an additional exact block, source rows **30–48** (19 recordings), from immutable source file:

- `data/source_faithful/ccost_2026/CCOST_metadata_chunk001_rows001_048.csv`
- source Git blob: `a4a3990c9e8c16d70010ff6ec023077e79e2336b`

Created:

1. `data/warehouse/ccost_2026_recording_quality_conditions_rows030_048.csv` — 19 rows
   - namespaced recording and speaker IDs
   - task (INT/WL/ST)
   - interviewer gender
   - source quality grade
   - SNR dB
   - quality flags/comments
   - word-list order
   - pace condition
   - misread frequency
   - total/labeled duration
   - interval count
   - exact source-row provenance

2. `data/warehouse/ccost_2026_acoustic_summary_rows030_048.csv` — 19 rows
   - mean intensity dB
   - max intensity dB
   - min intensity dB
   - intensity SD dB
   - dynamic range dB
   - peak level dBFS
   - peak intensity dB
   - intensity threshold dB
   - average speech dB
   - average noise dB
   - exact source-row provenance

3. `data/warehouse/ccost_2026_rows030_048_harmonization_manifest.json`
   - records source/output Git blobs, row counts, ID rules, transformations, validation, and double-counting guard.

Validation:

- 19 unique source recording IDs in each new projection.
- Source rows 30–48 map one-to-one to each table.
- Missing condition values remain empty rather than synthetic categories.
- No source acoustic value was normalized, imputed, rounded, unit-converted, or overwritten.
- No IPA, segment context, or missing labels were inferred.
- These are analysis-ready projections of observations already represented source-faithfully, so they **do not increment** the distinct scientific-observation total.

CCOST cumulative harmonized coverage is now:

- source-faithful metadata: 195/488 rows
- speakers: 24 harmonized speaker rows
- recordings: 195 harmonized recording rows
- recording quality/condition projections: 36 rows (source rows 30–48 and 179–195)
- acoustic-summary projections: 36 rows (same source ranges)
- stimuli: complete 206-row word list
- next authoritative metadata checkpoint: **row 196**

## Fresh discovery

### HIGH review — TaL Tongue and Lips Corpus (`tal_corpus_2021`)

Authoritative documentation: `https://ultrasuite.github.io/data/tal_corpus/`

Why it matters:

- primary non-singing English production
- 82 speakers total (TaL1 single speaker across six sessions + TaL80 81 speakers)
- synchronized waveform, raw ultrasound tongue imaging, lip video and synchronization signal
- approximately 24 h total parallel data / 13.5 h speech reported in corpus publication
- spontaneous utterances have **manually annotated** shorter segment boundaries, typically 5–10 s, with start/end times and transcription in `.lab` CSV-like files
- public documentation states CC BY-NC 4.0 distribution

Decision: **review, not core yet**. Before promotion, enumerate TaL1/TaL80 annotation/document files, distinguish manual from automatic layers, inventory participant metadata and synchronization semantics, and determine whether any manual phone/word landmarks or derived articulatory measurements are distributed. Do not mirror ~547 GB raw data to GitHub; retain authoritative paths/checksums and ingest annotation/metadata plus derived structured observations.

### HIGH review — MRPL2 Non-native Speech Corpus (`mrpl2_2026`)

Authoritative record: `https://zenodo.org/records/20365865`

Why it matters:

- primary non-singing L2-English production
- 4,500 utterances
- 23 non-native speakers
- 14 L1 backgrounds
- every utterance reported as manually annotated for phonetic mispronunciations relative to Modern Received Pronunciation
- deposit advertises annotated TextGrids, word transcripts, 48/16-kHz audio, plus corpus-wide expected and ground-truth phoneme sequence masters

Decision: **review, not core** because the Zenodo files are currently restricted. If access is authorized, inventory every TextGrid/master file; preserve expected vs. ground-truth sequences separately; ingest manual boundaries/error labels; namespace all IDs; and exclude any automatic-only alignment layer.

## Retrieval / screening notes

- The CCOST Zenodo record itself remained discoverable and reconfirmed the corpus/file inventory, but direct file retrieval timed out in this run; therefore no source rows 196+ were reconstructed from partial web snippets.
- TaL manual segmentation semantics were independently corroborated in the public UltraSuite documentation source repository: manually annotated short segments are distributed as `.lab` files with start/end seconds and transcription.
- MRPL2 was retained in review rather than represented as ingested because file-level access is restricted.

## Database status after reconciliation

- total sources: **186**
- accepted: **47**
- excluded: **31**
- review: **108**
- materially ingested: **45**
- distinct promoted scientific observations: **1,361,972**
- logical tables: **94**
- source-inventory records: **1,109**
- variable-dictionary records: **1,197**

## Next backlog priorities

1. CCOST: resume exact `CCOST_metadata.csv` ingestion at source row 196; never repeat 1–195.
2. CCOST: harmonize rows 1–29 and 49–178 into the same quality/condition + acoustic schemas, preserving row-level lineage.
3. CCOST: obtain `CCOST_manual.pdf` and enumerate manually corrected TextGrid tiers without mirroring WAV audio.
4. TaL: enumerate `.lab`, participant metadata and documentation files; establish source counts and checksums; promote only verified manual layers.
5. PAVOQUE: continue from verified literal YAML checkpoint `m0133` only when the complete source segment list can be retrieved exactly.
6. Continue repository/journal discovery with emphasis on manual VOT, voice quality, EGG, ultrasound/EMA/EPG, prosody/intonation, L2 production and clinical phonetic error labels.
