# Ingestion pass — 2026-09-14 08:10 PDT

## Backlog ingestion

### JSUT-label BASIC5000 manual annotation layer
- Advanced the authoritative manual symbolic layer from rows 1–120 to rows 1–160.
- Added `data/source_faithful/jsut_label_basic5000_manual_phoneme_rows_0121_0160.yaml`, preserving the 40 source values `BASIC5000_0121`–`BASIC5000_0160` verbatim from `sarulab-speech/jsut-label/e2e_symbol/phoneme.yaml` (Git blob `ae8933677c082c22238a895012b6b6dcba1f6625`).
- Added `data/harmonized/jsut_label_basic5000_manual_annotation_index_0121_0160.csv` with 40 collision-safe IDs and explicit source-row/value pointers.
- Manual layer: phonetic and prosodic symbolic annotation.
- Automatic Julius timing remains excluded (`automatic_not_ingested`); no automatic boundaries were admitted to the manual core.
- Underlying recording lineage remains `JSUT::BASIC5000`; the 40 new observations are annotation-layer observations and do not create 40 new recordings.
- Updated ingestion checkpoint to row 160; next source row is 161 / `BASIC5000_0161`.

### Validation
- Source-faithful slice contains exactly 40 contiguous source keys, 121–160.
- Harmonized slice contains exactly 40 linked rows, each with a unique namespaced utterance ID.
- All 40 rows point to `e2e_symbol/phoneme.yaml` and preserve the same source utterance key in `source_value_pointer`.
- All 40 rows have `manual_annotation_status=manual`, `timing_status=automatic_not_ingested`, and `underlying_recording_lineage=JSUT::BASIC5000`.
- Total JSUT manual symbolic rows now represented: 160.

## Discovery

### NURC/SP Minimum Corpus — HIGH review
- Added `nurc_sp_minimum_corpus` to the review queue.
- Authoritative repository states that the CORAA NURC-SP Minimum Corpus is a manually annotated Brazilian Portuguese spontaneous-speech corpus consisting of 21 audio files with audio-aligned multilevel transcripts organized according to linguistically motivated intonation units.
- Parent NURC/SP contains 375 inquiries / 334 hours, but the Minimum Corpus is a subset of legacy NURC recordings. This is therefore a secondary-recording lineage case.
- Potential eligibility rests on the user's explicit exception for genuinely new manual annotations that can be isolated from pre-existing recordings.
- Did **not** promote to core: exact tier-level manual versus automatic provenance and file-level annotation inventory have not yet been verified from the linked Portulan CLARIN data deposit.
- Recording deduplication rule if later promoted: retain legacy NURC recording lineage and count only the newly created manual annotation layer as new observations.
- Repository license is CC BY-NC-ND 4.0; redistribution restrictions must be respected.

## File/provenance inventory added
- Refreshed JSUT `phoneme.yaml` manifest checkpoint to rows 1–160.
- NURC/SP `README.md`: Git blob `acb2cb20c84eb2758cc1bcb6f19dbeaf4ff29bc1`.
- NURC/SP `LICENSE`: Git blob `9badf7abf51e412ff7bbe1ce7b66c5b05145138c`.
- Linked Portulan CLARIN corpus record retained as authoritative data-reference object; file enumeration pending.

## Errors / limits
- No source values were inferred or reconstructed.
- NURC/SP linked dataset files were not promoted because the present pass established corpus-level manual provenance but not per-tier provenance.

## Next backlog priorities
1. Continue JSUT from `BASIC5000_0161` while preserving manual symbols and excluding automatic timing.
2. Enumerate and parse the NURC/SP Minimum Corpus Portulan CLARIN files; isolate genuinely new manual annotation tiers from legacy recordings.
3. Retry IFA `DatabaseFiles.zip`, `LabelProtocol.zip`, and `Labels-*` archives for real TextGrid/TSV ingestion.
4. Ingest L2-ARCTIC v5 manual TextGrids once licensed package access is available; keep forced-alignment-only tiers out.
5. Enumerate Parker sonority Mendeley v6 at per-WAV / annotation-file level.
