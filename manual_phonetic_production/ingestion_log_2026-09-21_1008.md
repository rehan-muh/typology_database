# Ingestion pass — 2026-09-21 10:08 PT

## Discovery

Accepted `AUSPEECH_YANG2024`, *An Audio-Ultrasound Synchronized Tongue Movement Database for Mandarin speech*. Upstream documentation describes synchronized 16-kHz mono audio, ultrasound tongue imaging (DICOM), LAB/TextGrid annotations, 54 participants (43 healthy, 11 dysarthria), and approximately 700 GB total data. Inclusion evidence is unusually explicit: normal-speaker alignments were generated with MFA and then manually labeling-checked for verification; patient audio pronunciation labeling was manually annotated. Core admission is restricted to those human-verified/manual layers; automatic intermediates remain excluded.

Fresh repository/Zenodo searches also re-surfaced MRPL2 and RescueSpeech. MRPL2 remains restricted and already in REVIEW; RescueSpeech remains REVIEW because inspected public metadata says recordings are manually annotated but does not yet establish a phonetic/prosodic manual layer. No duplicate registry entries were created.

## Backlog ingestion performed

Enumerated the AUSpeech public repository root and `Example/` directory. Recorded immutable Git blob SHAs for README, TextGrid, LAB, WAV and DICOM. Parsed `Example/speaker00042_F_s1_stn00021.TextGrid` source-faithfully: duration 2.51806 s, one `words` IntervalTier, three intervals: [0,0.79] empty, [0.79,0.8] `yu1`, [0.8,2.51806] empty. No IPA was inferred from `yu1`. Raw WAV and ~99.8 MB DICOM were referenced/checksummed but not copied.

Added five variable-dictionary records for exact interval start/end, original text label, filename speaker ID and filename sex/gender code. The latter is retained as a source code because its semantics were not independently documented in the inspected README.

## Counts and integrity

Canonical counts after this pass: 232 sources = 57 accepted + 32 excluded + 143 review. Parsed observations: 1,364,270 across 206 logical tables. Inventory records: 1,237. Variable dictionary records: 1,261. `ingested_sources` remains 50 because AUSpeech is only partially ingested. Namespaced speaker convention specified as `AUSPEECH_YANG2024::speaker00042`.

## Next priorities

1. Locate the authoritative full AUSpeech data deposit and license; enumerate full LAB/TextGrid inventory and separate healthy/manual-verified vs patient/manual annotation layers.
2. Continue complete Marathi VOT TextGrid/XLSX extraction rather than merely inspecting individual examples.
3. Parse accessible VoxAngeles measurement TSVs and audited alignment payloads while keeping forced-alignment-only products excluded.
4. Continue IFCASL, CORPRES and L2-ARCTIC isolation of manually corrected subsets.
