# Ingestion pass — 2026-09-19 15:08 PT

## Discovery
Fresh repository-focused search was run for manual phonetic production annotations/measurements, emphasizing Zenodo/OSF and recent deposits. No newly discovered source met the core criterion strongly enough to admit during this pass. False positives were rejected rather than padded into the registry: PREMOVE (textual Ancient Greek/Latin annotation), biomedical/image segmentation deposits, and Jingju a-cappella (singing). Previously known EWA-DB remains access-restricted and layer-ambiguous; PxCorpus remains transcription/semantic rather than demonstrated phonetic annotation; COVYT remains excluded as third-party social-media audio.

## Backlog ingestion / source verification
Re-opened the authoritative sarulab-speech/jsut-label `e2e_symbol/phoneme.yaml` directly through GitHub. Verified that the upstream source contains a continuous block beginning at `BASIC5000_1570`; the retrieved window includes complete source strings through at least `BASIC5000_1612`, with `BASIC5000_1613` beginning before connector truncation. This re-establishes `BASIC5000_1570` as a valid continuation cursor and proves that the earlier upstream-fetch blockage was transient rather than an upstream data gap.

I did not create observation rows in this pass because the repository's current JSUT harmonized/source-faithful continuation filenames could not be reliably resolved from the truncated directory listing, and GitHub code search did not expose the recent blocks. Creating a new block without first proving the exact existing boundary risks duplicate underlying observations, which violates the lineage-level deduplication requirement. No values were guessed or normalized.

## Integrity / provenance checks
- Writes confined to `manual_phonetic_production/`.
- No automatic/forced-aligned observations admitted.
- No third-party media, singing, perception-only, synthetic, or textual-only datasets admitted.
- No inferred IPA, timing, speaker metadata, or context.
- Existing architecture confirmed to include source-faithful, harmonized, registry, ingestion-status, coarticulation, and warehouse-oriented material.

## Next backlog priorities
1. Resolve the exact current JSUT block boundary from repository tree/blob metadata, then ingest the verified `BASIC5000_1570+` source strings without duplication.
2. Enumerate/download UltraSuite UXTD/UXSSD/UPX archives and ingest only manual `slt_labels` and manually revised `reference_labels`; quarantine automatic tiers.
3. Isolate the 3,599 human-reviewed L2-ARCTIC utterances from corpus-wide forced alignments.
4. Continue PAVOQUE at its durable cursor and ingest remaining source-faithful manual labels/measurements.
5. Resolve file/tier-level manual subsets for IFCASL and Spontal-N before any observation-level admission.
6. Retrieve the Chenghai stop-study TextGrid/XLSX payloads and ingest manual VOT/GAP landmarks plus all workbook variables when binary access is available.
