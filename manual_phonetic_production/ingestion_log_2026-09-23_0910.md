# Ingestion pass — 2026-09-23 09:10 PT

## Discovery
- Accepted `rescuespeech_2023` after fresh repository verification. Zenodo v2 describes primary German speech recorded during simulated search-and-rescue exercises and explicitly says the recordings are manually annotated. Core scope is restricted to the clean human recordings/manual transcripts; synthetic/noise-contaminated enhancement derivatives are not independent observations.
- Re-screened MRPL2: 4,500 utterances / 23 non-native speakers / 14 L1s with manual phonetic-mispronunciation annotation, but Zenodo files remain restricted; retained as access backlog, not promoted.
- Re-screened German-English Code-Switching Corpus: derives from Spoken Wikipedia. Although upstream SWC XML is described as manually annotated at word and segment levels, this is secondary/resegmented material; no core admission without isolatable new manual annotation.
- Explicitly screened out singing/music and non-human vocalization hits returned by broad manual-segmentation searches.

## Backlog ingestion work
- Attempted object-level retrieval of accepted `pxcorpus_2023` from Zenodo v4. Repository documentation confirms a 375.7 MB archive (MD5 `97247fc5278cf4e3ab147aab4fc72f14`) containing synchronized `paths.txt`, `metadata.txt`, `seq.in`, `seq.out`, `seq.label`, plus dialogue `events.txt` and manual `dialogue_annotations.txt`. The current runtime rejected/failed the 375.7 MB binary transfer, so no fabricated observation rows were added. Archive parsing remains pending.
- PxCorpus documentation gives 903 recording sessions and synchronized row structure; metadata columns are participant id, age range, gender, and participant category; labels comprise intent, human-verified transcript, BIO entities, dialogue events/timestamps, and manual OK/Fail dialogue judgments. These mappings remain valid backlog targets once the archive can be materialized.

## Integrity / lineage decisions
- No automatic/forced-aligned data admitted as manual phonetic observations.
- No synthetic speech, singing, perception-only data, third-party media, or duplicate derivative recordings admitted.
- No counts were incremented for observation rows because no new observation-level archive was successfully parsed in this run.
- All repository writes are confined to `manual_phonetic_production/`.

## Next priorities
1. Materialize and parse PxCorpus archive; verify line-count identity across synchronized files before row ingestion.
2. Retrieve RescueSpeech clean ASR archive/transcript manifest and ingest utterance-level transcript observations; keep enhancement derivatives only in lineage.
3. Continue CCOST TextGrid member enumeration and PAVOQUE source-faithful promotion only after canonical ID reconciliation.
4. Revisit MRPL2 only if restricted-file access becomes available.
