# Ingestion pass — 2026-09-15 12:10 PT

## Backlog ingestion

Advanced PAVOQUE angry-style manual segmentation from `spike0006` through `spike0008`.

- Authoritative source: `marytts/pavoque-data/pavoque-angry.yaml`.
- Inclusion evidence: repository documentation explicitly describes supplied phonetic segments as manually corrected.
- Newly processed utterances: `spike0007`, `spike0008`.
- New segment/interval observations: 25 (18 + 7).
- Preserved source-faithful YAML: `data/source_faithful/pavoque_angry_utterances_0007_0008.yaml`.
- Harmonized segment table: `data/harmonized/pavoque_angry_segments_0007_0008.csv`.
- Original SAMPA-like labels and author-supplied endpoints retained verbatim; normalized IPA intentionally left blank because no explicit source mapping was applied.
- Derived fields are additive only: relative starts, durations, absolute starts/ends, collision-safe utterance/segment IDs.
- Validation: 25/25 unique segment IDs; contiguous segment indices within each utterance; monotonic endpoints; nonnegative durations; absolute endpoints reconcile with utterance offsets. Both source segmentations end 0.005 s before the nominal utterance boundary; this discrepancy is preserved rather than silently altered.
- No audio committed.

## Discovery

Fresh web/repository screening covered manually corrected phonetic segmentation and clinical/atypical speech production. PAVOQUE's inclusion evidence was reconfirmed. IFCASL remains high-priority because published corpus documentation reports phone/word segmentation with more than 50% manually corrected data, but the publicly retrievable corrected layers and redistribution/access terms still need verification before core ingestion.

A new/high-value review target is **HeyJay!** (Moro-Velazquez et al., Scientific Data, published 2026-06-03): primary atypical-speech WAV recordings, utterance transcription/intent tables, participant demographic/clinical metadata, and multiple independent SLP perceptual ratings are publicly described. It is not promoted to core in this pass because the manual-vs-automatic provenance of each production annotation layer must be verified first; the SLP perceptual annotations appear human-created and may qualify as production judgments if tied directly to these primary recordings.

Rejected discovery noise included NLP/text-only manual annotation, synthetic/TTS-only resources, perception-only material, and secondary reuse without isolatable new manual production annotation.

## Database delta

- parsed/source-faithful observations: 1,363,153 -> 1,363,178 (+25)
- logical tables: 175 -> 177 (+2)
- registry counts unchanged pending high-rigor candidate verification: 213 total; 52 accepted; 31 excluded; 130 review; 49 materially ingested

## Next priorities

1. Continue PAVOQUE at `spike0009+`, preferably in larger contiguous batches.
2. Retrieve and parse Chenghai `textgrid.zip` and `data analysis.xlsx` exhaustively.
3. Verify/download IFCASL corrected TextGrid layers and isolate corrected tiers.
4. Verify HeyJay! annotation provenance and repository payloads; ingest human SLP production judgments if qualifying.
5. Continue JSUT at `BASIC5000_0941+` without ingesting automatic Julius timing.
6. Continue Bangla, DanPASS TextGrid, and CLIPS_MT_MANUAL backlog.

All repository writes in this pass are confined to `manual_phonetic_production/`.
