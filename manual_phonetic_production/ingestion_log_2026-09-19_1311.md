# Ingestion pass — 2026-09-19 13:11 PT

## Discovery

Fresh repository-first/web discovery was run for manually annotated phonetic-production deposits, including searches targeting manual segmentation, VOT, formants, TextGrids, OSF/Zenodo and recent deposits.

### New review candidate

- `zenodo_8077622_rescuespeech` — RescueSpeech v2 (Zenodo, published 2023-06-12). Primary German speech recorded in simulated search-and-rescue exercises. The authoritative Zenodo record states that the recordings are manually annotated. This is not sufficient by itself to prove that the human annotation layer is phonetic/temporal rather than orthographic/transcriptional, so the source is deliberately held in REVIEW rather than admitted to core. Next action: enumerate payload files and inspect annotation formats/tier semantics and associated documentation; isolate only demonstrably human phonetic/temporal annotations.

### Screened but not admitted

- MRPL2: still a strong qualifying manual mispronunciation corpus (4,500 utterances, 23 speakers, 14 L1s) but Zenodo payload remains restricted; no observation rows manufactured.
- PxCorpus: primary French speech and human-verified transcription/semantic labels, but no demonstrated manual phonetic interval/measurement layer; not core.
- TunSwitch: primary Tunisian Arabic production with manual code-switch annotation, but annotation is linguistic/lexical rather than demonstrated phonetic timing/measurement; not core.
- Nonspeech7k: excluded (non-speech and third-party sources).
- Bird-song and unrelated manual annotation search hits: excluded by scope.
- EWA-DB: known restricted clinical speech candidate; manual annotation exists for 965 speakers but layer semantics/access remain unresolved; no duplicate registry row added in this pass.

## Backlog / repository audit

- Confirmed the database still has separate `source_faithful`, `harmonized`, `registry`, `ingestion_status`, `coarticulation`, and `warehouse` data areas.
- Searched the repository for an existing RescueSpeech record before writing; no match was found.
- No automatic/forced-aligned observations were admitted.
- No inferred IPA, invented timing, or guessed token context was written.
- No unrelated repository subtree was modified.

## Files written

- `data/registry/review_increment_2026-09-19_1311.csv`
- `ingestion_log_2026-09-19_1311.md`

## Next priorities

1. Resume source-faithful observation ingestion from accepted backlogs where authoritative continuation cursors can be re-established without truncation (especially JSUT 1570+).
2. Retrieve/enumerate UltraSuite archives and ingest only `slt_labels` and manually revised `reference_labels`; quarantine automatic tiers.
3. Isolate the L2-ARCTIC human-reviewed 3,599-utterance subset from corpus-wide forced alignments.
4. Resolve RescueSpeech annotation file semantics.
5. Continue PAVOQUE, IFCASL, Spontal-N, SPIRE VCV, Chenghai stops, and clinical/manual-boundary candidates at file/tier level.

## Integrity outcome

Pass is conservative: one genuinely new candidate was added to REVIEW; zero uncertain observations were promoted to core. This is intentional under the manual-verification inclusion rule.
