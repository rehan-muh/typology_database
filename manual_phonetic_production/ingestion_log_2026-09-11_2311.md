# Manual phonetic production ingestion log — 2026-09-11 23:11 PT

## Backlog ingestion

Canonical repository state was re-read before writing. The durable PAVOQUE mirror at run start was 14 chunks / 52 utterances / 1,543 manually corrected intervals through `spike0054`. The authoritative upstream file remains `marytts/pavoque-data/pavoque-angry.yaml`, blob `bff79d978f740f6c2b09bc595ade03b6bdf33423`.

This pass followed the actual source order and materialized the first `m`-series record:

- `m0001` — `Faye Dunaway war zweimal verheiratet.`
- source utterance span: 192.72–196.44 s (3.72 s)
- 28 manually corrected German-SAMPA/silence intervals
- final source annotation endpoint: 3.715 s, preserving the native 0.005 s residual to utterance end rather than stretching the final silence interval

Committed source-faithful files:

- `data/source_faithful/pavoque_angry_utterances_chunk015.csv`
- `data/source_faithful/pavoque_angry_segments_chunk015.csv`
- `data/source_faithful/pavoque_angry_file_manifest_chunk015.csv`

The source-faithful rows retain original text, style, source times, German-SAMPA/silence labels, and cumulative source endpoints. Interval starts and durations are deterministic endpoint differences in separate fields. No IPA or unavailable segmental context was invented.

Canonical PAVOQUE status now records 15 chunks / 53 utterances / 1,571 intervals. Direct upstream inspection verifies `m0002` as the next source record.

### Deduplication accounting

The validated full PAVOQUE parse already represents these scientific observations. The new durable rows therefore increase persistent source-faithful coverage but do not increase global `parsed_rows_total`, which remains 1,361,571. Inventory-file records increase from 1,023 to 1,025 for the two newly materialized observation files; provenance is also represented by the chunk manifest.

## Fresh discovery and screening

A fresh public-repository/literature sweep targeted manually corrected phonetic production resources, formant datasets, learner speech, and manually segmented speech. The pass reverified several relevant resources rather than creating duplicate or weak registry entries:

- **MRPL2 Non-native Speech Corpus**: 4,500 utterances from 23 non-native speakers and manually annotated phonetic mispronunciations, but Zenodo files remain restricted. Existing registry coverage is retained; no duplicate source was added.
- **L2-ARCTIC**: documentation again confirms the manually annotated ~150-utterance-per-speaker subset with corrected word/phone boundaries and substitution/deletion/addition tags; forced-aligned-only material remains outside the manual core.
- **IFCASL**: approximately 100 speakers with word/phone segmentation and >50% manually corrected data remains relevant, but this run did not establish a newly accessible public observation-level deposit isolating the corrected layer.
- **RescueSpeech**: publicly accessible primary German speech is described as manually annotated, but available description still does not establish a sufficiently specific manual phonetic interval/landmark layer for core admission.
- Search hits based on third-party media, singing, nonspeech events, or non-production annotation were not admitted.

No new source was added merely from a paper/landing-page claim. Discovery therefore improved screening confidence without inflating the registry.

## Validation

- `pavoque::m0001` is namespaced and unique in the new chunk.
- All 28 segment rows resolve to `pavoque::m0001`.
- Segment indices are monotonic 1–28.
- Interval starts equal the preceding cumulative endpoint; all derived durations are nonnegative.
- Final annotation endpoint remains 3.715 s versus source utterance duration 3.72 s.
- Upstream blob SHA is unchanged.
- No raw audio was committed.
- All writes are confined to `manual_phonetic_production/`.

## Reconciled state

- 152 tracked sources
- 46 accepted
- 25 excluded
- 81 review
- 44 materially ingested sources
- 1,361,571 distinct promoted/parsed observations across 86 logical tables
- 1,025 inventory-file records
- 1,148 variable-dictionary records
- PAVOQUE durable mirror: 53 utterances / 1,571 intervals; next `m0002`

## Next backlog priorities

1. Continue PAVOQUE from `m0002` in verified source order.
2. Enumerate and parse the Hu 2025 Tibetan trilingual Mendeley TXT files at observation level, including all trajectory timepoints and participant metadata.
3. Retry direct acquisition/parsing of the compact IFA annotation archives and database exports.
4. Enumerate the Bengali-English OSF deposit and ingest all five F1/F2 samples per vowel token plus source predictors.
5. Isolate L2-ARCTIC's manually corrected TextGrid subset from the forced-aligned remainder.
6. Continue accepted Chenghai Southern Min and manually labelled rt-MRI observation-level backlog.
