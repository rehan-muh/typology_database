# Ingestion pass — 2026-09-14 04:07 PT

## Backlog ingestion

### IFA Spoken Language Corpus
- Promoted authoritative Zenodo metadata into a source-faithful layer rather than leaving IFA manifest-only.
- Added `data/source_faithful/ifa_2001_zenodo_speaker_summary.csv`: 8 manually segmented speakers (N, G, L, E, R, K, H, O) with source sex, age, recorded-sentence net duration and segmented-sentence net duration.
- Added `data/source_faithful/ifa_2001_zenodo_corpus_counts.csv`: 6,128 recorded sentences, 73,067 recorded words, 4,492 segmented sentences, 51,782 segmented words, 74,702 syllables, 187,544 phonemes, 26,733 s recorded net duration and 19,867 s segmented net duration.
- Added harmonized warehouse projections with collision-safe `source_id::source_speaker_id` identifiers and preserved original sex labels.
- Added 14 variable-dictionary mappings for participant, duration, corpus-count, annotation-count and equipment fields.
- Added 27 previously uninventoried IFA archives to the file manifest: one COCOSDA compressed-audio derivative, ten chunk-audio archives and sixteen sentence-audio archives. Published Zenodo MD5s and sizes are preserved. Bulk audio remains authoritative-reference/checksum only.
- Recording metadata retained from the authoritative record: 16-bit, 44.1-kHz audio; Shure SM10A head-mounted and Sennheiser MKH 105 fixed microphones.
- Annotation provenance remains strong: the deposit identifies the corpus as hand-segmented, reports about 1,000 labeling hours, and reports inter/intra-labeler boundary differences.

### Retrieval attempt
- Attempted direct retrieval of `Labels-chunks.zip`, `Labels-validation.zip`, and `LabelProtocol.zip` from the authoritative Zenodo record.
- Browser-backed retrieval returned rate-limit / binary-decoding errors; direct container retrieval also failed because the runtime could not resolve Zenodo.
- Therefore no TextGrid intervals, labels, TSV rows, or checksums were invented. The three annotation archives plus `Labels-sentences.zip`, `DatabaseFiles.zip`, and `SLcorpus.zip` remain the highest-priority byte-level backlog.

## Fresh discovery

### JSUT-label / BASIC5000 manual annotation derivative — HIGH review
- Added `jsut_label_manual_prosody` to review.
- Public repository: https://github.com/sarulab-speech/jsut-label
- Repository documentation states that phonetic and prosodic information is based on manual annotation, while time information is automatically estimated with Julius.
- This is secondary reuse of JSUT recordings, but potentially qualifies because it contributes genuinely new manual annotation. Core ingestion must isolate manual label content from automatic timing and lineage-link to the underlying JSUT recordings instead of counting recordings twice.
- Recorded authoritative Git tree SHA `1978271ca6212e1ea742da8f149160f5679e8971`; repository contains BASIC5000 `.lab` files plus text/kana and end-to-end-symbol resources.

## Validation
- Source-faithful IFA speaker IDs are unique (8/8).
- Warehouse speaker IDs are namespaced as `ifa_spoken_language_corpus_2001::<ID>`.
- Corpus summary counts are copied from the authoritative deposit and are not inferred from archive sizes.
- Source-faithful rows added this pass: 9 distinct scientific metadata observations; warehouse tables are projections and are not double-counted as new observations.
- Registry totals reconciled to: 198 sources = 47 accepted + 31 excluded + 120 review.
- Updated totals: 46 materially ingested sources; 1,361,981 distinct promoted rows; 115 logical tables; 1,173 file-inventory records; 1,211 variable-dictionary records.
- All repository writes were restricted to `manual_phonetic_production/`.

## Next backlog priorities
1. Retry IFA `Labels-sentences.zip`, `Labels-chunks.zip`, `Labels-validation.zip`, and `LabelProtocol.zip`; verify MD5 before parsing; enumerate every TextGrid and tier; ingest every manual interval/label source-faithfully.
2. Parse IFA `DatabaseFiles.zip` TSV objects and `SLcorpus.zip`; inventory every table/file and preserve every original variable/value before harmonization.
3. Enumerate JSUT-label BASIC5000 completely, determine exact manual-vs-automatic fields, and build lineage links to JSUT recordings without duplicate recording admission.
4. Continue IFCASL and BeDiaCo file-level provenance resolution and public-file enumeration.
