# Ingestion pass — 2026-09-21 14:09 PT

## Backlog ingestion
Continued accepted source `PAVOQUE_ROETTIG2009` from the explicit `spike0004` cursor in `pavoque-angry.yaml` (immutable upstream Git blob `bff79d978f740f6c2b09bc595ade03b6bdf33423`). Parsed the complete utterance `spike0004` and all 30 manually corrected phonetic segment observations. Preserved prompt, German orthography, style, utterance start/end, original source labels, relative segment endpoints, and source order. Derived segment starts only from the immediately preceding source endpoint and absolute times only by adding the source utterance start; these derived fields do not overwrite originals. No SAMPA-like label was silently converted to IPA. Next contiguous cursor: `spike0005`.

Files added:
- `data/source_faithful/pavoque_angry_spike0004_utterance_20260921_1409.csv` — 1 observation.
- `data/source_faithful/pavoque_angry_spike0004_segments_20260921_1409.csv` — 30 observations.

Integrity checks: 30 segment indices contiguous; relative endpoints monotonic; first derived start 0; final source endpoint 3.775 s is within utterance duration 3.78 s (5 ms residual retained, not altered); namespaced source ID retained. Parsed-row increment = 31; logical-table increment = 2.

## Discovery
Added `PXCORPUS_KOCABIYIKOGLU2023` (Zenodo DOI 10.5281/zenodo.10080490) to REVIEW. Public record describes primary French non-singing spoken drug-prescription dialogues from 55 participants, manually transcribed and semantically annotated, with automatic transcriptions verified by humans. The record reports 2,067 recordings in prose but 1,981 in its summary table (262.27 min); discrepancy is retained verbatim. Core admission is withheld until downloadable files establish an isolatable manually created/corrected phonetic timing, landmark, interval, or measurement layer rather than only transcript/semantic annotation.

## Exclusion discipline
Search results that were text-only annotation, OCR, music/singing, or inherited secondary speech without newly demonstrated isolatable phonetic annotation were not admitted. Existing MRPL2, RescueSpeech, VoxAngeles, and Code-Switching Corpus records were not duplicated.

## Next backlog priorities
1. Continue PAVOQUE angry YAML from `spike0005`, then process remaining styles while reconciling outtakes against canonical recordings.
2. Parse accessible VoxAngeles measurement TSVs and audited alignment archives when connector payload limits permit; never mix unaudited MFA output into core.
3. Complete Marathi VOT TextGrid/workbook extraction and reconcile speaker-named artifacts.
4. Inspect PxCorpus downloadable archive for manual time-aligned/phonetic layers and exact file manifests before any promotion.
5. Continue repository-first searches for manually corrected TextGrid/EAF/EMA/ultrasound/EPG/aerodynamic/voice datasets.

## Canonical state after pass
236 sources = 59 accepted + 32 excluded + 145 review; 50 fully ingested; 1,364,401 parsed rows across 212 logical tables. All writes confined to `manual_phonetic_production/`.
