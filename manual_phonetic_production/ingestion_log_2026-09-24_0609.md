# Ingestion pass — 2026-09-24 06:09 PT

## Backlog ingestion

Continued PAVOQUE (`marytts/pavoque-data`) beyond the previously ingested happy examples. Parsed the complete first neutral utterance `a0001` from the authoritative `pavoque-neutral.yaml`: 24 manually corrected segment intervals, preserving the original SAMPA-like labels and source utterance timing. Added deterministic segment starts from successive source endpoints and recording-absolute boundaries as separate fields; original endpoint values remain unchanged. No IPA normalization was inferred.

Source-faithful output: `data/source_faithful/pavoque_neutral_backlog_20260924_0609.csv` (24 segment rows).

The upstream corpus documentation states that PAVOQUE is a single-speaker German read-speech corpus with five speaking styles; phone-level segmentation is available for all recorded utterances and labels were manually checked/corrected where needed. The repository YAML explicitly marks segments as manually corrected.

## Discovery / screening

Fresh repository/literature searches re-screened MRPL2, RescueSpeech, L2-ARCTIC, CORPRES, ROG, CARInA, and the German-English Spoken-Wikipedia derivative. No source was promoted without a separable qualifying manual layer. MRPL2 remains restricted; CORPRES remains high-priority because 40% is manually segmented/fully annotated on six phonetic/prosodic levels but an authoritative downloadable machine-readable distribution still needs resolution; ROG remains lineage-sensitive because it builds on SST/GOS2; CARInA remains excluded from core where alignment is semi/automatic; the Spoken-Wikipedia code-switching derivative requires lineage-level isolation.

## QC / provenance

PAVOQUE `a0001`: source utterance [0.0, 4.04] s; last annotated segment endpoint 4.035 s. Segment endpoints are monotonic. 24/24 rows retain original labels. No source values overwritten. Namespaced utterance ID: `pavoque::a0001`.

## Next backlog priorities

1. Continue complete PAVOQUE YAML parsing across neutral/happy/angry/sad/poker, excluding or separately reviewing outtakes.
2. Materialize and parse L2-ARCTIC human-reviewed TextGrids if access permits.
3. Resolve CORPRES distribution and isolate the manually segmented 40%.
4. Continue BAS manual partitions (VM2/PD1/PD2/SC10) and restricted CLIPS_MT_MANUAL metadata/object manifests.
5. Resolve Spontal-N manually corrected ~7% phone subset without admitting the automatic remainder.
