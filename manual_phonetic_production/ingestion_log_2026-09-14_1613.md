# Ingestion log — 2026-09-14 16:13 PT

## Backlog ingestion

- Advanced `jsut_label_basic5000_manual` from source rows 1–340 to 1–380.
- Re-read `BASIC5000_0341`–`BASIC5000_0380` directly from authoritative `sarulab-speech/jsut-label` file `e2e_symbol/phoneme.yaml`, git blob `ae8933677c082c22238a895012b6b6dcba1f6625`.
- Added source-faithful file `data/source_faithful/jsut_label_basic5000_manual_phoneme_rows_0341_0380.yaml` containing 40 verbatim manual phonetic/prosodic symbol sequences.
- Added harmonized index `data/harmonized/jsut_label_basic5000_manual_annotation_index_0341_0380.csv` with 40 collision-safe IDs of the form `jsut_label_basic5000_manual::BASIC5000_####` and recording lineage `JSUT::BASIC5000`.
- Automatic Julius timing remains excluded because the upstream project explicitly distinguishes manually annotated phonetic/prosodic information from automatically estimated time information.
- Updated checkpoint: next row `381`, next utterance `BASIC5000_0381`.

## Validation

- Source slice count: 40 rows.
- Harmonized slice count: 40 rows.
- Source identifiers are contiguous from `BASIC5000_0341` through `BASIC5000_0380`.
- Namespaced harmonized IDs are unique within the slice.
- No automatic timing rows were admitted.
- Underlying JSUT recordings are not counted as new recordings; only the new manual annotation layer is represented.

## Discovery

### MRPL2 Non-native Speech Corpus — HIGH review

Authoritative record: https://zenodo.org/records/20365865

The Zenodo record reports 4,500 utterances from 23 non-native English speakers representing 14 native languages. Each utterance is described as manually annotated for phonetic mispronunciations relative to Modern Received Pronunciation. The deposit also advertises annotated TextGrids, word transcripts, and two corpus-wide master files containing expected and ground-truth phoneme sequences.

The metadata record is public, but the files are restricted. The corpus was therefore **not** promoted to accepted core. Required next steps are authorized file access, exhaustive file enumeration, verification of which TextGrid tiers/labels are fully manual versus generated or corrected, extraction of annotator/reliability metadata, license/redistribution review, and source-faithful ingestion of all qualifying observations.

A manifest entry was added for the authoritative metadata record only. No hidden file names, checksums, counts, or variables were invented.

## Totals after reconciliation

- Sources: 211
- Accepted: 51
- Excluded: 31
- Review: 129
- Materially ingested sources: 49
- Parsed/source-faithful observation rows: 1,362,432
- Logical parsed tables: 143
- Inventory records: 1,202
- Variable-dictionary records: 1,246

## Next backlog priorities

1. Continue JSUT from `BASIC5000_0381` while preserving the manual-only provenance boundary.
2. Continue exhaustive PAVOQUE YAML parsing across all remaining expressive-style files.
3. Retry DanPASS archive enumeration/parsing and ingest the public Praat tiers if retrieval succeeds.
4. Resolve restricted/public access and tier-level provenance for MRPL2, IFCASL, HAMATAC, and other HIGH-review sources.
5. Prefer sources with token-level manual intervals/measurements and openly downloadable CSV/TextGrid/EAF/MAT files over paper-only registry additions.

All repository modifications in this run were confined to `manual_phonetic_production/`.
