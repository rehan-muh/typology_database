# Ingestion pass — 2026-09-23 17:12 PT

## Discovery

Accepted `github_marytts_pavoque` (PAVOQUE Corpus of Expressive Speech). Primary German spoken/read expressive production from one male native speaker. Inclusion evidence is unusually explicit: the public repository describes phone segments as manually corrected, and the corpus paper states phone-level segmentation is available for all recorded utterances and labels were manually checked/corrected where needed. The speaker's professional opera background does not make these recordings singing; corpus conditions are spoken neutral, happy, sad, angry, and poker styles.

## Files inventoried

Manifested all six public YAML annotation objects with exact Git blob SHA and byte size: angry, happy, neutral, outtakes, poker, sad. Also manifested README and license. Audio is original FLAC (44.1 kHz, 16-bit, mono) distributed through project releases; raw audio is not committed here. License: CC BY-NC-SA 4.0.

## Variables mapped

Mapped all visible YAML fields: `prompt`, `text`, `style`, utterance `start`, utterance `end`, segment `lab`, segment `end`; added deterministic derived segment-start and duration concepts without overwriting raw landmarks. Original SAMPA-like labels remain verbatim and normalized IPA remains null pending an explicit defensible mapping.

## Observation ingestion

Parsed the connector-accessible beginning of `pavoque-angry.yaml` into a source-faithful table: 41 manually corrected phone-boundary rows across `spike0001` and `spike0002`. These rows retain source prompt, text, style, absolute utterance interval, segment index, original label, and relative end landmark. No midpoint reduction was performed.

## Backlog ingestion / continuation

Attempted complete PAVOQUE YAML materialization. GitHub connector can expose the source but truncates multi-MB responses; the execution container cannot resolve github.com, so a full clone/download was not possible in this run. Therefore only directly verified rows were counted. Next priority: materialize/stream all six YAML objects, parse every utterance and segment, derive absolute boundaries, validate monotonicity and utterance containment, and then link release audio manifests without committing raw audio.

Existing backlog remains active: TunSwitch archive-member parsing, Golos transcript-manifest extraction, PxCorpus archive parsing, IFA TSV/TextGrid extraction, CCOST TextGrid/metadata materialization, and the eligible manual tiers of L2-ARCTIC/TaL.

## QC / lineage

PAVOQUE downstream MaryTTS voices, synthetic outputs, and CAMEO repackaging are derivative and must not become independent production observations. `pavoque-outtakes.yaml` remains separately conditioned. Namespaced IDs must use `github_marytts_pavoque::...`. No unrelated repository paths were modified.

## Counts after pass

266 sources total: 74 accepted, 36 excluded, 156 review. 51 sources have observation-level ingestion. 1,364,990 parsed rows across 241 logical tables; 1,319 manifest records; 1,387 variable-dictionary records.
