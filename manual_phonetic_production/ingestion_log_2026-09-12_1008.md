# Manually Annotated Phonetic Production Database — ingestion pass 2026-09-12 10:08 PT

## Backlog ingestion

Advanced the durable source-faithful PAVOQUE angry subset from 90 utterances / 2,685 intervals to 96 utterances / 2,843 intervals.

### Chunk 027

Literal upstream record order preserved from `marytts/pavoque-data:pavoque-angry.yaml` blob `bff79d978f740f6c2b09bc595ade03b6bdf33423`:

`m0041 -> m0043 -> m0044 -> m0045 -> m0036 -> m0046`

This pass corrected an earlier provenance statement that `m0036` was absent upstream. It is present, but occurs later than its numeric ID suggests, after `m0045`. No record was reordered to make IDs numerically monotonic.

Files written:

- `data/source_faithful/pavoque_angry_utterances_chunk027.csv` — 6 utterances.
- `data/source_faithful/pavoque_angry_segments_chunk027_part1.csv` — 82 manually corrected intervals (`m0041`, `m0043`, `m0044`).
- `data/source_faithful/pavoque_angry_segments_chunk027_part2.csv` — 76 manually corrected intervals (`m0045`, `m0036`, `m0046`).
- `data/source_faithful/pavoque_angry_file_manifest_chunk027.csv` — source/local lineage and blob SHAs.

Validation:

- 158 segment rows total.
- Namespaced utterance/segment IDs retained.
- Original German-SAMPA/silence labels retained without IPA invention.
- Original cumulative endpoints retained.
- Interval starts and durations are deterministic derivatives only.
- All six utterances have monotonically increasing cumulative endpoints.
- All six retain the source-native 0.005 s final residual rather than extending the last interval.
- `m0047` is the next verified upstream record.

The global distinct scientific-observation count was not incremented because these PAVOQUE observations are already represented in the validated full-corpus lineage; this materialization is a durable source-faithful mirror and would otherwise double-count observations.

## Discovery / screening

Added `mrpl2_nonnative_2026` as a HIGH-priority review increment. The public Zenodo record describes the MRPL2 Non-native Speech Corpus as 4,500 primary English utterances from 23 non-native speakers representing 14 L1s, with every utterance manually annotated for phonetic mispronunciations. The deposit advertises annotated TextGrids plus expected and ground-truth phoneme sequences. Files are currently restricted, so no byte-level files, observations, or derived rows were admitted to core. Future ingestion must isolate human annotations and avoid conflating any automatic ASR/MDD processing products with the manual layer.

Also re-screened L2-ARCTIC: its documentation confirms 3,599 manually examined utterances with 14,098 substitutions, 3,420 deletions, and 1,092 additions, but this source was already in the project backlog and was not duplicated.

## Database state after pass

- tracked sources: 157
- accepted: 47
- excluded: 25
- review: 85
- materially ingested sources: 44
- distinct promoted/parsed observations: 1,361,571
- parsed logical tables: 86
- inventory-file records: 1,067
- variable-dictionary records: 1,156

## Next priorities

1. Continue PAVOQUE from `m0047`, preserving literal source order rather than numeric sort order.
2. Complete byte-level ingestion of existing high-value public backlogs where the manual layer is already established: IFA corrected annotations, L2-ARCTIC manual subset, Tibetan trilingual F1/F2 trajectories, Nagle et al. L2 Spanish OSF, Cox & Dideriksen Danish IDS/ADS vowel data, and Consonant Challenge `handsegm.91.mlf.txt`.
3. Keep MRPL2 in review until file access becomes genuinely available; do not count paper/repository metadata as observation-level ingestion.
