# Ingestion pass — 2026-09-23 20:09 PT

## Discovery
Accepted `rescuespeech_zenodo_8077622_v2` (RescueSpeech, Zenodo v2, DOI 10.5281/zenodo.8077622). The authoritative Zenodo record describes authentic German speech recorded in simulated search-and-rescue exercises and explicitly states that the recordings are manually annotated. ASR clean speech: 2,412 recordings, 1:36:10, 26 speakers; 38 recordings have undetermined speaker identity.

## File inventory / lineage
Inventoried every top-level public Zenodo v2 object: `Readme.md` (MD5 `5b6bf9922c78e37aef96199f0eb34b4d`), `Task_ASR.tar.gz` (MD5 `c5b9f7857c1970bfe34eae0805c6987b`), and `Task_enhancement.tar.gz` (MD5 `0a933817af7ccd56982225ae46c3722c`). The authentic clean recordings and human annotations are eligible. Noisy copies created with five AudioSet-derived noise types and SNR augmentation are synthetic derivatives and are retained only for lineage; they are not independent production observations. Third-party noise is not eligible production data.

## Backlog ingestion performed
Parsed the complete speaker-by-split summary exposed in the authoritative README into `data/source_faithful/rescuespeech_zenodo_8077622_v2_readme_speaker_split_counts.csv`: 26 rows (`?` plus `spk01`–`spk25`), retaining source IDs and adding collision-safe `source_id::source_speaker_id`. Added seven dictionary records covering speaker ID, train/test/dev/total recording counts, duration, and the human transcription concept. Exact archive column naming for transcription remains pending extraction and is explicitly marked rather than invented.

## QC
README reports train=1,591, test=576, dev=245; these sum to 2,412 and match the stated corpus recording count. Its displayed aggregate `Totals` row nevertheless says 2,167. This inconsistency is preserved and flagged rather than silently corrected. The per-speaker rows are source-faithful. No synthetic noisy recordings were added to production counts. No IPA, phoneme contexts, demographic attributes, or annotation intervals were inferred.

## Remaining backlog
1. Extract `Task_ASR.tar.gz` without committing bulk audio; enumerate all archive members and parse train/test/dev TSVs.
2. Verify exact transcription, path, duration, speaker and any condition columns and extend the variable dictionary with original names/datatypes/codings.
3. Create recording/utterance rows for all 2,412 clean recordings and retain source-faithful human transcription values.
4. Hash/manifest annotation and metadata members inside the archive where feasible.
5. Keep `Task_enhancement.tar.gz` at lineage/manifest level unless metadata are needed to trace derived copies to clean originals.

## Counts after pass
269 sources: 76 accepted, 36 excluded, 157 review. Observation-ingested sources: 52. Parsed rows: 1,365,016 across 242 logical tables. Manifest records: 1,328. Variable-dictionary records: 1,403.
