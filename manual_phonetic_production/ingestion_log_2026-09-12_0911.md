# Manually Annotated Phonetic Production Database — ingestion pass 2026-09-12 09:11 PT

## Backlog ingestion completed

### PAVOQUE angry subset

Advanced the durable source-faithful mirror from **84 utterances / 2,512 intervals** through `m0033` to **90 utterances / 2,685 intervals** through `m0040`.

Chunk 026 contains six complete utterances in the literal upstream order:

- `m0034` — *Der Senator billigt die Deutsch Pflicht auf Schulhöfen.* — 39 intervals
- `m0035` — *Die Hauptrolle übernahm Jeffrey Wright.* — 28 intervals
- `m0037` — *Warren starb in Washington.* — 23 intervals
- `m0038` — *Das Pendant dazu ist eine Scheibe;* — 25 intervals
- `m0039` — *Einige Schüler gründen diesen Club neu.* — 30 intervals
- `m0040` — *Alain Robert: Mit nackten Händen.* — 28 intervals

Total: **6 utterances / 173 manually corrected German-SAMPA or silence intervals**.

The upstream source jumps directly from `m0035` to `m0037`; there is no `m0036` at this position. Source order was preserved and no missing record was invented. `m0041` (*Kiew wird von drei Flughäfen bedient.*) is the next verified source record.

### Source-faithful preservation and QC

- authoritative upstream repository: `marytts/pavoque-data`
- authoritative file: `pavoque-angry.yaml`
- pinned upstream blob: `bff79d978f740f6c2b09bc595ade03b6bdf33423`
- original German-SAMPA/silence labels preserved exactly
- original cumulative interval endpoints preserved exactly
- interval starts and durations stored only as deterministic derivatives
- no IPA normalization invented
- namespaced utterance and interval IDs retained
- endpoint order checked for monotonicity
- every chunk-026 utterance has the source-native **0.005 s terminal residual**; no final interval was extended to conceal it
- observation lineage already exists in the validated full-PAVOQUE parse, so these durable mirror rows do **not** increase the global distinct-scientific-observation count

The interval material is split into two same-schema files only because of write-size constraints: `pavoque_angry_segments_chunk026.csv` contains `m0034` (39 rows), while `pavoque_angry_segments_chunk026_part2.csv` contains the remaining five utterances (134 rows). The chunk manifest records both local blob SHAs and treats them as one lineage unit.

### Files added/updated

Added:
- `data/source_faithful/pavoque_angry_utterances_chunk026.csv`
- `data/source_faithful/pavoque_angry_segments_chunk026.csv`
- `data/source_faithful/pavoque_angry_segments_chunk026_part2.csv`
- `data/source_faithful/pavoque_angry_file_manifest_chunk026.csv`

Updated:
- `data/source_faithful/pavoque_angry_ingestion_status.json`
- `STATUS.json`

No unrelated repository paths were modified.

## Fresh discovery / screening

Discovery was run in parallel with backlog ingestion, emphasizing publicly deposited human-annotated production material.

### Re-screened / deduplicated

**IFCASL** resurfaced. It remains a high-value backlog because more than half of its word/phone segmentation was manually corrected, but this pass did not establish a newly transferable observation-level manual-only file beyond the already tracked lineage. No duplicate registry entry was added.

### Rejected from core on policy grounds

**COVYT** surfaced as a manually segmented/diarized YouTube speech resource. It is based on third-party online media rather than a primary research production collection, so it is outside the database's inclusion policy and was not admitted.

### Held without registry inflation

**Hamburg MapTask Corpus (HAMATAC)** surfaced with primary L2 German speech and substantial human correction/annotation, but the surfaced manual layer concerned POS/lemma/disfluency annotations rather than a clearly isolatable manual phonetic interval, landmark, or production-measure layer. It was not promoted on that evidence alone.

**Karl Eberhards Corpus (KEC)** surfaced with spontaneous southern German dialogues, EMA for a subset, manually corrected word boundaries, and automatically aligned segment boundaries. Because the segmental layer surfaced as automatic and no clean public human-verified articulatory/phonetic observation table was established in this pass, it was not admitted.

An **OSF early linguistic-production coding project** surfaced with manual segmentation/phonetic transcription methodology, but a public primary observation deposit satisfying the database criteria was not yet established, so it was not promoted.

These conservative non-admissions avoid paper-level registry inflation and contamination of the manual-only core.

## Reconciled state

- tracked sources: **156**
- accepted: **47**
- excluded: **25**
- review: **84**
- materially ingested: **44**
- distinct promoted/parsed observations: **1,361,571**
- logical tables: **86**
- inventory-file records: **1,064**
- variable-dictionary records: **1,156**

No new variables were introduced by chunk 026; it uses the established PAVOQUE source-faithful schema, so the variable dictionary did not change.

## Next backlog priorities

1. Continue PAVOQUE from verified `m0041` onward in larger transactional blocks.
2. Parse OMPAL's detailed per-rater JSON into source-faithful speaker/utterance/word/segmental-judgment and sentence-rating tables.
3. Retrieve and parse the Consonant Challenge Corpus `handsegm.91.mlf.txt` manual-only layer.
4. Materialize IFA human-corrected annotation archives when a byte-transfer route succeeds.
5. Enumerate the Nagle–Huensch–Zárate-Sández L2 Spanish OSF tree and extract observation-level manual production variables.
6. Isolate the UltraSuite UXTD/UXSSD manually revised reference-label subset while excluding force-aligned phone labels.
7. Continue trajectory-rich targets (Tibetan trilingual and Bengali–English F1/F2 data), preserving every original sampled timepoint.
