# Ingestion pass — 2026-09-23 18:11 PT

## Discovery

### IFCASL — added to review queue
- Primary French/German learner + native read-speech corpus, approximately 100 speakers.
- Publication states word- and phone-level annotation/segmentation and that more than 50% of the data were manually corrected; inter-annotator agreement is reported.
- Strong substantive fit for the database, especially L1/L2 phonetics, vowel quality/quantity, /h/, glottal stop, final devoicing, pitch range, and tempo.
- **Decision: REVIEW, not accepted.** This pass did not establish a directly downloadable authoritative machine-readable deposit in which the manually corrected tiers can be isolated from automatically aligned material. Core admission waits for that provenance boundary.
- Reference: https://aclanthology.org/L16-1212/

## Backlog ingestion / verification

### PAVOQUE
- Reopened the upstream `pavoque-angry.yaml` directly through the GitHub repository rather than relying on the earlier truncated summary.
- Verified the source-faithful schema again: `prompt`, `text`, `style`, utterance `start`, utterance `end`, and repeated segment objects containing `lab` and relative segment `end`.
- Verified complete manually corrected segment sequences for `spike0001` through `spike0008` in the accessible upstream excerpt. This extends direct inspection beyond the two utterances represented in the prior observation-level ingest.
- Labels remain source SAMPA-like labels verbatim; no IPA mapping or context was invented.
- Did **not** increment parsed-row totals: although the connector can return a large line-range excerpt, its response is still truncated and this run did not create a complete deterministic row conversion for the newly inspected utterances. This avoids counting observations not actually materialized in structured warehouse tables.
- Next backlog action: fetch the YAML in bounded line ranges, parse each complete utterance exactly once, and append namespaced segment IDs with absolute segment times derived separately from source-relative endpoints.

## Additional screening
- MRPL2 remains high-priority but file-restricted: 4,500 manually annotated utterances from 23 non-native speakers / 14 L1s; no observation ingest without file access.
- HAMATAC remains potentially useful for L2 German production and manual disfluency annotation, but repository access is restricted and its current manually corrected POS/lemma layer is not itself a phonetic annotation; do not promote on that evidence alone.
- ROG remains review-only because it is built on pre-existing SST/GOS 2 recordings; new manual prosodic/interactional layers must be isolated and lineage-deduplicated before admission.
- UrduSpeech was screened as review-worthy in principle only for its 9-hour manually corrected benchmark; the broader 156-hour LLM-curated corpus is not eligible merely from aggregate human quality assessment. No registry row added until the benchmark deposit/provenance can be verified.

## QC
- No unrelated repository paths modified.
- No automatic/forced-aligned observations promoted to core.
- No third-party media, perception-only, synthetic, or singing material admitted.
- No duplicate recordings counted.
- Parsed observation totals intentionally unchanged this run.
