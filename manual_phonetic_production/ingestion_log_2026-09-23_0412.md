# Ingestion log — 2026-09-23 04:12 PT

## Discovery
Repository-first searches covered Zenodo/OSF-facing queries for manually annotated/corrected phonetic segmentation, TextGrid, acoustic production, articulatory/EMA and related speech corpora.

### Newly accepted: CCOST
- source_id: `ccost_zenodo_17366698`
- Chulalongkorn Corpus of Spoken Thai, DOI 10.5281/zenodo.17366698.
- Primary Standard Thai production: approximately 7 h spontaneous + 5 h controlled speech, 49 speakers.
- Admission evidence: deposit describes corpus as phonetically annotated and includes `CCOST_manual.pdf`, specifically an annotation manual for force-alignment correction and transcription. Main corpus is WAV + TextGrid.
- Tasks: interview (`INT`), 206-item word list (`WL`), 25-sentence reading (`ST`).
- License: CC-BY-NC-SA 4.0.
- Main 6.2 GB archive was not copied into GitHub. Authoritative Zenodo reference plus deposit MD5 is preserved.
- All eight deposited objects were manifested with checksums and processing state.

## Backlog ingestion / object work
This run performed source-level ingestion for CCOST rather than stopping at a paper registry entry: the complete Zenodo object manifest, checksums, file roles, corpus/task counts, access/license constraints, and archive-processing backlog were written under `data/source_faithful/ccost/`. The large archive remains pending object-level extraction because the connected GitHub interface cannot ingest a 6.2 GB Zenodo binary directly. `CCOST_metadata.csv`, `wordlist.csv`, and `sentences.txt` are explicitly queued for parsing; `C-COST.zip` is queued for member enumeration and TextGrid conversion.

## Other screened candidates
- MRPL2: qualifies scientifically (4,500 manually annotated L2 utterances) but observation files remain restricted; no core promotion.
- RescueSpeech: already known/accepted; no duplicate source created.
- spINAch: rejected for core annotation use because phonetic annotations are Whisper + MFA automatic outputs.
- SSL-NL: secondary reuse plus forced alignments; not admitted.
- singing datasets (Jingju, vocadito, VocalSet): excluded by non-singing scope.
- TunSwitch: already reviewed; no duplicate source created.

## Validation / lineage
- Namespaced source ID used: `ccost_zenodo_17366698`.
- No IPA or context invented.
- No raw audio committed.
- Manifest records authoritative URL and MD5 for every deposit object.
- Writes confined to `manual_phonetic_production/`.

## Next priorities
1. Parse `CCOST_metadata.csv` into source-faithful + speakers/recordings/task/equipment/demographic tables.
2. Parse all columns of `wordlist.csv` and `sentences.txt`; construct lexical/stimulus tables without dropping source-specific fields.
3. Enumerate `C-COST.zip`; ingest every TextGrid tier/interval, preserving original Thai/transcription labels and boundary times, and inventory WAVs without committing audio.
4. Read `CCOST_manual.pdf` closely and encode tier meanings, correction procedure, annotator/QC provenance and transcription conventions in the variable dictionary.
5. Continue discovery emphasizing open token-level acoustic/articulatory measurement deposits rather than corpus descriptions alone.
