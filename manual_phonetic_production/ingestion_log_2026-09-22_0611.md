# Ingestion pass — 2026-09-22 06:11 PT

## Backlog ingestion
Advanced `rescuespeech_2023` from source-level acceptance to file-level inventory using the authoritative Zenodo v2 record (10.5281/zenodo.8077622). Manifested all three top-level files: `Readme.md` (MD5 `5b6bf9922c78e37aef96199f0eb34b4d`), `Task_ASR.tar.gz` (MD5 `c5b9f7857c1970bfe34eae0805c6987b`), and `Task_enhancement.tar.gz` (MD5 `0a933817af7ccd56982225ae46c3722c`). Zenodo reports 1 h 36 min and 2,412 clean ASR utterances. The record explicitly says the recordings are manually annotated.

Core-lineage decision: retain the 2,412 clean primary utterances as the eligible lineage pending archive/schema parsing. Do not admit the matched noisy ASR copies (synthetic contamination with AudioSet noise) or the larger speech-enhancement augmentation set as independent production observations. Their files/checksums remain in the source manifest for reproducibility and lineage control.

Archive bytes were not retrievable/enumerable through the available access path in this run. Therefore no observation rows, speaker counts, annotation tiers, timing units, or variable semantics were inferred. `Readme.md` is also left pending parse rather than reconstructed from paper prose.

## Discovery
Repository- and literature-focused search covered manually annotated production corpora and current deposits. A notable candidate is ROG (Dobrovoljc Zor et al., LREC 2026), a 10-hour spoken Slovenian resource with manual prosodic/interactional layers. It builds on pre-existing GOS 2 recordings, so it is admissible only under the new-manual-annotation exception. Public observation-level release and clean isolation of those new layers were not established in this pass; consequently it was not promoted to core.

Rechecked MRPL2: 4,500 utterances from 23 non-native English speakers with manually annotated phonetic mispronunciations, but Zenodo files remain restricted; no inaccessible observations were counted. Rechecked L2-ARCTIC documentation: 3,599 utterances were manually examined with 14,098 substitutions, 3,420 deletions and 1,092 additions; this reinforces the existing manual-subset provenance but does not change rows this pass.

## Validation / state
- Sources: 242 total = 61 accepted + 33 excluded + 148 review.
- Fully ingested sources: 50.
- Scientific observations: 1,364,792 across 236 logical tables (unchanged).
- File-manifest records: 1,250 (+3).
- Variable-dictionary records: 1,272 (unchanged).
- No unrelated repository paths modified.

## Next priorities
1. Enumerate `Task_ASR.tar.gz` without committing audio; identify transcript/annotation formats, speaker IDs, and human annotation granularity.
2. Parse every non-audio ASR object source-faithfully and construct RescueSpeech variable dictionary + harmonized utterance/recording/participant/provenance tables.
3. Continue large accessible manual-layer backlog rather than adding paper-only records; prioritize L2-ARCTIC manual subset acquisition and IFCASL manually corrected segmentation.
4. Verify whether ROG's newly created prosodic layers are publicly downloadable and isolatable from inherited GOS 2/SST material before registry admission.
