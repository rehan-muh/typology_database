# Ingestion pass — 2026-09-21 12:11 PT

## Discovery
Accepted `PAVOQUE_2009` (PAVOQUE Corpus of Expressive Speech). Authoritative upstream README documents a primary German expressive-speech corpus and explicitly states that phonetic segments are manually corrected. The speaker's training as a baritone does not make these recordings singing: corpus tasks are spoken neutral/acted expressive styles. License: CC BY-NC-SA 4.0.

Search also surfaced singing corpora (excluded by scope) and third-party/automatic subtitle corpora (not admitted). No uncertain automatic timing was promoted.

## File inventory
Recorded immutable Git blob identities for six public YAML annotation objects: angry, happy, neutral, outtakes, poker, sad. Sizes and SHAs are in `data/ingestion_status/pavoque_2009_20260921T1211.csv`. Audio is distributed through upstream releases and is not mirrored here.

## Observation ingestion
Parsed `pavoque-angry.yaml` through `spike0002`: 2 utterance rows and 41 manually corrected segment rows. Exact source orthography, style, utterance start/end, source segment labels, source-relative segment end times, and list order are retained. No SAMPA-to-IPA conversion was invented. Next contiguous cursor: `spike0003`.

## Variable dictionary
Added 8 source variables/faithful positional fields covering prompt ID, orthography, style, utterance timing, segment labels, relative segment end time, and source list order.

## Integrity / lineage
Namespaced utterance IDs use `PAVOQUE_2009::prompt`. Outtakes remain pending lineage audit so failed/repeated takes are not silently double-counted. Source labels remain untouched. Current partial ingestion is not counted as a fully ingested source.

## Canonical delta
Sources 233→234; accepted 58→59; parsed rows 1,364,270→1,364,313; logical tables 206→208; manifest records 1,238→1,244; variable-dictionary records 1,261→1,269. Excluded/review/fully-ingested counts unchanged.

## Next backlog priorities
1. Continue PAVOQUE angry YAML from `spike0003`, then enumerate happy/sad/poker and large neutral YAML.
2. Audit PAVOQUE outtakes against prompt/recording lineage before admission.
3. Continue Marathi VOT TextGrid/workbook extraction and AUSpeech multimodal metadata backlog.
4. Continue AphasiaBD archive-member enumeration when payload access permits.
