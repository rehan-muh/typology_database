# Manual phonetic production ingestion log — 2026-09-11 13:10 PT

## Backlog ingestion

Resumed from the corrected canonical PAVOQUE durable endpoint (`spike0017`) and inspected the authoritative `marytts/pavoque-data/pavoque-angry.yaml` directly at immutable blob `bff79d978f740f6c2b09bc595ade03b6bdf33423`.

Committed the next three complete source records:

- `spike0017` — `Ich glaube du hast mir eine Frage gestellt.` — 35 manually corrected segment/silence intervals
- `spike0018` — `Ich kann Fragen nicht beantworten – erzähl mir einfach etwas und ich werde antworten.` — 74 manually corrected segment/silence intervals
- `spike0019` — `Würdest du gerne mit jemand anderem sprechen?` — 40 manually corrected segment/silence intervals

New durable materialization: **3 utterance rows + 149 interval rows** in `pavoque_angry_utterances_chunk005.csv` and `pavoque_angry_segments_chunk005.csv`.

Original German text, expressive style, source utterance start/end, German-SAMPA/silence labels, and source-native cumulative interval endpoints are retained. `start_rel_s` and `duration_s` are deterministic derivatives from consecutive source endpoints. No IPA or missing segmental context was invented.

The source ordering was explicitly checked beyond `spike0019`: the next record in the YAML is `spike0021`. There is no `spike0020` record at that point, so the deterministic continuation is **`spike0021`** rather than an artificial contiguous ID.

### Canonical PAVOQUE state

- durable utterances: **19**
- durable manually corrected intervals: **587**
- next source record: **`spike0021`**
- immutable source blob: `bff79d978f740f6c2b09bc595ade03b6bdf33423`

The validated full PAVOQUE CI parse already represents these same observations, so global `parsed_rows_total` remains **1,361,571**. This run increases durable source-faithful persistence, not the count of distinct scientific observations.

## Discovery screening

A fresh production-data search covered manual TextGrid/segmentation resources, learner speech, laboratory phonetics, and trajectory datasets.

### West Central Bavarian longitudinal vowel production study

Wolfswinkler & Harrington (JIPA, published online 2021) is methodologically highly relevant. The paper states that vowel segment boundaries and formant tracking errors were manually corrected. It reports raw F1/F2/F3 trajectories subsequently time-normalized to eleven points, with adult and longitudinal child production data from rural West Central Bavarian. This would be an excellent trajectory source if the underlying observation-level files can be located. No publicly accessible data deposit was verified in this pass, therefore it was **not** admitted and no paper-only source row was added.

### Existing high-priority candidates rechecked

- **L2-ARCTIC manual subset:** still strongly eligible in principle; manual corrected word/phone boundaries and substitution/deletion/addition tags are documented, but licensed file acquisition and exact isolation of the manual subset remain pending.
- **MRPL2:** public record confirms manually annotated mispronunciations and TextGrids, but deposited files remain restricted.
- **IFCASL:** >50% manually corrected phone/word segmentation is documented; public isolable corrected observation files remain unresolved.
- **RescueSpeech:** manual annotation is documented, but a phone/landmark-level manual layer has not yet been demonstrated from the public deposit.

No new source was promoted merely from article-level evidence.

## Validation and integrity

- `spike0017`: 35 intervals; final endpoint 4.075 s within 4.080 s utterance duration.
- `spike0018`: 74 intervals; final endpoint 6.595 s within 6.600 s utterance duration.
- `spike0019`: 40 intervals; final endpoint 4.195 s within 4.200 s utterance duration.
- Segment indices are unique and consecutive within each utterance.
- All derived interval durations are nonnegative.
- Every committed interval resolves to one of the three committed namespaced utterance IDs.
- Source blob SHA is retained on utterance rows.
- No raw audio was committed.
- No unrelated repository content was modified.

## Reconciled state

- 144 tracked sources
- 45 accepted
- 25 excluded
- 74 review
- 44 materially ingested sources
- 1,361,571 distinct promoted/parsed observations across 86 logical tables
- 970 inventory-file records
- 1,148 variable-dictionary records
- PAVOQUE durable source-faithful mirror: 19 utterances / 587 intervals

## Next backlog priorities

1. Continue PAVOQUE at the actual next source record, `spike0021`, preserving the non-contiguous prompt numbering.
2. Return to accepted Chenghai Southern Min TextGrid/XLSX files for observation-level token/interval/measurement ingestion rather than further registry expansion.
3. Retry the accepted real-time MRI archive and promote the 392 manually labelled masks and velopharyngeal-closure judgments when archive bytes are accessible.
4. Resolve L2-ARCTIC licensed acquisition and isolate only manually corrected TextGrids from forced-aligned-only files.
5. Search specifically for a public data deposit associated with the West Central Bavarian longitudinal vowel study; if found, retain complete F1/F2/F3 trajectories rather than midpoint reductions.
