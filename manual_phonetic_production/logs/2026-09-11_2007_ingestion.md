# Manually Annotated Phonetic Production Database — ingestion pass 2026-09-11 20:07 PT

## Backlog ingestion

### PAVOQUE angry subset
- Continued from canonical durable state `spike0038`.
- Re-read immutable upstream `marytts/pavoque-data/pavoque-angry.yaml` at blob `bff79d978f740f6c2b09bc595ade03b6bdf33423`.
- Materialized complete source records `spike0039`–`spike0042` into chunk012.
- Added 4 utterance rows and 183 manually corrected German-SAMPA/silence interval rows.
- Preserved original labels and source cumulative endpoints exactly; `start_rel_s` and `duration_s` are deterministic derivatives only.
- Verified every new utterance retains the native 0.005 s difference between final annotation endpoint and utterance duration; no endpoint was silently stretched.
- No IPA normalization was guessed.
- Registered both chunk012 files in the canonical PAVOQUE file manifest with repository blob SHA, byte size, upstream blob SHA, license, and row scope.
- Canonical durable PAVOQUE state after this pass: 12 chunks, 40 utterances, 1,321 intervals; next verified source record `spike0043`.
- Global distinct-observation count is unchanged because these same scientific observations are already represented in the validated full PAVOQUE CI parse.

## Discovery and deduplication

### Danish infant-/adult-directed speech — rediscovered, not duplicated
- Discovery search surfaced Cox et al. (2023), *Infant-directed speech does not always involve exaggerated vowel distinctions: Evidence from Danish*, *Child Development* 94:1672–1696, DOI 10.1111/cdev.13950.
- Primary spontaneous production from 26 Danish-speaking caregivers; methods report manual Praat segmentation of 9,267 vowel tokens (4,523 ADS; 4,744 IDS), with raw formant data/code linked on OSF `ywf9m` and raw audio/video not public.
- Before adding a new registry source, warehouse-level deduplication found the source already present as `cox_dideriksen_kerenportnoy_2023_danish_ids`: 19 files discovered, 15 tables parsed, 40,502 rows parsed, and 143 variable records.
- A provisional duplicate REVIEW increment created during this pass was deleted immediately after that lineage check; source counts therefore do not increase.
- This is a useful discovery audit result: the crawler correctly avoided double-registering an already materially ingested dataset even though the external search surfaced it under a different human-readable title/key.

## Other discovery screening
- Re-screened MRPL2: manual phonetic error annotations remain relevant but files are restricted; no duplicate record created.
- Re-screened RescueSpeech: record states manual annotation but phonetic interval/landmark granularity remains insufficiently resolved for core admission; no duplicate record created.
- Search also surfaced manually transcribed/semantic corpora and automatic-only alignment resources; these were not added simply to increase registry size.

## Validation and provenance
- Writes confined to `manual_phonetic_production/`.
- New PAVOQUE IDs remain namespaced as `pavoque::source_utterance_id`.
- 183/183 new interval durations are non-negative and endpoints are monotonic within utterance.
- For all four new utterances, final annotated endpoint = utterance duration - 0.005 s.
- No existing observation rows were overwritten.
- No raw audio was committed.
- Discovery candidates were checked against the warehouse before final source-count reconciliation.

## Next backlog priorities
1. PAVOQUE `spike0043+` using the same immutable upstream blob and source-faithful schema.
2. Continue deeper semantic harmonization of the already parsed Danish IDS/ADS source rather than creating a duplicate source record.
3. Continue compact IFA hand-segmented annotation archive acquisition/parsing.
4. Continue accepted Chenghai Southern Min and manually labelled rt-MRI observation-level ingestion.
5. Isolate only genuinely new manual annotation layers from reused-recording corpora before core promotion.
