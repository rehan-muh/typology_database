# Ingestion pass — 2026-09-07 18:09 PDT

## Backlog ingestion

Advanced `coretta_italian_vowel_2018_2026` from metadata/linkage/stimulus-only source-faithful coverage into the actual `measurements.csv` observation table.

- Authoritative upstream: `stefanocoretta/coretta2018itaegg`, `data-raw/datasets/measurements.csv`, ref `main`, immutable Git blob `f3a67a6acb3aac900584698c68966177fe4f04e7`.
- Direct blob/path verification establishes 3,268 data rows and 14 original variables: `speaker`, `ipu`, `stimulus`, `sentence_ons`, `sentence_off`, `word_ons`, `word_off`, `v1_ons`, `c2_ons`, `v2_ons`, `voice_ons`, `voice_off`, `c1_rel`, `c2_rel`.
- Added 90 contiguous source-faithful data rows (upstream source lines 2–91) plus the original header. No values were normalized, recoded, rounded, or imputed.
- Preserved literal source missing marker `--undefined--` exactly. Both whole-token missing timing records and partially missing landmark records occur in the materialized slice.
- Added a 14-record file-specific variable dictionary for `measurements.csv`; standardized mappings are separate metadata and do not overwrite source variable names or values.
- Added an append-only schema correction because a prior audit had incorrectly attributed an 18-field schema to the exact same path/blob. The historical audit remains in place; the correction is authoritative.
- Added a superseding partial-ingestion audit: source lines 1–91 / data rows 1–90 are durable; source lines 92–3269 / 3,178 data rows remain pending. Future full-table consolidation must supersede rather than double-count the first 90 rows.

### Integrity checks

- All three durable chunks are contiguous by upstream source line: 1–31, 32–61, 62–91.
- All three use the same immutable upstream blob SHA.
- The first chunk preserves the authoritative source header; later chunks preserve exact continuation lines without injecting headers into source-faithful content.
- Original orthographic stimulus strings are retained; no IPA or phonological context is inferred.
- Global warehouse counter increment is +90 observations only. The logical table is still partial, so complete-table count remains unchanged.
- Variable-record counter increment is +14 for the new file-specific dictionary.

## Discovery

Screened fresh repository/literature search results across Zenodo, GitHub and phonetics-production queries.

### Excluded

`khosravani_garner_codeswitching_swc` — *Code-Switching Speech Corpus* (Zenodo 4434251).

The deposit explicitly resegments a subset of the pre-existing German Spoken Wikipedia Corpus. Although SWC contains manually annotated word/segment XML, this release is secondary reuse/resegmentation of existing recordings rather than a new primary production collection, and this pass did not identify a separable genuinely new manual phonetic annotation layer that would justify inclusion under the current policy. Recorded as an explicit exclusion rather than a duplicate source.

### Screened but not newly added

- MRPL2 Non-native Speech Corpus: fresh Zenodo search reconfirmed 4,500 utterances from 23 speakers / 14 L1 backgrounds and manual phonetic-mispronunciation annotation, but the corpus is already represented in the warehouse as a restricted accepted annotation layer; no duplicate registry entry was created.
- Parker sonority data: fresh Journal-of-Phonetics/Mendeley search reconfirmed manually segmented English/Spanish recordings and CC BY 4.0, but this candidate is already represented in the existing discovery backlog; no duplicate entry was created.

## Counters after this pass

- Accepted: 39
- Excluded: 24
- Review: 48
- Parsed warehouse rows: 692,591
- Parsed complete logical tables: 73
- Variable records: 880

## Errors / conservative handling

One attempt to replace the first same-pass Coretta partial-ingestion audit was blocked by the connector safety layer. No data were lost. An append-only superseding audit was created instead, preserving both historical states and making the 90-row state authoritative.

## Next backlog priorities

1. Continue Coretta `measurements.csv` from upstream source line 92 through 3269; then consolidate the chunks without double-counting.
2. Validate joins from Coretta `speaker + ipu` and stimulus text/IDs into the completed stimulus and formant-linkage layers before generating namespaced harmonized token IDs.
3. Materialize VoxAngeles duration and canonical decile F0/F1/F2/F3 trajectory tables while keeping quartiles as overlapping alternate representations.
4. Continue Tang participant-level MAT trial extraction and ROG 1.1 source-faithful annotation parsing.
5. Continue repository-first discovery for public token/interval/trajectory deposits with explicit manual or exhaustively human-verified production annotation provenance.
