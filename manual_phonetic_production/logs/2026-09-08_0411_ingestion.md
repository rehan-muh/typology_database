# Ingestion pass — 2026-09-08 04:11 PT

## Backlog ingestion

- Continued `coretta_italian_vowel_2018_2026` from the current authoritative warehouse boundary of 900 durable `measurements.csv` observations.
- Upstream source: `stefanocoretta/coretta2018itaegg:data-raw/datasets/measurements.csv`.
- Immutable upstream blob: `f3a67a6acb3aac900584698c68966177fe4f04e7`.
- Added 90 source-faithful observations in three contiguous chunks:
  - source lines 902–931 = data rows 901–930 → `measurements_lines_0902_0931.csv`
  - source lines 932–961 = data rows 931–960 → `measurements_lines_0932_0961.csv`
  - source lines 962–991 = data rows 961–990 → `measurements_lines_0962_0991.csv`
- Cumulative durable Coretta `measurements.csv` coverage: 990 / 3,268 observations.
- Remaining: 2,278 observations. Exact next continuation: source line 992 / data row 991.
- Preserved all 14 original source fields exactly and retained literal `--undefined--` values. No timing normalization, IPA inference, missing-value reinterpretation, or context invention was performed.
- Lineage rule is unchanged: eventual full-table consolidation must supersede these partial chunks rather than append a second copy of the same observations.

## Validation

- The three new chunks are contiguous with prior durable coverage through source line 901 and do not overlap earlier chunks.
- All three upstream reads resolve to the same immutable source blob SHA.
- The final newly committed chunk was re-read from the target repository after commit and matches the extracted upstream observations.
- Coretta `measurements.csv` is still an incomplete logical table, so complete-table count remains unchanged.
- Warehouse counters after this increment: 693,491 parsed rows; 73 complete logical tables; 880 variable records.
- Registry remains 39 accepted / 24 excluded / 53 review.
- No writes were made outside `manual_phonetic_production/`.

## Discovery / screening

Fresh repository and literature discovery was run in parallel with backlog ingestion.

- **MRPL2 Non-native Speech Corpus** resurfaced as a strong manual-production candidate: 4,500 utterances from 23 speakers and manually annotated phonetic mispronunciations with annotated TextGrids. It is already represented in the review backlog, and the public Zenodo record still marks the files restricted, so no duplicate review row was added.
- **L2-ARCTIC** resurfaced with a clearly isolatable manually corrected subset: approximately 150 utterances per speaker include corrected word/phone boundaries plus substitution, deletion and addition labels. It is already in review, so no duplicate registry entry was created.
- **ROG 1.1 / ROG corpus** resurfaced as a manually annotated spoken Slovenian resource with prosodic layers, but it derives from pre-existing SST/GOS 2 recordings. It therefore remains a source-specific backlog requiring isolation of genuinely new manual annotation layers rather than wholesale treatment of the underlying recordings as new primary data.
- Searches also surfaced nonspeech and other out-of-scope annotation datasets; these were screened out without adding duplicate exclusion rows.

No source was promoted merely to increase registry size. File-level provenance and recording-lineage rules remain the admission gate.

## Next backlog priorities

1. Continue Coretta `measurements.csv` from source line 992 / data row 991.
2. On completion, consolidate Coretta chunks, validate joins to `stimuli.csv` and `formants-ids.csv`, and replace—not add to—partial row accounting.
3. Resume VoxAngeles canonical decile trajectory/duration materialization, retaining quartile trajectories as overlapping alternates.
4. Continue Tang MAT participant/trial extraction and isolate ROG 1.1 newly created manual prosodic annotations from inherited speech/transcription layers.
