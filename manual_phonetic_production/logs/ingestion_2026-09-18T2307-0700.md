# Ingestion pass — 2026-09-18 23:07 PT

## Backlog ingestion

- Source: PAVOQUE (`pavoque`), upstream `marytts/pavoque-data`, `pavoque-angry.yaml`.
- Immutable upstream blob SHA: `bff79d978f740f6c2b09bc595ade03b6bdf33423`.
- Processed `spike0019`, text `Würdest du gerne mit jemand anderem sprechen?`, style `angry`, source utterance span 66.96–71.16 s.
- Added 40 source-faithful manual segment rows to `data/source_faithful/pavoque_angry_spike0019_segments.csv`.
- Original labels and cumulative endpoints are preserved without IPA inference or silent correction.
- Validation: 40 endpoints; monotonically increasing; no negative implied durations; final endpoint 4.195 s versus source utterance duration 4.200 s (5 ms residual preserved).
- Upstream sequence jumps from `spike0019` to `spike0021`; `spike0020` is not present at this location and must not be fabricated. Next actual backlog cursor is `spike0021` (26 segment rows visible upstream), followed by `spike0022` (24), `spike0023` (16), and `spike0024`.

## Discovery

Ran fresh web discovery across manually annotated/manual-segmented phonetic production datasets, including repository-oriented searches for TextGrid/manual segmentation and manually corrected phonetic production data. No new candidate was promoted to accepted status from this search pass because the returned candidates did not provide stronger file-level evidence than the existing review backlog. Weak snippet-only hits were not admitted.

## Integrity / lineage

- No raw audio committed.
- No unrelated repository paths modified.
- No duplicate `spike0019` source-faithful shard was present before this pass.
- IDs/labels are retained source-faithfully; normalization remains a separate future operation.

## Next priorities

1. Continue PAVOQUE at the actual next source item `spike0021` (do not create `spike0020`).
2. Continue enumerating PAVOQUE angry YAML and then remaining style YAMLs while checking for prior shards before each ingest.
3. Isolate human-corrected tiers/files for RescueSpeech and L2-ARCTIC rather than admitting automatic layers.
4. Continue file-level investigation of CORPRES, Hiemstra–Sadakata, AusKidTalk, MRPL2/access status, and other existing review sources.
5. Prefer observation-level public deposits with explicit manual/manual-corrected annotation provenance over paper-only registry growth.
