# 2026-09-09 15:08 ingestion pass

## Backlog ingestion

- Continued `voxangeles_2024` from the exact recorded continuation point, source line 1256 of upstream `data/phonetic_measurements/voxangeles_meanf0_file.tsv`.
- Fetched authoritative upstream blob SHA `9492dd2ed35044219aed483613fd3f7a44f292d3` and materialized two new source-faithful chunks:
  - lines 1256-1355: 100 observations
  - lines 1356-1455: 100 observations
- Added 200 observations total; cumulative durable mean-F0 coverage is now 1,454 observations spanning source lines 2-1455.
- Original recording IDs and numeric strings were preserved verbatim; no rounding, normalization, IPA inference, or context invention was performed.
- Continuation point is now source line 1456.
- Audited-only inclusion rule remains unchanged. The unaudited forced-alignment branch is excluded.
- The UTF-16 duration and decile trajectory tables remain blocked by the current connector's byte retrieval behavior; no rows from those tables were counted.

## Validation

- New ranges are contiguous with the previous terminal line 1255 and non-overlapping.
- Both ranges came from the same immutable upstream blob already pinned in the source manifest.
- Source-level manifest and ingestion status were advanced consistently to lines 2-1455 / 1,454 rows.
- Global parsed-row count advanced by exactly 200, from 1,356,860 to 1,357,060. Logical-table, file-manifest, and variable-dictionary counts were not inflated because no new logical table/schema was created.

## Discovery and backlog triage

- Re-ran fresh web/repository searches for manually annotated or manually corrected production data across TextGrid/YAML and acoustic-production deposits.
- Directly enumerated the public `marytts/pavoque-data` repository. High-value annotation files include `pavoque-angry.yaml`, `pavoque-happy.yaml`, `pavoque-neutral.yaml`, `pavoque-outtakes.yaml`, `pavoque-poker.yaml`, and `pavoque-sad.yaml`, each pinned by Git blob SHA in the upstream repository. Published documentation states that the YAML contains timed utterances and optionally manually corrected phonetic segments.
- PAVOQUE was not counted as newly ingested in this pass because the YAML observations were not yet fully parsed and relationally validated. It remains a high-priority durable-ingestion target.
- A French OSCE dialogue dataset surfaced with manually corrected transcripts, but files are restricted and the corrections are transcript-level rather than a clearly isolatable phonetic measurement/boundary layer; it was not promoted.
- A 2026 L2 vowel-imitation study again surfaced with manual vowel-boundary annotation and F1/F2/duration measurement, but no newly verified public observation-level deposit was established in this pass; no registry inflation.

## Next priorities

1. Continue VoxAngeles mean-F0 from source line 1456 if no higher-value row-level source can be materialized first.
2. Parse PAVOQUE YAML source-faithfully into utterance and segment interval tables with full file manifest, variable dictionary, timing, style, text, segment labels, and lineage.
3. Unlock VoxAngeles UTF-16 phone/word durations and full 10-point F0/F1-F3 trajectories through a byte-capable retrieval route; never substitute midpoint summaries.
4. Continue Tang MAT, KEC corrected word intervals + EMA, Plastic Mandarin trajectory, CCOST metadata/TextGrid, and IFCASL corrected-tier backlog.

All repository writes in this pass were confined to `manual_phonetic_production/`.
