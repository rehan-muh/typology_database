# Ingestion pass — 2026-09-21 16:12 PT

## Backlog ingestion

Advanced accepted source `pavoque` using the already mirrored immutable `pavoque-angry.yaml` source layer. Promoted `m0192` (`Die Bauweise ist vom Boden abhängig.`; angry; 922.8–926.46 s) as one utterance and all 30 manually corrected phonetic/silence intervals. Original SAMPA-like labels and relative segment endpoints are source-faithful. Derived relative starts, durations, and absolute timestamps are stored separately. `normalized_ipa` remains blank rather than guessing mappings. The final annotated endpoint is 926.455 s, leaving the source's 5 ms residual to utterance end (926.46 s) intact.

Immutable upstream blob: `bff79d978f740f6c2b09bc595ade03b6bdf33423`. Verified local mirror: `source_faithful/pavoque/pavoque_angry_lines_8921_9020_source_faithful.yaml`.

Rows added: 31 (1 utterance + 30 segment intervals). Logical tables added: 2. Cumulative canonical parsed rows: 1,364,475; logical tables: 216.

## Discovery

Fresh web/repository screening targeted manually annotated/corrected phonetic production and speech deposits. High-relevance hits included MRPL2, L2-ARCTIC, IFCASL, ROG, RescueSpeech, VoxAngeles, and secondary German-English code-switching material. These are existing/known candidates or have lineage/manual-scope restrictions already represented in the backlog; no duplicate or weakly evidenced source was promoted this pass. Automatic-only timestamps/forced alignments and secondary reuse without isolatable new manual phonetic annotation remain excluded from core admission.

## Validation / next priorities

PAVOQUE source status now records 29 promoted utterances and 164 promoted segment intervals. Next high-value promotion is complete `m0193`–`m0198`, followed by the earlier mirrored backlog and then remaining style YAMLs. Continue mirroring from angry YAML line 9421. Preserve source labels, residuals, provenance, and immutable blob identity; do not infer IPA.

All repository writes in this pass are confined to `manual_phonetic_production/`.
