# Aggressive ingestion pass — 2026-09-07 16:11 PDT

## Backlog ingestion

### Coretta Northwestern Italian vowel production (`coretta_italian_vowel_2018_2026`)

Completed the outstanding source-faithful ingest of `data-raw/datasets/formants-ids.csv` from `stefanocoretta/coretta2018itaegg`.

Immutable upstream Git blob: `88c34bcc1cc59df178225878eeb331354af1565e`.

New durable chunks this pass:

- upstream lines 2701–2950: 250 rows
- upstream lines 2951–3200: 250 rows
- upstream lines 3201–3254: 54 rows

New rows this pass: **554**.

Together with the previously durable rows covering upstream lines 2–2700, the logical table is now complete: **3,253/3,253 data rows**, contiguous through source line 3254. Original columns and values (`speaker`, `interval`, `vowel`, `ipu`) are preserved source-faithfully; no IPA or contextual labels were inferred.

Lineage rule: these rows are a token/formant linkage table for the same production corpus, not 3,253 new recordings. A later consolidated copy of `formants-ids.csv` must supersede the chunk files rather than be added to them.

Warehouse counters after this pass: **692,329 parsed rows; 72 complete parsed logical tables; 855 variable records**. The table-count increment reflects that `formants-ids.csv` is now complete rather than a partial logical table.

Per-source `ingestion_status.csv` was updated from its stale 199-row partial state to `SOURCE_FAITHFUL_LINKAGE_COMPLETE` with 3,253 rows.

## Discovery

### Barkov, Santiago & Peralta — multilingual vowel reduction

Added `barkov_santiago_peralta_2022_vowel_reduction` to the review queue. The study is methodologically eligible in principle: primary productions from 35 speakers spanning Mexican Spanish, American English, Russian, L2 English and L3 Russian; all productions were manually transcribed and segmented in Praat. The reported retained dataset contains 1,192 vowel tokens, with midpoint F1/F2, vowel duration, Lobanov-normalized values, derived Euclidean-distance centralization, task and language/group structure.

Decision: **REVIEW, not core yet**. This pass verified the manual production/segmentation methods from the publication but did not locate a directly downloadable row-level public token table with clear redistribution terms. Do not create rows from article summaries. Next action is to locate any Cambridge supplement, author repository, OSF/Zenodo deposit, or institutional archive and inventory actual files before admission.

### Other screening

Re-screened manually segmented speech-to-song material and Parker sonority data while searching repositories. The former is already correctly excluded as third-party audiobook/pre-existing media despite new manual segmentation; Parker is already tracked as a promising manually segmented primary-production deposit, so no duplicate registry record was added.

## Validation / integrity

- All writes confined to `manual_phonetic_production/`.
- No raw audio committed.
- New Coretta chunks use the same immutable upstream blob as all prior linkage chunks.
- Source-line coverage is now contiguous from line 2 through line 3254 with no intended gaps or overlaps.
- Registry addition is review-only; warehouse counts are unaffected by discovery.
- `STATUS.json` and `data/warehouse/ingestion_status.csv` updated to match the completed ingest.

## Next backlog priorities

1. Coretta `stimuli.csv` — 172 × 11, complete source-faithful ingest.
2. Coretta `measurements.csv` — 3,268 × 14, preserve literal `--undefined--` convention before harmonization.
3. Run Coretta harmonizer and validate token/stimulus/linkage joins without double-counting overlapping `formants.rda`/`ita_egg.rda` objects.
4. VoxAngeles durations, F0 deciles and F1/F2/F3 deciles; retain quartiles only as alternate representations.
5. Tang participant-level MAT trial structures and ROG 1.1 annotation archive.
6. Locate row-level public deposit for Barkov et al. and continue repository-first discovery across phonetics/sociophonetics/L2/clinical/articulatory production.
