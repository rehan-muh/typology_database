# Ingestion pass — 2026-09-07 17:10 PDT

## Backlog ingestion

Completed source-faithful materialization of Coretta et al. Northwestern Italian `data-raw/datasets/stimuli.csv` from `stefanocoretta/coretta2018itaegg` immutable Git blob `32ac81d0d7a8df152645dd20f26cac522c313f1d`.

- 172 data rows, 11 original variables.
- Preserved exactly: `stimulus_id`, `stimulus`, `sentence`, `word`, `c1`, `vowel`, `c2`, `backness`, `height`, `c1_place`, `c2_place`.
- Added a complete 11-variable source-specific dictionary covering mapping, domains, meanings, coding levels, annotation method, time/context reference, provenance, and notes.
- No IPA was inferred from spelling or source segment labels.
- The stimulus table is metadata/context and is not counted as new recordings or new speech tokens.
- Validation: committed source table ends at `stimulus_id=172`; expected and materialized row counts match; upstream blob matches the pre-existing immutable manifest.

Warehouse counters after this pass: 692,501 parsed rows; 73 complete logical tables; 866 variable records.

## Discovery / screening

Screened Hiemstra & Sadakata (2024), *Sentences used in the speech-to-song illusion: Comparisons of acoustic vowel space*. The study is attractive superficially because words/phonemes/vowels were manually segmented in Praat and vowel formants were analyzed. It is excluded, however, because the underlying sentence recordings were drawn from previously existing speech-to-song stimulus corpora including audiobooks and UvA Music Cognition materials. Under the database policy this is secondary reuse / third-party media rather than primary study-recorded production. Added an append-only exclusion increment; no rows were ingested.

## Integrity / lineage

- `coretta2018itaegg::stimuli` remains distinct from `coretta2018itaegg::production_tokens`.
- `formants-ids.csv`, `measurements.csv`, `ita_egg.rda`, and `formants.rda` remain in one production-token lineage and must not be summed as independent recordings.
- Source-faithful values are retained separately from future harmonized fields.

## Next backlog priorities

1. Materialize all 3,268 rows × 14 original variables from Coretta `measurements.csv`, preserving literal `--undefined--` missing conventions and all temporal landmarks.
2. Validate joins among Coretta measurements, stimuli, words, and formant-linkage tables without inventing missing context.
3. Resume VoxAngeles canonical decile F0/formant trajectory and duration ingestion, keeping quartiles as overlapping alternates.
4. Continue Tang/Parrell/Niziolek MAT-trial and ROG 1.1 backlog processing.
5. Continue repository-first discovery for public primary-production deposits with explicit manual/manual-correction evidence.
