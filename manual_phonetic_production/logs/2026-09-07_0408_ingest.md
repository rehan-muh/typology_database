# Ingestion pass — 2026-09-07 04:08 PDT

## Backlog ingestion

### VoxAngeles manual layer

Performed a fresh upstream audit of the six public phonetic-measurement tables in `pacscilab/voxangeles`. Verified exact Git blob identities and byte sizes for duration, F0, formant, and recording-level mean-F0 tables. Added `data/warehouse/voxangeles_2024_upstream_file_audit_2026-09-07_0408.csv`.

Important lineage rules recorded in the audit:

- `voxangeles_durations.tsv` belongs to the audited manually corrected phone-alignment lineage.
- F0 deciles and quartiles are overlapping representations of the same vowel-token lineage and must not be counted independently.
- F1/F2/F3 deciles and quartiles likewise overlap and must not be counted independently.
- `voxangeles_meanf0_file.tsv` is recording-level metadata/measurement, not a vowel-token table.
- The unaudited MFA layer remains excluded from the manually grounded core.

The connector exposed the complete upstream directory inventory and partial textual access to `voxangeles_meanf0_file.tsv` (verified schema: `file`, `meanf0`), but the runtime did not expose the multi-megabyte TSV contents as locally executable files. Therefore no warehouse row count was increased and no observation rows were fabricated. The existing `scripts/ingest_voxangeles_manual_layer.py` remains the correct full-materialization path once byte-level download is available to the execution runtime.

## Discovery

### Sprachspuren / PAD manually corrected alignment layer — REVIEW

Added `sprachspuren_pad_corrected_2021` to an append-only hour-stamped review increment. The project description states that students create dialect and Standard German orthographic transcriptions, generate WebMAUS TextGrids, and then manually correct the resulting TextGrids; corrected TextGrids are combined into CSV. The working analysis reports 25 aligned PAD recordings.

Decision: REVIEW, not core. The underlying PAD recordings are pre-existing historical recordings, so only the new corrected alignment/transcription layer could qualify. A stable downloadable corrected TextGrid/CSV deposit, file-level license, and exact lineage still need verification.

## Counts and validation

- Accepted sources: 38 (unchanged)
- Excluded sources: 19 (unchanged)
- Review queue: 37 (+1)
- Parsed warehouse observations: 431,751 (unchanged; conservative)
- Parsed tables: 64 (unchanged)
- Parsed variable records: 846 (unchanged)

All permanent changes in this pass are confined to `manual_phonetic_production/`.

## Next backlog priorities

1. Materialize VoxAngeles TSV bytes and run the existing source-faithful ingester; retain decile trajectories and namespaced token/recording IDs while preventing quartile/decile double counting.
2. Convert Tang participant-level `dataVals.mat` / `expt.mat` trial structures into trial and trajectory tables; preserve all F0/F1/F2/intensity samples and raw acoustic time axes.
3. Continue Mitra-Dutta recursive OSF archive parsing and SwehVd raw-formant parsing.
4. Parse ROG 1.1 EXMARaLDA/TRS/TEI/CONLL-U annotation archive when binary materialization is available.
5. Locate a stable PAD/Sprachspuren corrected alignment deposit before any admission.
