# Ingestion pass — 2026-09-21 00:09 PT

## Backlog ingestion / integrity audit

- JSUT: prior durable source-faithful ingestion is contiguous through `BASIC5000_1520`; next cursor remains `BASIC5000_1521`. This pass verified that public GitHub mirrors expose the recording IDs but did **not** expose the authoritative manual phonetic/prosodic symbol sequence in a form sufficient to ingest new observation rows without risking substitution of automatic/derived labels. Therefore no mirror-derived symbols or automatic timing were admitted.
- Deduplication rule retained: `source_id::utterance_id` is the scientific key; mirror copies and downstream TTS/ASR training lists do not create new observations.
- No existing accepted observations were overwritten.

## Fresh discovery / screening

### REVIEW — manually corrected TextGrid / phonetic deposits
Searches targeted public deposits containing `manually corrected`, `TextGrid`, `manual segmentation`, VOT, formants, ultrasound/EMA, prosody and voice-quality production data. Candidate hits were kept out of core unless the public record established both primary non-singing production and an isolatable human-created/corrected phonetic layer.

### REVIEW — IFCASL
French/German learner-speech corpus remains a high-priority access-tracing target because published corpus documentation describes phone/word segmentation with a substantial manually corrected portion. Core admission is deferred until the corrected observation files can be isolated from automatic alignment output and their public redistribution/access status verified.

### REVIEW — CORPRES
Russian phonetic/prosodic corpus remains high priority because corpus documentation describes a manually segmented and fully annotated subset with multiple phonetic/prosodic levels. Core admission is deferred until an authoritative public observation-level deposit is located and the manual subset can be separated from any non-manual material.

### REVIEW — L2-ARCTIC manual subset
Retain only the manually examined/corrected utterance subset as potentially qualifying. Corpus-wide forced-alignment products remain excluded. Next action is to enumerate the correction/error annotation files and establish a recording-level join that cannot leak automatic-only boundaries into core.

## Exclusion discipline

- Downstream JSUT TTS/ASR mirrors: lineage-only evidence; not independent datasets and not used as authoritative manual annotations.
- Automatic timing/forced alignment without explicit exhaustive human verification: excluded.
- Singing/music, perception-only, synthetic speech, and third-party-media collections: excluded by standing criteria.

## Validation

- Writes confined to `manual_phonetic_production/`.
- No raw audio committed.
- No inferred IPA or context added.
- No observation count increased without source-faithful observation-level data.
- No duplicate recording lineage admitted.

## Next backlog priorities

1. Resume JSUT at `BASIC5000_1521` only from the authoritative manual-symbol source.
2. Trace and ingest the isolatable manually corrected L2-ARCTIC subset at observation level.
3. Locate/download CORPRES manual phonetic/prosodic annotation files if publicly accessible.
4. Locate/download IFCASL corrected phone/word segmentation files and separate them from automatic output.
5. Continue trajectory-first ingestion for sources exposing full F1/F2/F0/articulatory time series rather than midpoint summaries.
