# Ingestion pass — 2026-09-09 07:12 PDT

## Backlog ingestion

### VoxAngeles (`voxangeles_2024`)

Promoted the separately isolatable **human-audited annotation layer** to accepted core lineage. The pre-existing source recordings themselves are not counted as a novel VoxAngeles recording collection. Core is restricted to `data/audited_aligned` and measurements derived from those corrected TextGrids; `data/unaudited` remains excluded because it is force-aligned without manual correction.

Durably ingested 54 exact source-faithful Abkhaz recording-level rows from `data/phonetic_measurements/voxangeles_meanf0_file.tsv` (upstream Git blob `9492dd2ed35044219aed483613fd3f7a44f292d3`). Post-write reread matched first/last source IDs and full-precision values.

Inventoried all six public phonetic-measurement tables and four extraction scripts with exact upstream Git blob SHA and file size. Added 75 table-specific variable-dictionary records for every verified variable in the duration, F0-decile, formant-decile and mean-F0 schemas. Quartile-table variables were not guessed; those schemas remain pending direct verification.

Verified extraction semantics from upstream Praat code: duration tables retain word/phone labels, previous/following phone context, phone interval index, and phone/word boundaries; F0 is sampled at 10 points across each audited vowel; F1-F3 are each sampled at the same 10 normalized positions. Raw source values remain distinct from future normalized/long views.

Large measurement files could be inventoried but not safely returned in full through the current connector response size path. This pass therefore records them as pending full mirror rather than falsely counting them as parsed. Next continuation should mirror complete durations, F0 deciles, formant deciles and remaining mean-F0 rows, then enumerate audited TextGrid ZIP contents.

## Discovery

Added `chang_chien_lu_2026_l2_vowel_imitation` to HIGH review. The July 19, 2026 paper reports primary Mandarin-L1 English-vowel imitation with all recorded imitations manually vowel-bounded by three trained annotators, duration plus midpoint F1/F2 extraction, and a 12% common reliability subset (Krippendorff alpha F1=.92, F2=.80, duration=.86). No public observation-level repository was resolved in this pass, so it was not admitted to core.

Automatic VOT-estimation resources and singing-derived hits were screened but not admitted without an isolatable qualifying manual primary-production layer.

## Accounting delta

- tracked sources: +2 (1 accepted; 1 review)
- materially ingested sources: +1
- parsed rows: +54
- logical parsed tables: +1
- upstream file-manifest records: +10
- variable-dictionary records: +75

## Next backlog priorities

1. VoxAngeles full duration + 10-point F0 + 10-point F1-F3 tables and audited TextGrid package manifests.
2. OOCC manually corrected transcript ZIP retrieval/parsing.
3. Tang participant-level MAT extraction.
4. KEC human-corrected word intervals + linked EMA.
5. Plastic Mandarin 21-point manually corrected F0 trajectories.
6. IFCASL isolation of only manually corrected files/tiers.
7. PAVOQUE durable phone-segmentation mirroring.
