# Ingestion pass — 2026-09-18T23:07Z

## Scope
Aggressive discovery + backlog audit for the manually annotated primary non-singing phonetic production database. Writes confined to `manual_phonetic_production/`.

## Backlog ingestion/audit

### PAVOQUE expressive speech — accepted
- Re-opened authoritative upstream `pavoque-angry.yaml` (`marytts/pavoque-data`, Git blob `bff79d978f740f6c2b09bc595ade03b6bdf33423`) and checked the next continuation point against the local source-faithful shards.
- Local ingestion already contains `spike0001`–`spike0008`; the next source utterance is `spike0009`. This prevents re-ingesting `spike0007`/`spike0008` and establishes the continuation cursor explicitly.
- Confirmed that the upstream YAML retains utterance-level `prompt`, `style`, `time`, `start`, and full `segments` sequences with original segment labels and endpoint times. No trajectory/midpoint reduction or IPA invention is appropriate.
- No duplicate observation rows were appended in this pass because the immediately visible continuation material (`spike0007`/`spike0008`) is already represented locally. Next ingestion must start at `spike0009` and continue through the angry YAML before moving to the other five YAML objects.

### JSUT manually annotated labels — accepted backlog
- Re-audited the upstream `sarulab-speech/jsut-label` tree against local shards. Local source-faithful ingestion currently reaches `jsut_labels_0951_0980.csv`; upstream contains additional manually annotated TextGrid material beyond that cursor.
- Explicit continuation cursor: first unprocessed item after the current local shard is the source item beginning at 0981. Preserve original Japanese phone/word labels and all interval boundaries before any normalization.
- No duplicate rows were appended while establishing this cursor.

## New discovery

### AusKidTalk — REVIEW, high priority
- Candidate: AusKidTalk, a large Australian child-speech corpus described in *Scientific Data* (2022), with repository material distributed through Figshare/DataCite-linked records.
- Primary production lineage: recordings were collected directly from Australian children rather than reused from a pre-existing speech corpus.
- Corpus documentation reports orthographic and phonetic transcriptions and discusses manual correction/validation in the annotation workflow. Because the surfaced documentation also involves automatic speech-processing components, admission must be subset-specific: only annotation/measurement objects whose manual creation, correction, or exhaustive human verification can be established at file/observation level qualify.
- Keep in REVIEW until the Figshare file manifest is enumerated and the manually verified layers can be isolated from automatic products. Do not admit corpus-wide forced-aligned/automatic boundaries merely because manual transcription exists elsewhere in the corpus.
- High-value potential domains if qualification is confirmed: child speech production, pronunciation/phone labels, timing, lexical/task metadata, demographic metadata, recording metadata, and clinically relevant speech-development variables.

## Exclusion discipline / lineage
- No third-party media, perception-only, singing, synthetic-speech, or unverified automatic products admitted.
- No automatic tiers promoted to manual status.
- No source-specific labels normalized to IPA without an explicit defensible mapping.

## Integrity checks
- Checked PAVOQUE continuation against existing source-faithful shard names to avoid duplicate recording/token lineage.
- Checked JSUT continuation against existing shard endpoint (`0951_0980`) before setting the next cursor.
- No files outside `manual_phonetic_production/` modified.

## Next backlog priorities
1. JSUT: ingest 0981 onward in bounded source-faithful shards, retaining complete TextGrid interval content and immutable upstream provenance.
2. PAVOQUE: continue at `spike0009`; exhaust angry YAML, then remaining YAMLs, preserving all source fields.
3. AusKidTalk: enumerate Figshare/DataCite file objects, licenses, checksums, and annotation-method provenance; isolate manually corrected/verified layers before acceptance.
4. Continue file-level work on CORPRES, RescueSpeech, Hiemstra–Sadakata speech material, L2-ARCTIC manual subset, Russian Fricatives, and other accepted/review backlog rather than adding paper-only registry entries.
