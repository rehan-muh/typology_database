# Manual phonetic production ingestion log — 2026-09-11 21:11 PT

## Backlog ingestion

Canonical repository state was re-read before writing. The durable PAVOQUE status at the start of the pass was 12 chunks / 40 utterances / 1,321 intervals through `spike0042`; upstream `marytts/pavoque-data/pavoque-angry.yaml` remains immutable at blob SHA `bff79d978f740f6c2b09bc595ade03b6bdf33423`.

Materialized source-faithful chunk 013:

- `spike0043` through `spike0051`
- 9 complete utterance rows
- 151 manually corrected German-SAMPA/silence interval rows

Committed files:

- `data/source_faithful/pavoque_angry_utterances_chunk013.csv`
- `data/source_faithful/pavoque_angry_segments_chunk013.csv`
- `data/source_faithful/pavoque_angry_file_manifest_chunk013.csv`

All original source labels and cumulative endpoints are retained. Segment starts and durations are deterministic endpoint differences. No IPA was invented. Source-native utterance/annotation timing differences were preserved rather than silently extending the final interval.

Canonical PAVOQUE status now records 13 chunks, 49 utterances, and 1,472 intervals through `spike0051`; `spike0052` is the next verified upstream record.

These durable rows do not increase the global scientific-observation total because the same annotation lineage is already represented in the validated full PAVOQUE parse.

## Fresh discovery

Added one new HIGH REVIEW candidate:

### BESST / Speech production under stress for machine learning

Pešán et al. (Scientific Data, 2024; DOI 10.1038/s41597-024-03991-w) report primary Czech speech from 79 participants recorded during controlled stress and cognitive-load tasks. Their BESSTiANNO system was used for precise segmentation and manual verification of audio and video streams, and the paper states that participant data underwent manual checks/corrections. The resource therefore potentially satisfies the human-verified production criterion and contains unusually rich synchronized condition/physiology metadata.

It is not admitted to core yet because obtaining the released dataset requires signing a license agreement. Exact released segmentation metadata and the boundary/label creation history must be enumerated so that only genuinely human-created or human-corrected speech annotations enter the core.

Registry increment: `data/registry/increments/2026-09-11_2111_review.csv`.

## Validation

- New utterance IDs use `pavoque::source_utterance_id` namespacing.
- All 151 interval rows resolve to the nine newly materialized utterances.
- Segment indices are unique and ordered within utterance.
- All computed durations are nonnegative endpoint differences.
- Upstream PAVOQUE SHA is unchanged.
- No raw audio was committed.
- BESST remains REVIEW because access/annotation-layer isolation is unresolved.
- All writes are confined to `manual_phonetic_production/`.

## Reconciled state

- 151 tracked sources
- 46 accepted
- 25 excluded
- 80 review
- 44 materially ingested sources
- 1,361,571 distinct promoted/parsed observations across 86 logical tables
- 1,021 inventory-file records
- 1,148 variable-dictionary records
- PAVOQUE durable mirror: 49 utterances / 1,472 intervals; next `spike0052`

## Next backlog priorities

1. Continue PAVOQUE at `spike0052` and materialize another contiguous block rather than single-record increments where possible.
2. Retry IFA compact annotation archive retrieval and parse `Labels-chunks`, validation labels, sentence labels, and protocol/database exports immediately if bytes become reachable.
3. Enumerate the Bengali-English OSF trajectory deposit and ingest all five F1/F2 samples per vowel token plus all source predictors.
4. Isolate L2-ARCTIC's manually corrected subset from its forced-aligned remainder.
5. Enumerate BESST released segmentation metadata if license-accessible, preserving synchronization/task/stress variables and human-verification provenance.
6. Continue Chenghai Southern Min and manually labelled rt-MRI observation-level ingestion.
