# Ingestion log — 2026-09-23 07:08 PT

## Discovery
Aggressive repository search covered manual annotation, manual correction, phonetic production, TextGrid/segmentation and speech-production deposits on Zenodo/OSF. Screened obvious non-production and excluded-domain hits (singing/music, non-speech annotation, third-party-media speech). COVYT was not admitted because its source recordings are public social-media videos despite manual segmentation/diarisation. AdoVoc Pro and Jingju were rejected by the non-singing criterion.

## Newly accepted: PxCorpus (Zenodo 10080490)
Primary French spoken drug-prescription experiment. Deposit states 4 h of transcribed/annotated dialogues, 55 participants, automatic transcripts verified by humans, and describes 2,067 recordings as manually transcribed and semantically annotated. Detailed category table gives 903 sessions, 1,981 recordings, 262.27 minutes (434 medical-expert + 570 doctor + 977 non-expert recordings). Preserve this count discrepancy pending direct archive enumeration.

Deposit structure documented at observation level: `PxSLU/paths.txt`, `metadata.txt`, `seq.in`, `seq.out`, `seq.label`, `PxSLU_conll.txt`, WAV `recordings/`, split files, plus `PxDialogue/events.txt` and manually annotated `dialogue_annotations.txt`. Audio is mono 16 kHz, 16-bit signed PCM. `paths.txt` row order links recordings to transcript, semantic labels, intent and metadata. Manual dialogue annotation uses OK/Fail judgments.

Archive: `PxCorpus.zip`, 375.7 MB, MD5 `97247fc5278cf4e3ab147aab4fc72f14`, CC BY 4.0. Large archive not committed; authoritative Zenodo reference/checksum retained.

## Database actions
- Added accepted source registry increment.
- Added source file manifest with archive checksum and expected member inventory.
- Added 9-entry variable dictionary covering recording path, participant id, age range, gender, expertise category, human-verified transcript, BIO entities, intent, manual dialogue judgment, and event timestamp (10 rows including temporal event field).
- Updated STATUS counts to 255 total / 67 accepted / 35 excluded / 153 review; 1,279 inventory records; 1,306 variable dictionary records.
- No observation-row totals incremented because archive bytes were not fetched/parsed in this run; this avoids claiming data ingestion not actually completed.

## QC / lineage
Primary recordings were collected experimentally from participants, not third-party media. Human verification/manual transcription is explicit. No phone-level boundaries or IPA are inferred. Semantic labels and dialogue judgments are retained as supplied. Recording-count discrepancy is explicitly unresolved rather than silently normalized.

## Next backlog
1. Fetch/stream-enumerate PxCorpus archive and ingest parallel text tables; build namespaced speaker/recording/utterance IDs and validate all parallel row counts.
2. Enumerate CCOST archive members and parse metadata/stimulus files plus eligible corrected TextGrids.
3. Continue canonical PAVOQUE promotion only after checking existing promoted IDs to avoid duplicate observations.
4. Continue object-level access work for IFCASL/CORPRES/BAS manual corpora.
