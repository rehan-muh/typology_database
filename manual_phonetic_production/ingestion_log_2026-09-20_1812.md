# Ingestion pass — 2026-09-20 18:12 PT

## Backlog ingestion

- Advanced accepted PAVOQUE angry source-faithful parse from literal upstream record `m0136` to `m0137`.
- Parsed 21 manually corrected phonetic intervals from immutable upstream `pavoque-angry.yaml` blob `bff79d978f740f6c2b09bc595ade03b6bdf33423`.
- Preserved original German-SAMPA-like labels and cumulative endpoints exactly; derived interval starts/durations only from adjacent endpoints.
- Utterance `pavoque::m0137`: prompt `New York: Springer Verlag.`, source utterance 705.00–708.06 s, final annotation endpoint 3.055 s, 0.005 s terminal residual.
- No IPA normalization was invented. No raw audio added. Existing spike-specific convenience files remain lineage duplicates and do not increment scientific observation counts.
- Durable PAVOQUE totals after this pass: 51 chunks, 183 utterances, 5,750 intervals. Next literal upstream cursor: `m0138`.

## Discovery / screening

Fresh searches covered Zenodo, GitHub, manually corrected TextGrid, formant/VOT, production-corpus and manual-annotation terms.

- MRPL2: revalidated as high-value but restricted; 4,500 manually annotated non-native English utterances / 23 speakers / 14 L1s. No observation-level ingest because files remain access-restricted.
- RescueSpeech: rediscovered and deduplicated against existing source manifest; no duplicate registry row created.
- PAVOQUE: upstream manual-correction criterion reconfirmed; continued existing lineage rather than creating a new source.
- Golos Russian speech corpus: candidate for REVIEW rather than core admission in this pass. Public descriptions say recordings are manually annotated, but the surfaced evidence concerns transcription/crowdsourced speech labels rather than an isolatable phonetic interval/landmark/measurement layer; requires repository-level schema inspection before admission.
- REYD Yiddish TTS Corpus: candidate for REVIEW. Hand-corrected text and checked segmentation are documented, but segmentation is described as automatic; do not admit unless human verification can be shown to cover the relevant time-aligned production layer.
- Nonspeech7k: excluded from this production database scope (human nonspeech event classification, third-party source media including Freesound/YouTube).
- MSNER: excluded as secondary reuse of VoxPopuli with named-entity annotations rather than new phonetic production annotations.
- MASC/MEG-MASC/NDC treebank hits: not admitted; annotations are linguistic/experimental-listening layers or secondary corpus reuse, not qualifying new phonetic production measurements.

## Integrity / provenance checks

- Namespaced PAVOQUE utterance ID used (`pavoque::m0137`).
- Original label and endpoint values retained.
- 21 intervals strictly ordered and non-overlapping by deterministic construction.
- Final endpoint 3.055 s is less than 3.060 s utterance duration by 0.005 s, matching established source-native residual handling.
- Upstream blob SHA unchanged from prior durable chunks.
- No changes outside `manual_phonetic_production/`.

## Next priorities

1. Continue durable PAVOQUE parse at literal source record `m0138` (then `m0139`, `m0141`, etc.; never infer missing numeric prompt IDs).
2. Continue JSUT manual-symbol backlog using its current ingestion-status cursor, guarding against duplicate convenience exports.
3. Parse full Tibetan English diphthong F1/F2 trajectory files if public object-level access can be materialized.
4. Materialize and parse UltraSuite/UXTD/UXSSD/UltraPhonix manual reference/SLT layers, excluding automatic phone labels.
5. Enumerate TaL manual segmentation CSVs and link them to media manifests without committing bulk media.
6. Inspect Golos repository schema/license to determine whether any genuinely phonetic/manual time-aligned layer qualifies beyond transcript labels.
