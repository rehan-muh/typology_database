# Ingestion pass — 2026-09-14 12:09 PT

## Backlog ingestion

### PAVOQUE angry durable source-faithful mirror
- Continued literal upstream YAML order after `m0132` with `m0133`–`m0136`.
- Added `data/source_faithful/pavoque_angry_utterances_chunk050.csv`: 4 utterance rows preserving source IDs, German text, expressive style, absolute utterance bounds, source filename, and upstream blob SHA.
- Added `data/source_faithful/pavoque_angry_segments_chunk050.csv`: 115 manually corrected German-SAMPA/silence interval rows.
- Preserved each original label and cumulative endpoint exactly. Derived only interval start (previous endpoint; 0 for first interval) and duration (end-start). No IPA normalization or missing context was invented.
- All four utterances have the source-native 0.005 s terminal residual between utterance duration and final segment endpoint; these were preserved rather than repaired.
- Added `pavoque_angry_chunk050_manifest.json` with row counts, blob SHAs, transformations, residuals, next literal source record, and explicit double-counting guard.
- Updated `pavoque_angry_ingestion_status.json`: 50 durable chunks, 182 utterances, 5,729 intervals; latest `m0136`, next `m0137`.
- The durable chunk mirror is a provenance/completeness layer for a lineage already represented in the validated full PAVOQUE parse, so these 119 rows do **not** increment the distinct scientific-observation total.

## Discovery / review queue

### HAMATAC v1.0.0 — HIGH review
- Authoritative ZFDM/CLARIN metadata reports primary L2-German map-task speech, 26 speakers, 24 communications, 3.3 h, 24 recordings/transcriptions, and 21,433 words.
- Explicit manual layers: orthographic transcription, disfluency annotation, and `pho` manual annotation of phonetic phenomena.
- Automatic TreeTagger POS/lemma layers must remain segregated/excluded unless a manually corrected layer can be isolated.
- Data access is restricted, so no row-level files were fabricated or admitted. Next action: resolve authorized access, enumerate files/checksums, and ingest only manual tiers.
- Version lineage must supersede/deduplicate earlier HAMATAC releases.

### CLIPS_MT_MANUAL — HIGH review
- BAS documentation reports 3,228 inspected/partially repaired WAV turns, 3,228 corrected CLIPS annotation sets, BPF/Praat/EMU annotation derivatives, 30 speakers, and 104,056 phonetic segments.
- Documentation explicitly states manual phonetic segmentation and partial closure+burst segmentation in plosives.
- This is secondary use of legacy CLIPS recordings; eligibility is limited to the genuinely new corrected/manual annotation layer. Original CLIPS recordings must not be counted as new recordings.
- Access/distribution is controlled by BAS terms (no redistribution to third parties); no corpus bytes were copied in this pass.

## Registry / inventory changes
- Added `data/registry/review_increment_2026-09-14_1209.csv` with HAMATAC and CLIPS_MT_MANUAL.
- Added `data/registry/file_manifest_increment_2026-09-14_1209.csv` with the two authoritative metadata/documentation objects.
- Updated `STATUS.json`: 207 total sources; 50 accepted; 31 excluded; 126 review; 49 materially ingested; 1,362,292 distinct promoted observations; 1,196 inventory records; 1,236 variable-dictionary records.

## Validation
- Namespaced PAVOQUE IDs retained.
- Source record order follows literal YAML sequence, not assumed numeric continuity.
- Segment endpoints are monotonically increasing within each newly ingested utterance.
- Derived durations are non-negative.
- No normalized IPA generated without a documented mapping.
- No duplicate scientific observations counted from the durable PAVOQUE mirror.
- New discovery candidates remain review-only because access/lineage conditions are unresolved.
- All writes in this pass were confined to `manual_phonetic_production/`.

## Next backlog priorities
1. Continue PAVOQUE angry from literal source record `m0137`, then advance other PAVOQUE expressive styles while retaining the same lineage guard.
2. Resolve HAMATAC restricted access and enumerate its EXMARaLDA/manual `pho` and disfluency tiers separately from automatic POS/lemma annotations.
3. If CLIPS_MT_MANUAL access is available, enumerate ACS/PHN/STD/WRD/PAR/TextGrid/EMU files and isolate corrected/manual annotations against original CLIPS lineage before promotion.
4. Continue existing accepted-source backlogs with accessible token/trajectory/annotation files rather than adding paper-only registry records.
