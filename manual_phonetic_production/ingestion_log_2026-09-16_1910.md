# Ingestion pass — 2026-09-16 19:10 PT

## Discovery
Searched recent/direct OSF, Zenodo, GitHub and phonetic-production repository results with emphasis on manual Praat/TextGrid segmentation and manually corrected production measurements. Most surfaced records were tools, non-speech, restricted, or already-known corpora and were not admitted.

New material candidate: `bortlik_vowel_formants_demo_2026` (https://github.com/jakubbortlik/vowel-formants). Repository directly exposes `sounds/`, `textgrids/`, `results/`, `stimuli.csv`, and analysis code. README states that the interface supports human inspection of spectrograms/formants/pitch and manual mark/unmark decisions. The inspected `speaker1_f.TextGrid` contains two interval tiers (`word`, `vowel`), and `results/speaker1_f.csv` contains 8 vowel observations with F0, F1, F2, F3, F2/F1, F3/F2, duration, measurement proportion, phrase position, following-consonant voicing, and manual marked flag.

Conservative decision: REVIEW, not accepted core, because this pass could not establish from the public repository alone that the example recordings are a primary research deposit or that all word/vowel boundaries were manually created/corrected. The observations are nevertheless materially preserved in a review-layer source-faithful representation so future provenance resolution does not require re-parsing.

## Backlog/material ingestion
Parsed and committed all 8 accessible token measurement rows from `results/speaker1_f.csv`. Parsed and committed all 16 non-empty TextGrid intervals (8 word + 8 vowel) from `textgrids/speaker1_f.TextGrid`, retaining exact source labels and floating-point boundaries. No IPA was inferred. Mapped all 14 result columns plus TextGrid xmin/xmax/text semantics in a 17-row variable-dictionary increment. Source blob SHAs retained in provenance notes/log: TextGrid `16637408b4aa3a9f65476ab2be8af2c6178ba039`; results CSV `ce8171d37f1649588460ba56396e15d621b9529e`.

## Counts for this pass
- new review candidates: 1
- new source-faithful token rows: 8
- new source-faithful interval rows: 16
- new variable-dictionary rows: 17
- files materially parsed: 2
- automatic/forced-aligned observations promoted: 0
- invented IPA/context: 0

## Validation
Namespaced source identifier used throughout. Word/vowel tier counts match (17 total intervals each; 8 non-empty labels each). Results contain 8 rows, matching the 8 non-empty vowel intervals. Source labels retained verbatim (`I`, `e`, `i`, `a`; word labels including `i`/`f` positional suffixes in TextGrid). Raw acoustic values preserved without normalization overwrite. No unrelated repository subtree was touched.

## Next backlog priorities
1. Continue authoritative JSUT manual e2e_symbol/phoneme.yaml extraction from BASIC5000_1351+.
2. Download/enumerate Russian Fricatives SWISSUbase public files and map all acoustic variables.
3. Inventory TaL manual short-segment CSVs and preserve full articulatory/acoustic linkage.
4. Isolate L2-ARCTIC human `/annotation` subset only.
5. Extract CCOST human-corrected TextGrid word/syllable/phone/toneme tiers.
6. Resolve provenance for the Bortlik example recordings before any promotion from review to accepted.
