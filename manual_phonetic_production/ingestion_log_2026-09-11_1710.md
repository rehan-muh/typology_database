# Manual phonetic production ingestion log — 2026-09-11 17:10 PT

## Backlog ingestion

Resumed from canonical PAVOQUE endpoint `spike0030`. The authoritative source remains `marytts/pavoque-data/pavoque-angry.yaml`, whose current blob SHA was reverified as `bff79d978f740f6c2b09bc595ade03b6bdf33423`. The upstream documentation identifies the segment layer as manually corrected German-SAMPA labels with silence `_` and utterance-relative cumulative endpoints.

This pass durably committed four complete records, `spike0031`–`spike0034`, in:

- `data/source_faithful/pavoque_angry_utterances_chunk009.csv`
- `data/source_faithful/pavoque_angry_segments_chunk009.csv`

The increment contains **4 utterance rows + 148 manually corrected segment/silence interval rows**: 32 intervals for `spike0031`, 57 for `spike0032`, 30 for `spike0033`, and 29 for `spike0034`. Original German text, expressive style, source utterance start/end, original SAMPA/silence labels, and exact cumulative source endpoints are retained. Interval starts and durations are deterministic derivatives of consecutive cumulative endpoints. No IPA or unobserved context was invented.

### PAVOQUE validation

- all four records are complete upstream records; `spike0035` was verified as the next source record but was not partially materialized
- segment indices are consecutive within each utterance
- endpoints are monotonic and derived durations are nonnegative
- `spike0031`: 3.775-s final annotation vs 3.780-s utterance
- `spike0032`: 5.695 vs 5.700 s
- `spike0033`: 3.535 vs 3.540 s
- `spike0034`: 3.655 vs 3.660 s
- each source-native 0.005-s residual is preserved rather than silently stretching the final interval
- upstream blob SHA remains `bff79d978f740f6c2b09bc595ade03b6bdf33423`

Canonical durable PAVOQUE state is now **9 chunks / 32 utterances / 1,008 intervals**, latest committed record `spike0034`, next verified record `spike0035`. The PAVOQUE manifest now contains 18 chunk-file records. These observations already exist in the validated full PAVOQUE CI parse, so the global distinct-observation count is intentionally unchanged.

## Discovery and source admission

### IFA Spoken Language Corpus — ACCEPTED

A fresh repository/direct-record screen identified the IFA Spoken Language Corpus (`10.5281/zenodo.14904090`) as an unambiguous core source. It is **primary, non-singing Dutch speech explicitly described as hand-segmented**, with approximately **1,000 hours of human labeling** and an explicit reliability study. Eight speakers (4F/4M, ages 15–66) were selected for phonemic segmentation. Public source counts are **19,867 s (5:31 h) segmented speech, 4,492 segmented sentences, 51,782 words, 74,702 syllables, and 187,544 phonemes**.

The public Zenodo deposit provides Praat annotation archives, relational/TSV data, validation labels, the labeling protocol, auxiliary transcriptions/scripts, and audio archives. A complete 35-record top-level file manifest was written to `data/source_faithful/ifa_spoken_language_file_manifest.csv`, preserving filenames, published sizes and MD5 checksums. Large audio is reference/checksum-only by policy; compact annotations, validation/protocol material and database exports are marked HIGH backlog.

A direct attempt to retrieve `Labels-chunks.zip` through the execution download path failed in this run. Therefore **zero IFA observation rows are claimed as ingested**. `data/source_faithful/ifa_spoken_language_ingestion_status.json` explicitly records processed-files = empty and the exact pending archives; the source is accepted but not yet counted as materially ingested.

## Discovery follow-through on existing HIGH review

Mitra & Dutta's Bengali–English production source (`10.17605/OSF.IO/DSB2X`) was reverified through the paper: materials, data files and analysis code are openly deposited on OSF. The target remains HIGH because it has manually segmented target vowels and five F1/F2 trajectory samples per token. Direct OSF file-tree retrieval could not be resolved through the available web execution path in this run, so the candidate remains REVIEW and no paper-level measurements were fabricated or promoted.

## Reconciled state

- **148 tracked sources**
- **46 accepted**
- **25 excluded**
- **77 review**
- **44 materially ingested sources**
- **1,361,571 distinct promoted/parsed observations across 86 logical tables**
- **1,013 inventory-file records** (976 prior + 2 new PAVOQUE files + 35 IFA deposit-file records)
- **1,148 variable-dictionary records**
- PAVOQUE durable mirror: **32 utterances / 1,008 intervals**
- IFA: accepted, 35 deposit files inventoried, observation promotion pending

## Next backlog priorities

1. Continue PAVOQUE at verified `spike0035`, committing only complete records.
2. Retrieve and parse IFA `Labels-chunks.zip`, `Labels-validation.zip`, `LabelProtocol.zip`, then `Labels-sentences.zip` and database/TSV exports; enumerate every contained table/TextGrid and build variable dictionary entries before harmonization.
3. Resolve OSF `DSB2X` file-tree access and ingest the Bengali–English token table while preserving all five F1/F2 trajectory timepoints and all original variables.
4. Resolve licensed L2-ARCTIC acquisition and isolate only the 3,599 manually annotated utterances from forced-aligned-only material.
5. Continue observation-level ingestion of accepted Chenghai Southern Min and manually labelled rt-MRI sources.

All repository writes in this pass were confined to `manual_phonetic_production/`.
