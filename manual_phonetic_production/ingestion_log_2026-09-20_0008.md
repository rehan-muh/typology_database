# Ingestion pass — 2026-09-20 00:08 PT

## New accepted source: PAVOQUE

Accepted `pavoque` (PAVOQUE Corpus of Expressive Speech), a primary non-singing German expressive-speech corpus. Upstream documentation explicitly describes the phonetic segments as manually corrected and supplies segment labels plus endpoint times. The speaker is a professional actor and baritone opera singer, but the corpus material represented here is spoken expressive prompts, not singing; singing/music remains excluded.

Authoritative repository: https://github.com/marytts/pavoque-data
License: CC BY-NC-SA 4.0.

### File inventory established

Six annotation YAML objects are publicly visible and retained by authoritative source reference rather than copied wholesale:

- `pavoque-angry.yaml` — blob `bff79d978f740f6c2b09bc595ade03b6bdf33423`, 641141 bytes
- `pavoque-happy.yaml` — blob `9ed4b66625a6c6df746730b6bf45ae35b9baaa38`, 611136 bytes
- `pavoque-neutral.yaml` — blob `29deb5aa1b75b1aaa98d51896f9d849e12bc7bc0`, 4968933 bytes
- `pavoque-outtakes.yaml` — blob `a72da519c13068f8c05b6fd038cd8ecac540b80c`, 286310 bytes
- `pavoque-poker.yaml` — blob `20a8bdc3cee75f3e19517dfeeda1a2230cb033c9`, 630189 bytes
- `pavoque-sad.yaml` — blob `199a4dde1e3a50576a1a691664473aa9f35f41ed`, 604376 bytes

Audio is distributed separately through releases / build tasks and was not committed to this repository.

### Observation ingestion

Parsed the first two `angry` utterances (`spike0001`, `spike0002`) into a source-faithful segment table: 2 utterances and 41 segment intervals. Original labels, source text, style, utterance start/end, and all supplied segment endpoint values are preserved. Segment starts are deterministically derived from the immediately preceding supplied endpoint (0 for the first segment); no acoustic landmarks, IPA, or contexts were invented.

Next cursor: `pavoque-angry.yaml` / `spike0003`. Backlog is all remaining utterances in angry plus complete happy, neutral, outtakes, poker, and sad YAMLs. This source should be ingested exhaustively because it provides dense manual segment timing rather than paper-level summaries.

## Discovery screening

Fresh searches across Zenodo/GitHub/phonetics deposits also surfaced: (1) a French OSCE dialogue dataset with manually corrected transcripts, but its human layer is orthographic/diarization rather than demonstrated phonetic annotation and files are restricted, so not admitted; (2) ESMUC choir material with manually corrected F0/note annotations, excluded because it is singing/music; (3) Brain Treebank manual word onsets, excluded because underlying audio is third-party Hollywood media; (4) GRAIN, excluded under third-party radio-interview lineage and predominantly automatic speech layers.

## Integrity/provenance

All writes in this pass are under `manual_phonetic_production/`. No unrelated repository content was modified. Source IDs and future speaker/utterance IDs must remain namespaced (`pavoque::...`). Original SAMPA-like labels are preserved and are not silently converted to IPA. Automatic-only material was not admitted.
