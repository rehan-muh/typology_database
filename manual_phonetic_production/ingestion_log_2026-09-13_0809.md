# Manually Annotated Phonetic Production Database ingestion log — 2026-09-13 08:09 PDT

## Backlog ingestion

### PAVOQUE (`pavoque`, angry subset)

Advanced the durable source-faithful mirror from chunk043 to chunk044 by reading the authoritative `marytts/pavoque-data` `pavoque-angry.yaml` at blob `bff79d978f740f6c2b09bc595ade03b6bdf33423`.

Literal upstream records ingested, without prompt-number reordering:

- `m0107` — 78 intervals
- `m0109` — 45 intervals
- `m0110` — 33 intervals
- `m0111` — 44 intervals

Total this pass: **4 utterances, 200 intervals**.

Durable angry-subset totals after this pass: **158 utterances, 4,917 intervals, 44 chunks**.

Files created:

- `data/source_faithful/pavoque_angry_utterances_chunk044.csv`
- `data/source_faithful/pavoque_angry_segments_chunk044.csv`
- `data/source_faithful/pavoque_angry_chunk044_manifest.json`

Status updated:

- `data/source_faithful/pavoque_angry_ingestion_status.json`

Validation/provenance checks:

- namespaced utterance IDs use `pavoque::source_id`;
- source text, style, utterance bounds, German-SAMPA/silence labels, and cumulative segment endpoints preserved;
- interval starts derive only from the previous source endpoint (0 for the first interval);
- interval durations derive only as endpoint minus start;
- no IPA or phonological context invented;
- each utterance retains a 0.005-s source-native terminal residual;
- upstream YAML is known not to be prompt-number ordered, therefore `m0108` is **not** classified as absent merely because it does not occur in this local sequence;
- next verified literal record is `m0112`;
- PAVOQUE's scientific observations already exist in the validated full lineage, so these durable chunks are excluded from the global distinct-observation increment.

## Fresh discovery

### Parker — Data for studying sonority / Sound level protrusions as physical correlates of sonority

Added to HIGH review as `parker_sonority_2008_2019`.

Evidence supporting review:

- primary non-singing English and Spanish speech production;
- recordings made in 2005;
- direct linkage to a Journal of Phonetics article;
- repository states recordings were manually segmented and analyzed in Praat 4.3.17;
- 44.1-kHz source recordings and recording equipment are documented;
- public Mendeley Data record is CC BY 4.0.

Reason not promoted to core yet: the rendered repository page describes the public files as raw WAV data and does not enumerate annotation/measurement files. The bundle/API must be enumerated before asserting that manual boundaries or derived observations are publicly available. If only WAV is deposited, retain the source/lineage reference but do not create inferred segmentation or measurement observations.

Registry file created:

- `data/registry/review_increment_2026-09-13_0809.csv`

## Reconciliation

Global status after this pass:

- tracked sources: **176**
- accepted: **47**
- excluded: **29**
- review: **100**
- materially ingested: **45**
- distinct promoted scientific observations: **1,361,972** (unchanged; PAVOQUE double-count guard)
- logical tables: **90**
- inventory-file records: **1,109**
- variable-dictionary records: **1,197**

## Next backlog priorities

1. Continue PAVOQUE in literal YAML order beginning with `m0112`.
2. Resume CCOST metadata exactly at source row 196/488 and continue source-faithful 37-variable ingestion.
3. Extract CCOST word/syllable/phone/toneme TextGrid annotations without mirroring raw audio when an annotation-only route is available.
4. Enumerate Parker/Mendeley bundle contents; promote only if manual segmentation or derived measurement artifacts are directly recoverable.
5. Continue HIGH-review repository resolution for open trajectory, voice-quality, articulatory, clinical, sociophonetic, and L2 production sources.

All repository writes in this pass were confined to `manual_phonetic_production/`.
