# Ingestion pass — 2026-09-12 15:12 PT

## Backlog ingestion
- Advanced PAVOQUE angry source-faithful materialization from 110 utterances / 3,254 manually corrected intervals through `m0062` to 114 utterances / 3,417 intervals through `m0066`.
- Added `pavoque_angry_utterances_chunk032.csv` for literal upstream records `m0063`, `m0064`, `m0065`, `m0066`.
- Added `pavoque_angry_segments_chunk032.json` containing 163 source-faithful segment observations with original German-SAMPA/silence labels and original cumulative end points.
- Added `pavoque_angry_file_manifest_chunk032.csv` with local blob SHAs and immutable upstream blob lineage (`bff79d978f740f6c2b09bc595ade03b6bdf33423`).
- Reconciled `pavoque_angry_ingestion_status.json`, which had lagged the already committed chunk031, and advanced it through chunk032.
- Verified terminal residuals: `m0063`, `m0064`, `m0065`, `m0066` each end 0.005 s before the nominal utterance duration; preserved without timing repair.
- Verified next literal upstream record: `m0067`.

## Discovery / screening
- Searched current public repository and journal-indexed material for manually segmented or manually validated production datasets across acoustic, articulatory, L2, clinical, and general phonetics.
- Re-screened the Yaru Hu (2025) Tibetan-Chinese-English diphthong dataset; it is already in HIGH review and therefore was not duplicated. Its public Mendeley record reports F1/F2 trajectories and peak values from manually annotated/validated primary production, CC BY 4.0; direct file resolution remains the backlog item.
- Screened Hiemstra & Sadakata (2024), *Sentences used in the speech-to-song illusion: Comparisons of acoustic vowel space*. The study manually segmented recordings in Praat, but the analyzed speech derives from audiobooks and pre-existing UvA/Tierney stimulus collections. Added an explicit `SECONDARY_MEDIA` exclusion increment rather than admitting it.
- Other surfaced items such as MRPL2 and RescueSpeech were already tracked; no duplicate registry entries were created.

## Validation
- Namespaced utterance IDs retained (`pavoque::m0063` etc.).
- Original text/style/start/end retained at utterance level.
- Original segment labels and cumulative end points retained exactly.
- No IPA normalization was invented.
- Derived starts/durations are defined only as deterministic differences between successive source endpoints.
- Global scientific-row count remains 1,361,571 because this PAVOQUE lineage is already represented in the validated full-corpus parse; chunk materialization is not double-counted.
- All writes were confined to `manual_phonetic_production/`.

## Updated state
- total sources: 160
- accepted: 47
- excluded: 26
- review: 87
- materially ingested: 44
- distinct promoted/parsed observations: 1,361,571
- logical tables: 86
- inventory-file records: 1,082
- variable-dictionary records: 1,156

## Next backlog priorities
1. Continue literal PAVOQUE order from `m0067`.
2. Resolve and parse the already-tracked Hu (2025) Mendeley TXT files and participant DOCX if direct bitstream URLs become available.
3. Continue attempts to materialize L2-ARCTIC manual `/annotation` TextGrids and the manually checked spontaneous subset while excluding the automatic `/textgrid` layer.
4. Resolve AUSpeech patient/manual TextGrids and SAIT-EMA manual error/annotation layers without mirroring large raw media archives.
5. Continue OMPAL detailed per-rater JSON extraction and Consonant Challenge `handsegm.91.mlf.txt` retrieval.
