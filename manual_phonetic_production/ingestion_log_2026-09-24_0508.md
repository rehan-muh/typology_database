# Ingestion log — 2026-09-24 05:08 PT

## Discovery
Aggressive web/repository screening rechecked MRPL2, RescueSpeech, HAMATAC, L2-ARCTIC, IFCASL, ROG, CARInA, KEC and other manual/corrected speech resources. New core admission: **PAVOQUE Corpus of Expressive Speech** (`pavoque_2014`). It is primary German studio speech, non-singing despite the speaker also being professionally trained as an opera singer. Upstream documentation explicitly states that style YAML utterances optionally contain **manually corrected phonetic segments**, with SAMPA-based labels and segment endpoints.

## Repository/object inventory
Recursively enumerated the public upstream Git tree. Captured immutable Git blob SHA and byte size for `pavoque-angry.yaml`, `pavoque-happy.yaml`, `pavoque-neutral.yaml`, `pavoque-outtakes.yaml`, `pavoque-poker.yaml`, `pavoque-sad.yaml`, `README.md`, and `LICENSE.md`. The neutral YAML alone is 4,968,933 bytes. Outtakes are held at review scope pending production-validity screening; angry/happy/neutral/poker/sad are eligible spoken style layers.

## Backlog ingestion performed
Opened `pavoque-happy.yaml` and converted actual source observations rather than stopping at registry metadata. Ingested 3 utterance records (`poppy0001`–`poppy0003`) and all 57 manually corrected segment records exposed for them. Preserved verbatim German text, style, source utterance IDs, original SAMPA-based segment labels, raw utterance starts/ends and raw segment end times. Derived segment starts only by ordered previous-end logic (first segment = 0) and stored derived recording-absolute times separately. No IPA normalization was invented.

## Variable dictionary
Added 10 source/derived variable records: `prompt`, `text`, `style`, utterance `start`, utterance `end`, segment `lab`, segment `end`, derived relative segment start, derived absolute segment start, and derived absolute segment end. Units/time reference and manual/derived provenance are explicit.

## QC / lineage
PAVOQUE style files are one underlying recording lineage, not independent datasets. Large FLAC audio is not copied into this Git repository. Git blob identities are retained for annotation/documentation objects. Segment timing monotonicity was checked for the ingested three utterances. The final segment endpoints (1.595, 1.295, 2.435 s) remain within their corresponding utterance durations (1.6, 1.3, 2.44 s).

## Counts changed
+1 source (accepted), +1 materially ingested source, +60 parsed observation rows, +2 logical tables, +8 manifest records, +10 variable-dictionary records. Canonical totals: 275 sources; 83 accepted / 36 excluded / 156 review; 56 ingested sources; 1,365,146 parsed rows across 247 logical tables; 1,373 manifest records; 1,467 variable-dictionary records.

## Next priorities
1. Continue `pavoque-happy.yaml` to exhaustion, then parse angry/neutral/poker/sad YAMLs completely.
2. Isolate Spontal-N's manually corrected gold phone subset.
3. Materialize/parse L2-ARCTIC manual TextGrids if access route becomes available.
4. Resolve IFCASL public object access and isolate >50% manually corrected phone/word material.
5. Continue CORPRES and BAS manual-partition access work (VM2/PD1/SC10), without admitting automatic layers.
