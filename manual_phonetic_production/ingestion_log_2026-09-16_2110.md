# Ingestion pass — 2026-09-16 21:10 PT

## Material ingestion

### PAVOQUE Corpus of Expressive Speech (`pavoque_2009`) — ACCEPTED / HIGH

Directly inspected the public GitHub repository and its YAML annotations. This is primary German non-singing speech from one male native speaker. The speaker is also a professional actor and trained baritone opera singer, but the corpus itself is explicitly expressive **speech**, not singing. Repository documentation states that optional phone segments are **manually corrected**. Audio is 44.1-kHz, 16-bit mono FLAC. License: CC BY-NC-SA 4.0.

Repository annotation inventory: `pavoque-angry.yaml` (641,141 B), `pavoque-happy.yaml` (611,136 B), `pavoque-neutral.yaml` (4,968,933 B), `pavoque-outtakes.yaml` (286,310 B), `pavoque-poker.yaml` (630,189 B), `pavoque-sad.yaml` (604,376 B). Exact Git blob SHAs were captured in the source-faithful manifest.

Parsed and ingested the first two `angry` utterances (`spike0001`, `spike0002`) as a concrete continuation checkpoint: 2 utterances and 41 manually corrected segment intervals. Original German orthography, style, utterance absolute boundaries, source SAMPA labels, and source segment end-times were retained. Segment starts were deterministically derived from the previous segment end and stored separately; absolute segment boundaries were also derived without overwriting source values. No IPA was invented.

Added a 10-row variable dictionary covering every source field encountered plus explicit derived-time fields. Remaining YAML content is pending; next run should continue `pavoque-angry.yaml` from `spike0003`, then process happy/neutral/poker/sad. `outtakes` should remain separately flagged until its suitability for core analysis is reviewed.

## Discovery

Fresh repository/web search also surfaced AphasiaBD (Zenodo): 38 Bengali post-stroke aphasia patients, 335 primary clips across conversation/repetition/question-answering/reading, manually segmented at sentence level. This is potentially admissible clinical production but should remain REVIEW until the downloadable file inventory and annotation granularity are inspected; sentence-level manual segmentation alone may be too coarse for many phonetic analyses.

Re-screened CORPRES: 60 hours Russian speech, 40% manually segmented and fully annotated across six phonetic/prosodic levels. High-value historical backlog candidate, but public observation-level access still needs resolution.

## Validation / exclusions

- PAVOQUE lineage is primary; no duplicate underlying database reuse identified.
- Singing corpora encountered in search were excluded from consideration.
- LJ Speech IPA alignments were not admitted: MFA-generated alignment without qualifying exhaustive human verification.
- VoxAngeles remains admissible only for its audited/manually corrected subset; unaudited alignments remain excluded.
- No unrelated repository paths were modified.

## Next backlog priorities

1. Continue PAVOQUE `angry` from `spike0003`; then ingest happy/neutral/poker/sad YAML fully.
2. Resume JSUT BASIC5000 continuation checkpoint.
3. Russian Fricatives observation-level extraction.
4. TaL manual segments.
5. L2-ARCTIC human `/annotation` subset.
6. CCOST corrected TextGrid tiers.
7. Inspect AphasiaBD Zenodo file inventory and decide accepted vs review/excluded at annotation granularity.
