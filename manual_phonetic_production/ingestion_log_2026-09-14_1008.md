# Manually Annotated Phonetic Production Database ingestion log — 2026-09-14 10:08 PT

## Backlog ingestion

### JSUT-label BASIC5000
- Authoritative source: `sarulab-speech/jsut-label/e2e_symbol/phoneme.yaml` (Git blob `ae8933677c082c22238a895012b6b6dcba1f6625`).
- Ingested source rows 201–240 (`BASIC5000_0201`–`BASIC5000_0240`).
- Added immutable/source-faithful YAML slice: `data/source_faithful/jsut_label_basic5000_manual_phoneme_rows_0201_0240.yaml`.
- Added linked harmonized annotation index: `data/harmonized/jsut_label_basic5000_manual_annotation_index_0201_0240.csv`.
- Preserved manual phonetic/prosodic symbol strings exactly; no IPA conversion or reinterpretation introduced.
- Underlying recording lineage remains `JSUT::BASIC5000`; recordings are not recounted.
- Julius-estimated timing remains excluded because upstream documentation identifies it as automatic rather than manual.
- Validation: 40 source-faithful rows, 40 harmonized rows, contiguous IDs 0201–0240, namespaced IDs, zero automatic timing rows admitted.
- New checkpoint: `BASIC5000_0241` / source row 241.

## Fresh discovery

### PAVOQUE Corpus of Expressive Speech — ACCEPTED
- Repository: `marytts/pavoque-data`.
- Primary non-singing German expressive speech from one male native German professional actor; being an opera singer by training is speaker biography, not the task: the corpus consists of acted spoken styles.
- Audio specification documented upstream: FLAC, 44.1 kHz, 16 bit, mono.
- Annotation provenance is explicit: YAML files contain optionally **manually corrected** phonetic segments; segment labels use German SAMPA, and each segment end time is in seconds relative to utterance start.
- Direct inspection of `pavoque-angry.yaml` confirms source objects containing `prompt`, `text`, `style`, `start`, `end`, and repeated `{lab, end}` segment objects.
- License: CC BY-NC-SA 4.0.
- Inventoried README plus six public annotation objects with exact Git blob SHAs and byte sizes: angry, happy, neutral, outtakes, poker, sad.
- No derived IPA was invented. Full parse should retain source SAMPA plus defensible normalized IPA only after a documented mapping layer is added.

## Counts after reconciliation
- Sources: 204 total; 50 accepted; 31 excluded; 123 review.
- Materially ingested sources: 48.
- Promoted/source-faithful observation rows: 1,362,246.
- Logical tables: 133.
- File-inventory records: 1,194.
- Variable-dictionary records: 1,229.

## Next backlog priorities
1. Continue JSUT BASIC5000 at row 241 while preserving the automatic-timing exclusion.
2. Fully parse the six PAVOQUE YAML annotation files into source-faithful utterance and segment objects, then harmonize utterances/conditions and phonetic intervals with both relative and absolute interval times.
3. Enumerate PAVOQUE release audio assets without committing bulk FLAC; retain authoritative release URLs/checksums and link utterance boundaries to their source recordings.
4. Add PAVOQUE variable-dictionary entries for every YAML field and segment subfield before normalization.
5. Continue high-priority annotation-file retrieval for IFA, IFCASL, L2-ARCTIC manual TextGrids, and other accepted/review sources where file-level annotation provenance is already established.

## Integrity/provenance checks
- All IDs added in harmonized JSUT rows use the `source_id::source_utterance_id` namespace.
- No unrelated repository paths were modified.
- No audio binaries were committed.
- PAVOQUE was accepted only after direct repository documentation and annotation-file inspection established primary production and manually corrected phonetic segmentation.
