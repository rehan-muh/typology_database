# Manual phonetic production ingestion log — 2026-09-12 05:09 PDT

## Backlog ingestion

- Advanced PAVOQUE angry durable source-faithful materialization from `m0026` through `m0029`.
- Added `data/source_faithful/pavoque_angry_utterances_chunk022.csv`: 3 utterances (`m0027`–`m0029`) in literal upstream order.
- Added `data/source_faithful/pavoque_angry_segments_chunk022.csv`: 84 manually corrected German-SAMPA/silence intervals.
- Added `data/source_faithful/pavoque_angry_file_manifest_chunk022.csv` with exact Git blob SHAs and immutable upstream blob lineage.
- Updated `data/source_faithful/pavoque_angry_ingestion_status.json` to 22 durable chunks, 80 utterances, and 2,328 intervals. Next verified upstream record: `m0030`.

## PAVOQUE validation

- Original utterance text, style, source times, labels and cumulative segment endpoints retained.
- Namespaced IDs use `pavoque::source_id` conventions.
- Interval starts and durations are deterministic derivatives; originals were not overwritten.
- No IPA values were invented.
- Segment sequences are monotonic and positive-duration within each newly ingested utterance.
- `m0027`, `m0028`, and `m0029` each preserve a 0.005 s source-native final timing residual rather than silently extending the final interval.
- Global distinct scientific-observation count remains unchanged because the PAVOQUE lineage is already represented in the validated full-corpus parse.

## Discovery and source registration

### OMPAL — accepted

- Source: `phantomhsieh/OMPAL-corpus`; publication DOI `10.21437/Interspeech.2025-983`.
- Primary non-singing Mandarin production corpus with 1,850 WAV utterances from 49 speakers: 82 native-speaker reference utterances and 1,768 French-L1 learner utterances.
- Human annotation is explicit and directly relevant to the database: expert Mandarin teachers independently judge consonant correctness, vowel correctness, and tone correctness at word level, plus accuracy, fluency, and prosody at sentence level.
- Repository license is CC BY 4.0.
- Registered as accepted in `data/registry/increments/2026-09-12_0509_accepted.csv`.
- Added `data/source_faithful/ompal_2025_file_manifest.csv` with 14 authoritative records covering the three master JSON files, ten train/test derivative JSON files, and the upstream audio tree. The derivative train/test files are explicitly flagged for lineage checking so their ratings are not double-counted.
- Added `data/source_faithful/ompal_2025_variable_dictionary.csv` with source-faithful mappings for `accuracy`, `fluency`, `prosody`, `text`, `words`, `phoneme_consonant`, `phoneme_vowel`, and `tone`.
- `non-native_scores-detail.json` is approximately 10 MB and exposes per-rater arrays. The connector can inspect it but truncates one-shot responses, so full observation-level parsing was not falsely claimed in this pass. It is now a top backlog target.

## Counts after reconciliation

- tracked sources: 154
- accepted: 47
- excluded: 25
- review: 82
- materially ingested: 44
- distinct promoted/parsed observations: 1,361,571
- logical tables: 86
- inventory-file records: 1,053
- variable-dictionary records: 1,156

## Next backlog priorities

1. Continue PAVOQUE from `m0030` in literal upstream order.
2. Parse OMPAL `non-native_scores-detail.json` into namespaced utterance, word-target, per-rater categorical-production, and sentence-level prosody/judgment tables; keep aggregate and train/test files as derivative lineage rather than duplicate observations.
3. Retrieve and parse Consonant Challenge `handsegm.91.mlf.txt` only; exclude its automatic segmentation files.
4. IFA hand-segmented label archives.
5. Hu Tibetan trilingual English-vowel F1/F2 trajectories.
6. Bengali–English five-timepoint F1/F2 trajectory deposit.
7. Manually corrected L2-ARCTIC subset only.
8. Chenghai Southern Min production data.
9. Manually labelled rt-MRI material.

All repository writes in this pass were confined to `manual_phonetic_production/`.
