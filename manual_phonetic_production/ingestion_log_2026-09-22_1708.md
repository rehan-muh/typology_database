# Ingestion pass — 2026-09-22 17:08 PT

## Backlog ingestion

Continued the accepted PAVOQUE primary-production backlog from the authoritative `marytts/pavoque-data` repository. Added four source-faithful angry-style utterance records: `spike0018`, `spike0019`, `spike0021`, and `spike0022`. Preserved original German text, source timing, derived duration separately, style, manual-correction provenance, source filename, and current upstream blob SHA (`bff79d978f740f6c2b09bc595ade03b6bdf33423`). The source skips `spike0020`; no synthetic placeholder was created. No IPA was invented.

The upstream YAML also contains manually corrected segment sequences for these utterances. They remain pending exhaustive row conversion; cumulative segment endpoints must be preserved verbatim and converted to interval starts/durations only as separate deterministic fields.

## Discovery

Ran fresh repository-focused searches for manually corrected TextGrid/segmentation, VOT, and phonetic corpora. Re-screened MRPL2 (strong manual phonetic mispronunciation annotations but restricted files), IFCASL (>50% manually corrected phone/word segmentation; access/observation extraction remains backlog), HAMATAC (primary L2 German map-task recordings with transcription/disfluency annotation, but repository access is restricted), COVYT (manual segmentation over third-party social-media recordings; excluded by lineage rule), ROG (new annotations over pre-existing GOS/SST recordings; secondary lineage), and REYD Yiddish TTS (human-checked segmentation but TTS-oriented corpus requires provenance/access audit). No weak candidate was admitted to core.

## Validation / next priorities

- Namespaced IDs used for all newly added observations.
- No raw audio committed.
- No inaccessible values inferred.
- No third-party-media source admitted.
- Next PAVOQUE work: convert all deposited segments for spike0015–0019 and continue contiguous source enumeration from spike0021 onward, preserving the source's missing spike0020 identifier.
- Continue IFCASL/HAMATAC access-resolution and primary-lineage audit.
