# Manually Annotated Phonetic Production Database — ingestion log 2026-09-13 05:13 PT

## Backlog ingestion

### CCOST (`ccost_2026`)
- Continued from the exact prior checkpoint at source metadata row 179.
- Parsed and committed authoritative `CCOST_metadata.csv` rows **179–195** (17 recording rows), preserving all 37 original columns and source values without normalization or silent dropping.
- New source-faithful file: `data/source_faithful/ccost_2026/CCOST_metadata_chunk006_rows179_195.csv`.
- Added 17 collision-safe recording projections in `data/warehouse/ccost_2026_recordings_chunk006.csv`.
- Added one new speaker projection (`S24`) in `data/warehouse/ccost_2026_speakers_chunk006.csv`; existing `S23` was reused rather than duplicated.
- CCOST metadata checkpoint is now **195/488**; next row is **196**.
- CCOST source-faithful rows are now **401** = 195 metadata + 206 complete word-list stimulus rows.
- Updated the CCOST file manifest, which had a stale metadata parse label (`88/488`), to the actual `195/488` checkpoint.
- Revalidated the authoritative repository metadata: 49 Standard Thai speakers, approximately 7 h interview + 5 h controlled speech, manually corrected ASR/MAUS annotations at orthographic/word/syllable/phone/toneme levels, CC-BY-NC-SA 4.0. The 6.2 GB audio/TextGrid archive remains reference-only; raw audio is not mirrored.
- `sentences.txt` remained unavailable through the preview endpoint during this pass; no sentence text was guessed or reconstructed.

## Discovery

### HIGH review — Acted clear speech corpus (Mayo 2013)
- Repository: University of Edinburgh DataShare, DOI `10.7488/ds/138`.
- Primary non-singing British-English production by one male native speaker producing 25 TIMIT sentences under five speaking/listener conditions.
- Repository description explicitly states **automatic and hand-corrected phone-level transcription**.
- Public deposit exposes WAV, LAB, TextGrid, prompt, README, and segmentation-criteria resources.
- Kept in review rather than core pending exact file-level licence/redistribution interpretation and isolation of hand-corrected versus automatic-only annotation products.
- If admissible, next action is to inventory/checksum every annotation file, ingest all corrected phone intervals and labels, preserve prompt/condition linkage, and retain source audio by reference rather than duplicating it unnecessarily.

## Validation / integrity
- All new IDs are namespaced as `ccost_2026::...`.
- No existing CCOST speaker or recording IDs were overwritten.
- No IPA or phonological context was inferred from orthography.
- No raw audio was committed.
- No files outside `manual_phonetic_production/` were changed.
- Global parsed-row count increased by exactly 17 to **1,361,972**.
- Registry totals after discovery: **173 tracked / 47 accepted / 29 excluded / 97 review / 45 materially ingested**.

## Next backlog priorities
1. Continue CCOST metadata from row 196, preserving the exact source checkpoint.
2. Obtain annotation-only access to `C-COST.zip` and ingest manually corrected word/syllable/phone/toneme TextGrid tiers without mirroring WAV audio.
3. Resolve the Acted Clear Speech deposit licence and, if compatible, ingest its hand-corrected phone TextGrids/labels.
4. Continue promotion work on existing HIGH-review trajectory-rich sources (SwehVd, Carignan nasalance, Belz et al. acoustic/EMA) once repository file trees can be enumerated reliably.
