# Ingestion pass — 2026-09-14 20:09 PT

## Backlog ingestion

- Advanced `jsut_label_basic5000_manual` from durable coverage through `BASIC5000_0420` to `BASIC5000_0460`.
- Re-read rows 421–460 directly from authoritative `sarulab-speech/jsut-label:e2e_symbol/phoneme.yaml`, Git blob `ae8933677c082c22238a895012b6b6dcba1f6625`.
- Added source-faithful YAML `data/source_faithful/jsut_label_basic5000_manual_phoneme_rows_0421_0460.yaml` with 40 verbatim values.
- Added harmonized index `data/harmonized/jsut_label_basic5000_manual_annotation_index_0421_0460.csv` with 40 namespaced utterance IDs and explicit `automatic_not_ingested` timing status.
- Reconciled a stale per-source checkpoint: `jsut_label_basic5000_ingestion_status.json` still claimed coverage only through 380 even though durable 381–420 source-faithful/harmonized files existed. Checkpoint now correctly records contiguous coverage through 460.
- No underlying JSUT recordings were counted as new recordings; only the new manual annotation layer is represented.

## Discovery

Fresh searches targeted Zenodo and OSF for manual phonetic annotation, manual segmentation, corrected TextGrid, speech-production and corpus deposits. The strongest returned speech hit, COVYT, uses public social-media/YouTube recordings and therefore fails the standing exclusion against third-party media collections. Singing/music results were excluded. Other returned records were non-speech annotation resources or automatic/synthetic work. No candidate was promoted without sufficient evidence.

## Validation

- New JSUT source rows: 40.
- New JSUT harmonized IDs: 40, namespaced `jsut_label_basic5000_manual::BASIC5000_####`.
- Source rows are contiguous through 460.
- Automatic Julius timing rows admitted: 0.
- Authoritative upstream blob unchanged from prior passes.
- Global parsed/source-faithful observation count advanced from 1,362,472 to 1,362,512.
- Writes confined to `manual_phonetic_production/`.

## Next priorities

1. Resume JSUT at `BASIC5000_0461`.
2. Continue exhaustive PAVOQUE YAML parsing across styles.
3. Retry DanPASS TextGrid archive retrieval and enumerate all members/tier structures/checksums.
4. Work HIGH-review sources with accessible annotation files before adding additional paper-only registry entries.
