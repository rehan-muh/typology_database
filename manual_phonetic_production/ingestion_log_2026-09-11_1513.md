# Manual phonetic production ingestion log — 2026-09-11 15:13 PT

## Backlog ingestion

Resumed from the canonical durable PAVOQUE endpoint `spike0025`. The authoritative source is `marytts/pavoque-data/pavoque-angry.yaml`, pinned to immutable Git blob `bff79d978f740f6c2b09bc595ade03b6bdf33423`. The upstream README explicitly describes the segment layer as manually corrected, with German-SAMPA labels and utterance-relative end times, and licenses the corpus CC BY-NC-SA 4.0.

This pass durably committed the next three complete source records in actual source order:

- `spike0026` — `Du machst dich lächerlich.` — 19 manually corrected segment/silence intervals
- `spike0028` — `Wann wirst du endlich mal erwachsen?` — 31 intervals
- `spike0029` — `Das Leben ist ein Krieg, entweder du gewinnst oder du verlierst.` — 52 intervals

`spike0027` is not present between `spike0026` and `spike0028` in this source ordering. This is recorded as a local ordering fact only; no global corpus absence is inferred without full-corpus verification. Direct inspection after `spike0029` establishes `spike0030` as the next source record.

New durable materialization: **3 utterance rows + 102 interval rows** in:

- `data/source_faithful/pavoque_angry_utterances_chunk007.csv`
- `data/source_faithful/pavoque_angry_segments_chunk007.csv`

Original German text, expressive-style label, source utterance start/end, German-SAMPA/silence labels, and source-native cumulative interval endpoints are retained. Interval starts and durations are deterministic derivatives from consecutive endpoints. No IPA or unobserved context was invented.

### Validation

- `spike0026`: 19 intervals; final endpoint 3.055 s vs 3.060 s utterance duration
- `spike0028`: 31 intervals; final endpoint 3.655 s vs 3.660 s utterance duration
- `spike0029`: 52 intervals; final endpoint 5.335 s vs 5.340 s utterance duration

The source's recurring 5-ms record-end residual is retained exactly rather than extending the final silence interval. Segment indices are consecutive within utterance, endpoints are monotonic, all derived durations are nonnegative, and all 102 interval rows resolve to one of the three namespaced utterance IDs.

### Canonical PAVOQUE state

- durable source-faithful chunks: **7**
- durable utterances: **27**
- durable manually corrected intervals: **796**
- latest committed source record: **`spike0029`**
- next verified source record: **`spike0030`**
- immutable upstream source blob: `bff79d978f740f6c2b09bc595ade03b6bdf33423`

This pass added `data/source_faithful/pavoque_angry_ingestion_status.json` and `data/source_faithful/pavoque_angry_file_manifest.csv`. The manifest explicitly inventories all 14 durable chunk files (utterance + segment files for chunks 001–007) with repository blob SHA, byte size, upstream source lineage, source SHA, row scope, and license. This closes the prior gap in which canonical PAVOQUE progress existed mainly in hourly logs.

The validated full PAVOQUE CI parse already represents these scientific observations, so global `parsed_rows_total` remains **1,361,571**. Durable materialization is provenance/persistence work and is not double-counted as new scientific observations.

## Discovery pass

A fresh repository/web discovery sweep targeted Zenodo and OSF records mentioning manually corrected or manually segmented speech-production annotations, TextGrids, and ultrasound/phonetic annotation.

### French OSCE Dialogue Dataset — screened, not admitted

A 2026 Zenodo resource contains primary recorded French clinical-training dialogues and 26 manually corrected transcripts, but the public record establishes correction at the transcript/diarization level rather than a manual phonetic interval, landmark, or production-measurement layer. It therefore does not meet the core inclusion criterion for this database as currently documented. Files are also restricted. No registry row was added solely for transcript correction.

### Passau-SFCH — screened, not admitted

The Zenodo deposit contains about 11 hours of German football-coach press-conference speech and manually corrected transcriptions, plus continuous humor/sentiment annotations. It is third-party/media-derived speech rather than primary recordings collected for the study and therefore fails the primary-recording criterion even before considering the lack of a manual phonetic layer. No registry row was added.

Other search hits were non-speech/manual-correction datasets and were discarded as irrelevant. No new candidate was added merely to inflate discovery counts.

## Reconciled state

- **145 tracked sources**
- **45 accepted**
- **25 excluded**
- **75 review**
- **44 materially ingested sources**
- **1,361,571 distinct promoted/parsed observations across 86 logical tables**
- **974 inventory-file records**
- **1,148 variable-dictionary records**
- PAVOQUE durable source-faithful mirror: **27 utterances / 796 intervals**

## Next backlog priorities

1. Continue PAVOQUE from verified next record `spike0030` and persist complete manually corrected intervals in chunk008.
2. Enumerate the AUSpeech ScienceDB hierarchy and isolate/checksum the manually verified normal-speaker TextGrids and manually annotated dysarthric-speaker files before core admission.
3. Return to accepted Chenghai Southern Min TextGrid/XLSX material for token/interval/measurement-level promotion.
4. Retry the accepted real-time MRI archive and promote the 392 manually labelled articulator masks and velopharyngeal-closure judgments when archive bytes are accessible.
5. Resolve licensed L2-ARCTIC acquisition and isolate only the manually corrected subset from the forced-aligned remainder.
