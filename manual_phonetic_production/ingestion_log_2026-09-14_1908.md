# Ingestion pass — 2026-09-14 19:08 PT

## Backlog ingestion

Advanced `jsut_label_basic5000_manual` from `BASIC5000_0380` through `BASIC5000_0420`.

- Authoritative upstream: `sarulab-speech/jsut-label/e2e_symbol/phoneme.yaml`
- Upstream blob SHA: `ae8933677c082c22238a895012b6b6dcba1f6625`
- Rows newly preserved: 40 (`BASIC5000_0381`–`BASIC5000_0420`)
- Source-faithful file: `data/source_faithful/jsut_label_basic5000_phoneme_0381_0420.yaml`
- Harmonized index: `data/harmonized/jsut_label_basic5000_manual_annotation_index_0381_0420.csv`
- Annotation layer: manual phonetic/prosodic symbol sequence
- Timing: automatic Julius estimates remain excluded
- Recording lineage: `JSUT::BASIC5000`
- Next checkpoint: `BASIC5000_0421`

No IPA normalization was invented and no timing values were inferred.

## Discovery

Ran fresh searches across Zenodo, OSF-oriented queries, and GitHub for manually annotated/corrected production corpora and phonetic segmentation. Screened returned candidates against the strict primary-production criteria. The strongest returned speech candidates were already represented in the registry/backlog (MRPL2, RescueSpeech, PAVOQUE), while other hits were secondary resegmentations, text-only annotation, singing/music, or automatic alignment. No candidate was promoted merely to increase source count.

## Validation

- JSUT IDs are contiguous from the previous checkpoint through 0420.
- IDs are namespaced as `jsut_label_basic5000_manual::BASIC5000_XXXX`.
- All 40 harmonized rows point to the authoritative `e2e_symbol/phoneme.yaml` source and `JSUT::BASIC5000` lineage.
- Automatic timing is explicitly marked `automatic_not_ingested`.
- Source strings were preserved verbatim before harmonization.
- No unrelated repository subtree was modified.

## Next backlog priorities

1. JSUT `BASIC5000_0421+` in bounded authoritative slices.
2. Exhaustive PAVOQUE YAML parsing across remaining styles/utterances.
3. DanPASS TextGrid archive retrieval/enumeration and tier-level ingestion.
4. File-level verification for HIGH-review corpora with manual correction claims, especially restricted/access-controlled deposits where authoritative bytes become available.
