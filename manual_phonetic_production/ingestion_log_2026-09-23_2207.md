# Ingestion pass — 2026-09-23 22:07 PT

## Discovery
Accepted `marathi_vot_kadu_rao_2025` (VOT Analysis of Marathi). Public GitHub repository documents primary recordings from 6 native Marathi speakers (3 female, 3 male), ages 21–52, a 36-word stimulus list targeting 12 plosives, and manual Praat annotation of stop burst and voicing onset. This directly satisfies primary/non-singing/human-annotation criteria.

## Repository inventory
Inspected the public repository and its `Final sound & Textfiles/TextGrids` directory. Manifested seven visible TextGrid annotation objects with exact Git blob SHAs/sizes, plus `Male vot.xlsx` and the stimulus PDF. Audio remains referenced upstream rather than duplicated here.

## Backlog/observation ingestion
Parsed the complete non-empty word tier visible in `F1_mono.TextGrid`: 36 source-faithful word intervals with original spellings/case and exact xmin/xmax values. Namespaced recording/speaker IDs were created. The same TextGrid contains a `phonemes` tier; in the inspected content it is blank over the visible intervals, so no segment labels or IPA were invented. Full parsing of the other TextGrids and the binary XLSX remains backlog.

## Variable dictionary
Added source-faithful mappings for word label, xmin, xmax, phonemes-tier label, burst landmark, voicing-onset landmark, and VOT. Units/windows are explicit. Spreadsheet raw values remain pending until binary extraction.

## Screening notes
HAMATAC was revisited: its repository metadata explicitly lists manual orthographic transcription, manual disfluencies and a `pho` manual annotation of phonetic phenomena. It remains a high-priority existing backlog candidate; restricted access and the need to inspect/isolate the phonetic tier prevent speculative observation ingestion in this pass. Spontal-N also has a manually corrected subset of automatic phoneme alignment; isolate only that corrected subset if publicly recoverable. IFCASL remains review pending a separable public deposit of the manually corrected >50% subset.

## QC
No inferred IPA. No automatic alignment promoted. No raw audio committed. All new IDs are source-namespaced. Only directly inspected observations counted.

## Next priorities
1. Parse all remaining Marathi TextGrids and `Male vot.xlsx`; reconcile burst/voicing landmarks against spreadsheet VOT values.
2. Resolve HAMATAC restricted files and isolate `pho` manual tier.
3. Locate/isolate Spontal-N manually corrected 7% phoneme subset.
4. Continue IFCASL deposit hunt and accepted-source backlog ingestion.
