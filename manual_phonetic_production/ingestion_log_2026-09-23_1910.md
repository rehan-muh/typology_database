# Ingestion pass — 2026-09-23 19:10 PT

## Discovery

Accepted `kec_southern_german` (Karl Eberhards Corpus) with strict tier-level eligibility. Current University of Tübingen/BAS descriptions report 40 spontaneous Southern German dialogues, 79 speakers, and half-hour EMA recordings for 20 speakers. The original 2016 publication described an earlier 35-dialogue release with 13 EMA speakers; these counts are retained as versioned provenance rather than conflated.

Manual evidence is unusually explicit: Praat TextGrid WORD boundaries were manually corrected. Segment boundaries were generated by forced alignment inside those corrected words and are therefore not eligible for the core manual segment layer. The database records this distinction explicitly.

## Backlog / source-level ingestion

Inventoried six logical source-object classes: WAV audio, Praat TextGrids, EMA trajectories, participant questionnaires, R processing scripts, and the reduced-form lexicon. BAS Repository access is ACA (free for scientific users; otherwise licence required), so this run preserves authoritative source references rather than claiming unrestricted redistribution.

Seeded nine variable-dictionary records covering original word labels and manual interval boundaries; the ineligible automatic segment tier for lineage; full 3-D EMA trajectories; gender, birth year, native language; and the explicit questionnaire missing marker `keine Angabe`. No IPA or segment context was invented. EMA is specified as trajectory data and must remain full-resolution (400 Hz in the original description) when materialized.

## Counts / QC

- Sources: 268 total; 75 accepted; 36 excluded; 157 review.
- Observation-ingested sources: 51.
- Parsed rows: 1,364,990 across 241 logical tables (unchanged; ACA corpus files were not available through the connector for deterministic row materialization).
- File-manifest records: 1,325 (+6).
- Variable-dictionary records: 1,396 (+9).
- Relational/provenance rule checked: manual word layer eligible; forced-aligned segment layer excluded; release-version counts kept separate.

## Discovery screening notes

Fresh searches also re-surfaced MRPL2, RescueSpeech, ROG, L2-ARCTIC and IFCASL, all already represented in the database/backlog. GRAIN-S was rejected for this database because its new manual contribution is syntactic annotation of pre-existing interviews rather than a new phonetic-production annotation layer. Nonspeech7k was rejected as non-speech/third-party media. An automatically aligned child-directed-speech corpus has a public manually corrected subset and should be checked in a later pass for whether that subset is primary enough and isolatable under the database's secondary-reuse rule.

## Next backlog priorities

1. Materialize KEC TextGrids/EMA through an authorized BAS/CLARIN route if connector access becomes available; parse only manual word tiers plus primary EMA/metadata.
2. Inspect the public manually corrected subset of the child-directed-speech alignment corpus (OSF `ke44q`) and establish recording lineage/licensing before admission.
3. Continue IFCASL hunt for authoritative downloadable TextGrids with isolatable manually corrected `Real` tier.
4. Continue PAVOQUE complete-YAML materialization rather than counting connector-truncated excerpts.
