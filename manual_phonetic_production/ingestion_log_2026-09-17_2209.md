# Ingestion pass — 2026-09-17 22:09 PT

## Backlog ingestion / integrity correction

Audited the previous PAVOQUE angry-style observation batch directly against the pinned upstream `pavoque-angry.yaml` blob `bff79d978f740f6c2b09bc595ade03b6bdf33423`. The existing local rows labelled `spike0009`–`spike0016` did not match that upstream YAML (texts, timings, and segment sequences diverged), despite carrying the correct blob SHA. They were therefore not source-faithful and must not remain in the core.

Replaced the bad batch with six directly verified upstream utterances, `spike0012`–`spike0017`, preserving original prompt, text, style, absolute utterance start/end seconds, every original segment label, and every cumulative segment endpoint. No IPA normalization was invented. The replacement contains 6 utterances and 163 segment endpoints. Rows `spike0009`–`spike0011` are intentionally absent until re-extracted completely from upstream; no unverifiable carry-forward was allowed.

Validation performed: label/end-time cardinality equality per utterance; monotonically increasing segment endpoints; final endpoint within the corresponding utterance duration; source SHA pinned. This correction supersedes the claims/counts in commit `98d49a140fa21713176a61b2262ae13a75fb4045` for the affected file.

## Discovery / backlog audit

Re-audited L2-ARCTIC from the official PSI Lab documentation. Core-wide forced-aligned `/textgrid` material remains excluded. The isolatable `/annotation` layer is explicitly manual: 3,599 read-speech utterances across 24 speakers were manually examined, with corrected word/phone boundaries and phone substitutions/deletions/additions. Official counts are 14,098 substitutions, 3,420 deletions, and 1,092 additions. The spontaneous `suitcase_corpus` is even stronger provenance: 22 speakers; two trained phonetic-transcription RAs each transcribed half and cross-checked the other half; all transcriptions were finally checked by John Levis; approximately 9,458 phone segments and 3,083 word segments over 26.1 minutes, with 1,673 substitutions, 456 deletions, and 90 additions. These manual layers qualify, while automatic alignments do not.

Access remains gated by the official download form / emailed Google Drive link, so no observation rows were fabricated in this pass. Priority when package access is available: ingest `/annotation` and `suitcase_corpus` TextGrids only, inventory all tiers including optional annotator comments, preserve ARPAbet labels verbatim plus comments containing IPA, and namespace speaker/utterance/interval IDs.

## New-search triage

Fresh search also surfaced AutoRPT (LREC 2026), which bootstraps prosodic annotation and is useful as a provenance warning: model-generated predictions are not admissible unless the released layer can be shown to have been manually modified/verified. SAIT-EMA remains review-only because its phonetic TextGrids are MFA-generated even though raw EMA/audio are primary.

## Next priorities

1. Continue PAVOQUE from `spike0018` only after direct upstream extraction; also recover `spike0009`–`spike0011` from complete upstream records.
2. Obtain/parse L2-ARCTIC manual `/annotation` and `suitcase_corpus` TextGrids; never ingest corpus-wide forced alignment as manual evidence.
3. Continue observation-level work on Russian Fricatives, Estonian spontaneous-speech TextGrids, UltraSuite manual SLT labels, and Tang–Parrell–Niziolek OSF tables.
