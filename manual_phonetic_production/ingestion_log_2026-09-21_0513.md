# Ingestion pass — 2026-09-21 05:13 PT

## Discovery

Accepted `parker_sonority_2008_mendeley`: primary English and Spanish speech recorded in 2005 for Parker, *Journal of Phonetics* 36 (2008), deposited as Mendeley Data DOI 10.17632/5jcwt7rffw.6. Deposit states that WAV files were manually segmented and analyzed in Praat. License CC BY 4.0. This satisfies manual-production inclusion. Raw audio should not be mirrored into this GitHub repository; retain authoritative deposit URLs/checksums and ingest any separately exposed annotation/analysis tables source-faithfully when retrievable.

Excluded discovery noise: singing/choir datasets even where F0/phoneme annotations are manual; third-party social-media speech; automatic-only alignment products; perception/neural/movie-stimulus datasets.

## Backlog ingestion / VoxAngeles

Revalidated accepted `voxangeles` against upstream `pacscilab/voxangeles`. Upstream README explicitly separates `data/audited_aligned` (manually corrected phone-level alignments and audited transcriptions, often checked against original UCLA field notes) from `data/unaudited` (forced-aligned, not manually corrected). Only audited material is admissible.

Enumerated the upstream `data/phonetic_measurements` directory and verified six immutable files and blob SHAs:

- `voxangeles_durations.tsv` — `312cc96927ad4e2a1d79957499e82d9a5cf0340c`
- `voxangeles_f0_deciles.tsv` — `2d20c3de967ca03c80dfe78d22e29fb1383148c9`
- `voxangeles_f0_quartiles.tsv` — `afccf9eb747835743cd59607576361bed556b3d2`
- `voxangeles_formants_deciles.tsv` — `e0d8e6de5a5a85c3d1b45fc8c2d8bad8716597c6`
- `voxangeles_formants_quartiles.tsv` — `216ee5c99310ac5bfa0fd744370cb0f3912b20f7`
- `voxangeles_meanf0_file.tsv` — `9492dd2ed35044219aed483613fd3f7a44f292d3`

Verified schema/sample of `voxangeles_meanf0_file.tsv`: columns `file`, `meanf0`; IDs such as `abk-002-000`; numeric mean F0 values. Do not invent units beyond upstream documentation; map `meanf0` only after extraction-method documentation is tied to the variable dictionary.

Recursive upstream tree confirms per-language ZIP archives under `data/audited_aligned` (e.g. `abk.zip`, `ace.zip`, `ady.zip`, etc.) with immutable Git blob SHAs. These are the priority annotation payloads for subsequent materialization/parsing; do not substitute `data/unaudited`.

## Integrity / counts

No VoxAngeles observation rows were added in this pass because the GitHub connector cannot safely materialize the multi-megabyte TSV/ZIP payloads into the database tables. Existing canonical parsed-row/table counts therefore remain unchanged. One newly accepted source was added to the registry; canonical source counts should advance by +1 accepted/+1 total when STATUS is reconciled.

## Next priorities

1. Materialize/parse VoxAngeles audited alignment ZIPs and six measurement TSVs; inventory every member and variable, preserve all trajectory sample points, and namespace IDs.
2. Trace downloadable files for Parker sonority DOI 10.17632/5jcwt7rffw.6, enumerate WAV/annotation/analysis assets, checksum source files, and ingest non-audio structured observations.
3. Continue access tracing for IFCASL corrected phone/word segmentation and CORPRES manual subset.
4. Maintain strict exclusion of automatic-only layers and duplicate underlying recordings.
