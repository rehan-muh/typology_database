# Manually Annotated Phonetic Production Database — ingestion log 2026-09-13 06:12 PT

## Backlog ingestion

### CCOST (`ccost_2026`)
- Verified the exact prior checkpoint at **195/488** authoritative metadata rows; next row remains **196**.
- Retried authoritative Zenodo access. The record page remained available, but the direct CSV retrieval path timed out during this pass.
- No metadata rows were reconstructed from secondary snippets, guessed, or repeated.
- The 6.2 GB audio/TextGrid archive remains reference-only; no raw audio was committed.

### PAVOQUE (`pavoque`, angry subset)
- Shifted backlog effort to an already qualified, directly auditable source rather than leaving the run at paper-level work.
- Continued from the prior durable source-faithful checkpoint after `m0099`.
- Parsed and committed literal upstream records **m0101, m0103, m0104, m0105**.
- New utterance table: `data/source_faithful/pavoque_angry_utterances_chunk042.csv` — **4 rows**.
- New interval table: `data/source_faithful/pavoque_angry_segments_chunk042.csv` — **124 rows** (22 + 28 + 40 + 34).
- Preserved source utterance IDs, original German text, `angry` style, utterance bounds, German-SAMPA/silence labels, and cumulative segment endpoints.
- Derived only interval starts and interval durations from the source cumulative endpoints; did not infer IPA or missing phonological context.
- Each of the four utterances retains the source-native **0.005 s terminal residual**.
- Added checksum/blob-linked chunk manifest `data/source_faithful/pavoque_angry_chunk042_manifest.json`.
- Important integrity finding: the upstream YAML is **not numerically ordered by prompt ID**. After `m0105`, the literal sequence continues `m0106`, `m0102`, `m0100`, `m0096`, `m0107`. Therefore lower-numbered IDs not yet encountered must not be treated as absent merely because their number is below the current prompt.
- Next verified literal record: **m0106**.
- Double-counting guard retained: these durable rows mirror a PAVOQUE lineage already represented in the validated full parse, so they do **not** increment the distinct scientific-observation total.

## Discovery

### HIGH review — AusKidTalk
- Added `auskidtalk_2026` to the review queue.
- Primary non-singing Australian-English child production from **620 children aged 3–12**, spanning typical development, speech-sound-disorder, and autism cohorts and five elicitation tasks.
- The Task 1 annotation workflow reports time-aligned Praat TextGrid/CSV outputs with human correction or transcription from scratch for **461 files**.
- Human review encompasses labels and temporal boundaries as well as child/non-child rejection, noise marking, and phoneme-level discrepancy flags. A reported 380-file sample contained **85,436 reviewed intervals** and **61,371 identified target words**.
- Kept in review rather than admitted to core because the corpus data are presently governed by a data-custodian/on-request access model rather than an openly downloadable research deposit.
- Promotion requires a public or authorized data endpoint from which the exact annotation/measurement files and redistribution terms can be enumerated. Code-only material is not sufficient for ingestion.

## Validation / integrity
- PAVOQUE chunk 042 contains exactly **4 utterance rows and 124 interval rows**, matching its manifest.
- All PAVOQUE IDs remain namespaced as `pavoque::...`.
- Original source labels/endpoints were not overwritten by derived values.
- No IPA was invented.
- No raw audio was committed.
- No files outside `manual_phonetic_production/` were changed.
- Global distinct parsed-observation count remains **1,361,972** because of the PAVOQUE lineage double-counting guard.
- Registry totals after discovery: **174 tracked / 47 accepted / 29 excluded / 98 review / 45 materially ingested**.

## Next backlog priorities
1. Resume CCOST metadata at source row **196** as soon as the authoritative CSV endpoint responds; never restart from row 1.
2. Obtain annotation-only access to `C-COST.zip` and ingest every manually corrected word/syllable/phone/toneme TextGrid tier without mirroring WAV audio.
3. Continue PAVOQUE in **literal upstream order** from `m0106`, preserving the non-numeric ordering rather than sorting prompt IDs.
4. Continue promotion work on trajectory-rich HIGH-review sources and resolve a usable data endpoint for AusKidTalk before admitting any of its observations.
