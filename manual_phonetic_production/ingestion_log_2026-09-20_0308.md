# Ingestion pass — 2026-09-20 03:08 PT

## Backlog ingestion

Added `mlml_autovot_manual_training` as an accepted-partial source restricted to the genuinely new manual VOT annotation layer. AutoVOT documentation states that training examples use manually labeled VOTs. The public tutorial tree was enumerated and contains separate voiced/voiceless training directories with paired WAV/TextGrid files. Parsed `cas7D_1054_11_1.TextGrid` and admitted only its non-empty `vot` interval: 0.9374492677584578–1.0190903846153845 s (duration 0.0816411168569267 s). Source blob SHA: `9eaf78b5372159db578ad32358b12e97e0904fef`.

The same TextGrid also contains `phones` and `words` tiers. These were deliberately not admitted because this pass did not establish that those tiers are manual; this prevents automatic/forced-aligned material from leaking into core. Underlying recording/corpus lineage remains unresolved, so speaker/demographic/lexical metadata are not invented. Next pass should enumerate every training TextGrid in both voiced and voiceless directories, extract only non-empty manual VOT tiers, preserve blob SHAs, then resolve recording lineage and deduplicate.

## Discovery

Repository/web searches targeted manual segmentation, VOT, formants, TextGrid deposits, OSF, Zenodo, and GitHub. AutoVOT was promoted because it exposes actual paired recordings/TextGrids and explicitly requires manually labeled VOT training examples. Several false positives were rejected from ingestion: Jingju manual segmentation is singing; DPI-Watch is respiratory inhaler sound rather than speech production; medical/image segmentation hits are non-speech; Praanscribe is automatic Whisper/G2P alignment; getVOT/VOT-CP are measurement software and were not treated as datasets without independently qualifying deposited manual observations.

## Integrity/provenance

Writes confined to `manual_phonetic_production/`. Namespaced source ID used. Exact source boundaries retained; duration added only as a transparent deterministic derivative. No IPA, phone identity, lexical context, speaker metadata, or manual status was inferred. New registry, source-faithful, variable-dictionary, and ingestion-status records were added. Existing repository content was not overwritten.
