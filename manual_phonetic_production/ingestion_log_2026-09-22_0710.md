# Ingestion pass — 2026-09-22 07:10 PT

## Discovery
Screened repository-focused candidates for manually created/corrected primary production data. Added `incromin_test_calls_2025` to review. The public GitHub corpus contains primary consented multilingual call recordings; transcripts/translations were automatically initialized then manually corrected, while timestamps are explicitly unrevised/automatic except for a specifically manually corrected diarization file. Core admission is deferred until manual layers are isolated from automatic timing.

Excluded `swc_codeswitch_2021`: the German-English Code-Switching Speech Corpus is a derivative/resegmentation of the pre-existing Spoken Wikipedia Corpus. Although SWC has manual word/segment annotations, this candidate is secondary reuse and no genuinely new isolatable manual phonetic layer was established.

Also re-screened MRPL2 (strong manual mispronunciation annotations but files restricted), PAVOQUE (already accepted/backlogged), and irrelevant/non-production search hits; none were promoted without observation-level provenance.

## Backlog ingestion
Revalidated `rescuespeech_2023` against authoritative Zenodo v2. The record states manually annotated native-German recordings, 1 h 36 min ASR duration, 2,412 clean utterances, and 2,412 synthesized noisy counterparts. Three top-level files remain manifested with authoritative MD5 checksums. Attempted retrieval of the small `Readme.md` and archive-access path; retrieval failed in this environment. No archive schema or observation rows were invented. Clean utterances remain eligible; noisy copies and speech-enhancement augmentation remain excluded as synthetic derivatives.

## Counts / validation
- sources: 244 = 61 accepted + 34 excluded + 149 review
- fully ingested sources: 50
- parsed scientific rows: 1,364,792 (unchanged)
- parsed logical tables: 236 (unchanged)
- file manifest records: 1,250 (unchanged)
- variable dictionary records: 1,272 (unchanged)
- relational/count invariant: 61 + 34 + 149 = 244
- no unrelated repository paths modified

## Next priorities
1. Retrieve/enumerate RescueSpeech `Task_ASR.tar.gz`; parse transcript/annotation files before audio and ingest all clean primary observations.
2. Enumerate InCroMin meeting metadata and isolate human-corrected transcript/diarization layers from automatic timestamps.
3. Continue acquisition attempts for restricted/high-value MRPL2 and other manually segmented production deposits.
4. Continue PAVOQUE only from authoritative verified units; do not infer missing cursor entries.
