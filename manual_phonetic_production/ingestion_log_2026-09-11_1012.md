# Manual phonetic production ingestion log — 2026-09-11 10:12 PT

## Backlog ingestion

Advanced the durable source-faithful PAVOQUE angry-style mirror by 400 exact UTF-8 source lines: `pavoque-angry.yaml` lines 8921–9320. All four chunks were verified against immutable upstream Git blob `bff79d978f740f6c2b09bc595ade03b6bdf33423` and committed under `source_faithful/pavoque/`.

The contiguous angry-style prefix is now lines 1–9320. It contains 240 complete source records plus one partial record (`m0197`) at the endpoint. This increment closes the previous partial `m0190`, contains complete `m0191`, `m0192`, `m0193`, `m0194`, `m0195`, `m0196`, `m0162`, `m0177`, and `m0186`, and enters `m0197`. The next deterministic continuation point is line 9321.

### Source-order correction

Prompt identifiers are not monotonic in file order. In particular, `m0177` and `m0186` occur at lines 9221–9320 even though neither appeared in earlier local windows. Earlier audit statements about their non-occurrence referred only to those inspected windows and must not be interpreted as global absence from `pavoque-angry.yaml`. No historical audit log was silently rewritten; the correction is recorded prospectively here and in current ingestion status.

## Observation-level promotion

Promoted `pavoque::m0191` (original text: `Sie war der Göttin Freya geweiht.`) to the warehouse utterance layer, retaining original prompt, German text, style, source start/end times, deterministic duration, manual-annotation scope, source-line window, and immutable upstream blob SHA.

Promoted all 24 manually corrected SAMPA/silence intervals for `m0191` to the segment-interval layer. Source-native labels and relative endpoints were retained unchanged. Interval starts, durations, and absolute start/end times are stored as separate deterministic derivations. `normalized_ipa` remains blank because no explicit source-supported SAMPA→IPA mapping was applied in this pass. The final source endpoint is 3.475 s while the utterance duration is 3.480 s; the 5 ms residual was retained rather than silently extending the last interval.

This pass adds 25 promoted observation rows: 1 utterance + 24 intervals. PAVOQUE cumulative promoted totals are now 28 utterance rows and 134 interval rows. Global promoted/parsed rows are 1,361,571 across 86 logical tables.

## Discovery and screening

Fresh searches covered public/manual phonetic alignment and segmentation resources across repository and corpus literature.

- **Elsner & Ito child-directed-speech manual subset (OSF `ke44q`)**: the publication states that a subset of the forced-aligned corpus was manually corrected and that both automatic and manually corrected alignments are public. Because the underlying recordings are pre-existing child-directed-speech data, this is potentially admissible only under the rule allowing genuinely new, isolable manual annotations over reused recordings. It remains a discovery/review candidate pending direct OSF file enumeration, proof that the manual subset can be isolated without mixing automatic rows, and license/provenance capture. It was not admitted to core in this pass.
- **CORPRES / Fully Annotated Corpus of Russian Speech**: documentation describes primary speech from eight speakers, 60 hours total, with 40% manually segmented and fully annotated on six phonetic/prosodic levels. The evidence is strong for conceptual inclusion, but an authoritative currently public observation-level deposit was not located in this pass. No paper-only accepted registry row was created.
- **IFCASL** was rediscovered and deduplicated against the existing review state rather than added again.
- **Jingju a Cappella** was encountered again and remains excluded because it is singing/music production despite manual phone segmentation.

## Validation and provenance

- All repository writes in this pass are confined to `manual_phonetic_production/`.
- No raw audio was committed.
- Four source-faithful chunk manifest records were added; global inventory-file records are now 967.
- Namespaced observation IDs use `source_id::source_record_id` and `source_id::source_record_id::segment` conventions.
- No missing IPA, segmental context, or measurement values were invented.
- Source endpoints remain immutable raw fields; derived temporal fields do not overwrite them.
- Source counts are unchanged: 143 tracked / 45 accepted / 25 excluded / 73 review / 44 materially ingested.

## Next backlog priorities

1. Continue `pavoque-angry.yaml` from line 9321.
2. Promote complete `m0192–m0196`, `m0162`, `m0177`, and `m0186` with all manually corrected intervals, then continue older mirrored PAVOQUE promotion backlog (`m0179`, `m0181–m0185`, `m0187–m0189`).
3. Enumerate the OSF `ke44q` manual-correction subset at file level and admit only if manual rows are cleanly isolable from automatic alignments.
4. Return to accepted Chenghai Southern Min TextGrid/XLSX observation-level ingestion.
5. Retry observation-level acquisition/parsing of the accepted real-time MRI masks and velopharyngeal-closure judgments.
6. Continue archive-level resolution of high-value REVIEW sources such as IFCASL, AusTalk, and AphasiaBD without admitting transcript-only/manual-sentence layers as phone-level annotation.
