# Manual phonetic production ingestion log — 2026-09-11 22:10 PT

## Backlog ingestion

Canonical repository state was re-read before writing. The durable PAVOQUE state at the start of the pass was 13 chunks / 49 utterances / 1,472 intervals through `spike0051`; upstream `marytts/pavoque-data/pavoque-angry.yaml` remains at blob SHA `bff79d978f740f6c2b09bc595ade03b6bdf33423`.

Materialized source-faithful chunk 014:

- `spike0052` through `spike0054`
- 3 complete utterance rows
- 71 manually corrected German-SAMPA/silence interval rows (21 + 24 + 26)

Committed files:

- `data/source_faithful/pavoque_angry_utterances_chunk014.csv`
- `data/source_faithful/pavoque_angry_segments_chunk014.csv`
- `data/source_faithful/pavoque_angry_file_manifest_chunk014.csv`

All original source labels and cumulative endpoints are retained. Segment starts and durations are deterministic endpoint differences. No IPA was invented. Source-native terminal annotation timing is preserved. Direct upstream inspection establishes that `spike0054` is followed by `m0001`, so the durable mirror has now completed the source's `spike` sequence and the next deterministic continuation is `m0001`.

Canonical PAVOQUE status now records 14 chunks, 52 utterances, and 1,543 intervals. These durable rows do not increase the global scientific-observation total because their annotation lineage is already represented in the validated full PAVOQUE parse.

## Fresh discovery

Added one new HIGH REVIEW candidate:

### Hu (2025), Tibetan trilingual learners' English vowel production

Public Mendeley Data deposit (DOI `10.17632/9y6c4hnt7v.1`; CC BY 4.0) reports primary English diphthong production from Tibetan-Chinese-English trilingual learners. The deposit explicitly describes F1/F2 trajectories and peak formant values for /aɪ, eɪ, ɔɪ, aʊ, oʊ/, participant demographic/linguistic metadata, Praat acoustic analysis, and careful manual annotation and validation. The public record exposes participant-background material plus sex-separated TXT measurement files.

It remains REVIEW until the complete deposit file tree is enumerated and the TXT schema is parsed. Before core admission, verify speaker counts, token identifiers, units, trajectory sampling/timepoint definitions, missing-value conventions, and which values were manually created/corrected versus automatically extracted then validated.

Registry increment: `data/registry/increments/2026-09-11_2210_review.csv`.

## Validation

- New PAVOQUE utterance IDs use `pavoque::source_utterance_id` namespacing.
- All 71 interval rows resolve to the three newly materialized utterances.
- Segment indices are unique and monotonic within utterance.
- All derived durations are nonnegative endpoint differences.
- Upstream PAVOQUE blob SHA is unchanged.
- No raw audio was committed.
- Hu 2025 remains REVIEW pending file-level schema/provenance verification.
- All writes are confined to `manual_phonetic_production/`.

## Reconciled state

- 152 tracked sources
- 46 accepted
- 25 excluded
- 81 review
- 44 materially ingested sources
- 1,361,571 distinct promoted/parsed observations across 86 logical tables
- 1,023 inventory-file records
- 1,148 variable-dictionary records
- PAVOQUE durable mirror: 52 utterances / 1,543 intervals; next `m0001`

## Next backlog priorities

1. Continue PAVOQUE at `m0001`, preserving source order rather than assuming numeric continuity across prompt families.
2. Enumerate and parse Hu 2025 Mendeley TXT measurement files and participant metadata; ingest full trajectories if the files expose observation-level time series.
3. Retry IFA compact annotation archive retrieval and parse labels/protocol/database exports when bytes are reachable.
4. Enumerate the Bengali-English OSF trajectory deposit and ingest all five F1/F2 samples per vowel token plus all source predictors.
5. Isolate L2-ARCTIC's manually corrected subset from its forced-aligned remainder.
6. Continue Chenghai Southern Min and manually labelled rt-MRI observation-level ingestion.
