# Manual phonetic production ingestion — 2026-09-23 03:13 PT

## Backlog ingestion / reconciliation

- Audited canonical `data/source_faithful/pavoque/` on `main` before appending further segment blocks.
- Canonical source-faithful files currently cover `spike0001` through `spike0010` in five two-utterance CSV blocks.
- Verified upstream `pavoque-angry.yaml` still continues with `spike0011` (`Ich bin Spike, welches Problem hast du?`, angry, recording-relative start 35.52 s, end 39.36 s) and `spike0012` (`Entschuldigung, sag das nochmal.`, angry, start 39.36 s, end 42.78 s).
- Wrote `data/ingestion_status/pavoque_reconciliation_2026-09-23_0313.csv` so subsequent runs begin at `spike0011` and do not rely on non-canonical prior run narratives.
- Did not fabricate segment rows from truncated transport output. Next run should fetch the complete YAML object (or exact range) and promote complete `spike0011+` intervals only after all labels/endpoints are available.

## Discovery

### TunSwitch — Zenodo 8342762
- Repository record inspected directly; DOI `10.5281/zenodo.8342762`.
- Deposit has five public objects totaling about 2.0 GB: `paper.pdf` (md5 `d02f41b1a1a20e50ab91e9974675e444`), `test_wavs.zip` (`7cdd5b100f2f1f4429b74a920ca338fa`), `TextData.zip` (`eb23d02b843b3b607670b5bba1887add`), `TunSwitchCS.zip` (`6bbb3c6014819d28c6cb08692d27a31e`), and `TunSwitchTO.zip` (`dce80d1ee335c602ae38637828b87e9e`).
- **TO component:** primary prompted Tunisian speech: 89 participants, 2631 distinct phrases. Kept in REVIEW because the record does not establish manual/manual-corrected phonetic boundaries, landmarks, or production measurements. Orthographic prompt/transcript availability alone is insufficient for core admission.
- **CS component:** explicitly manually annotated language tags around French/English words, but underlying audio is harvested from radio shows/podcasts and segmented with WebRTC-VAD after automatic overlap/music filtering. EXCLUDED from core under the third-party-media rule, while lineage is retained to prevent accidental later admission.
- Added split-level review/exclusion record in `data/registry/review_increment_2026-09-23_0313.csv`.

## Search screening

Repository-first searches covered manual segmentation/annotation, TextGrid, formant/VOT, articulatory/EMA and production deposits on Zenodo/OSF. Search noise included singing corpora (excluded by scope), third-party media (e.g. COVYT; already incompatible with lineage rule), and non-speech manual segmentation datasets. No weak candidate was promoted merely to increase source counts.

## QC / integrity

- Writes confined to `manual_phonetic_production/`.
- No existing source-faithful observations overwritten.
- PAVOQUE canonical coverage is now explicitly recorded as `spike0001–spike0010`; next prompt is `spike0011`.
- TunSwitch is represented at component/lineage level rather than assigning one misleading dataset-wide status.

## Next priorities

1. Fetch complete PAVOQUE `spike0011+` segment arrays and bulk-promote contiguous source-faithful blocks with endpoint monotonicity and terminal-residual QC.
2. Inspect `TextData.zip` metadata for TunSwitch TO only if it can establish human verification; never admit CS recordings to core.
3. Continue object-level access work for IFCASL, CORPRES, BAS manually segmented corpora, and other primary production deposits with explicit human annotation provenance.
