# Ingestion pass — 2026-09-14 18:07 PT

## Discovery

Accepted **L2-ARCTIC manual annotation layer** after direct documentation review. The corpus contains 26,867 scripted utterances from 24 L2 English speakers across six L1 groups. Human annotators manually examined 3,599 scripted utterances. Official documentation reports 14,098 phone substitutions, 3,420 deletions, and 1,092 additions. The v5 suitcase subset adds spontaneous production from 22 speakers (~26.1 min, ~9,458 phone segments), annotated by two trained research assistants who cross-checked one another and whose transcriptions were finally checked by John Levis; reported errors are 1,673 substitutions, 456 deletions, and 90 additions.

### Inclusion boundary

Only `annotation/*.TextGrid` and the suitcase manual annotation layer qualify as manual production annotation. Ordinary `textgrid/*.TextGrid` files are forced-alignment output and are explicitly excluded. This boundary is recorded in the manifest so future bulk ingestion cannot silently promote automatic phone/word intervals.

### Variables inventoried

Added dictionary entries for source `words`, `phones`, optional `comments`, speaker code, L1, and gender. Original ARPAbet/error labels must remain source-faithful. Comments can contain IPA, but no IPA normalization should be invented from ambiguous labels. Namespaced speaker IDs must use `l2_arctic_manual::source_speaker_id`.

## Backlog ingestion work

Re-opened current database state and confirmed the previous durable checkpoint: JSUT-label BASIC5000 manual coverage ends at `BASIC5000_0380`, with `BASIC5000_0381` next. No source-faithful JSUT rows were fabricated in this pass because the authoritative blob slice was not retrieved to the required row range. Existing automatic Julius timing remains excluded.

L2-ARCTIC corpus bytes were not ingested because the authoritative download requires completing the corpus license/access form. Rather than use an unofficial mirror as immutable provenance, this pass records the authoritative collections and marks manual TextGrid parsing `accepted_pending_download`.

## Files written

- `data/registry/accepted_increment_2026-09-14_1807.csv`
- `data/registry/file_manifest_increment_2026-09-14_1807.csv`
- `data/registry/variable_dictionary_increment_2026-09-14_1807.csv`
- this log
- `STATUS.json`

## Validation / lineage

- Primary recordings: yes.
- Non-singing: yes.
- Manual/human-verified layer: yes, explicitly isolated.
- Forced-aligned layer: explicitly excluded.
- Third-party media: no.
- Duplicate lineage: no duplicate source found in repository search; L2-ARCTIC is treated as its own primary recording lineage.
- Bulk audio policy: reference-only; do not commit WAV archive to GitHub.

## Next priorities

1. Resume JSUT from `BASIC5000_0381` only after authoritative blob retrieval.
2. Obtain authoritative L2-ARCTIC v5 archive under its access terms; enumerate every member and checksum; parse all 3,599 scripted manual TextGrids plus the 22-speaker suitcase annotation set.
3. Materialize source-faithful interval tables preserving all tiers/labels/comments before harmonization; derive starts/durations separately and retain raw boundaries.
4. Continue exhaustive PAVOQUE YAML and DanPASS TextGrid backlog.
5. Continue repository discovery emphasizing manual acoustic/articulatory measurement deposits rather than ASR-only corpora.
