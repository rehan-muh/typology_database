# Manual phonetic production ingestion log — 2026-09-11 16:10 PT

## Backlog ingestion

Resumed from canonical PAVOQUE endpoint `spike0029`. The authoritative source remains `marytts/pavoque-data/pavoque-angry.yaml`, pinned to immutable Git blob `bff79d978f740f6c2b09bc595ade03b6bdf33423`; the corpus documentation identifies this segment layer as manually corrected and uses German-SAMPA labels plus silence.

This pass durably committed `spike0030` (`Du bist einfach nur ein blöder Romantiker, du hast keine Ahnung von der Realität.`) as **1 source-faithful utterance row + 64 manually corrected segment/silence interval rows** in:

- `data/source_faithful/pavoque_angry_utterances_chunk008.csv`
- `data/source_faithful/pavoque_angry_segments_chunk008.csv`

Original text, expressive-style label, source utterance start/end, German-SAMPA/silence labels and cumulative source endpoints are retained. Interval starts and durations are deterministic derivatives from consecutive endpoints. No IPA or unobserved phonetic context was invented.

### Validation

- segment indices: consecutive 1–64
- cumulative endpoints: monotonic
- derived durations: all nonnegative
- final annotated endpoint: 6.355 s
- utterance duration: 6.360 s
- residual: 0.005 s, retained rather than silently stretching the final silence interval
- next upstream source record verified directly: `spike0031`
- current upstream file blob SHA reverified as `bff79d978f740f6c2b09bc595ade03b6bdf33423`

Canonical PAVOQUE durable state is now **8 chunks / 28 utterances / 860 intervals**, latest record `spike0030`, next verified record `spike0031`.

The PAVOQUE file manifest was extended to 16 chunk-file records. During that rewrite, a transcription typo in the already-existing chunk007 utterance blob SHA was detected and immediately corrected against the unchanged underlying Git blob. No source data were altered.

The validated full PAVOQUE CI parse already represents these scientific observations, so global `parsed_rows_total` is intentionally unchanged; durable materialization is not counted as a second scientific observation.

## Discovery pass

### L2-ARCTIC manual subset — REVIEW, metadata sharpened

Fresh verification of public Lhotse corpus tooling provides unusually useful exact counts for the isolated human-annotated portion: **3,599 manually annotated utterances across 24 speakers, approximately 9,458 phone segments excluding silence, 1,673 phone substitutions, 456 deletions and 90 additions**. The broader corpus contains forced alignment, so only the corrected manual subset is admissible. The authoritative corpus download still requires manual acquisition/acceptance of source terms, so no forced-aligned-only material is being promoted. A high-priority review increment was added with these isolation requirements.

### Mitra & Dutta Bengali–English phonetic transfer — new HIGH review candidate

A 2023 bilingual speech-production study was identified with an **open OSF data/materials/code deposit** (`10.17605/OSF.IO/DSB2X`). The study used 10 Bengali–English bilingual speakers and manually segmented/annotated the target vowels in Praat. For 2,333 retained target tokens (`[ʌ]`: 1,137; `[æ]`: 1,196), F1 and F2 were measured at **five trajectory timepoints: 5%, 15%, 25%, 35%, 45%** of the vowel. Conditions include mixed vs nonswitched production and two elicitation paradigms. This is high-value for the database because it supplies dynamic vowel measurements rather than midpoint-only summaries.

It remains REVIEW until the OSF file tree is enumerated and actual token-level files are downloaded/parsed. The ingest must preserve all five timepoints, raw versus normalized values separately if both exist, and speaker/word/task/context identifiers. Boundary timestamps will be retained if deposited; none will be invented.

Both candidates were written to `data/registry/increments/2026-09-11_1610_review.csv` after repository-wide search found no existing ARCTIC or Mitra/Dutta records, preventing known duplicate registry insertion.

## Reconciled state

- **147 tracked sources**
- **45 accepted**
- **25 excluded**
- **77 review**
- **44 materially ingested sources**
- **1,361,571 distinct promoted/parsed observations across 86 logical tables**
- **976 inventory-file records**
- **1,148 variable-dictionary records**
- PAVOQUE durable source-faithful mirror: **28 utterances / 860 intervals**

## Next backlog priorities

1. Continue PAVOQUE at verified `spike0031`; `spike0031` through at least `spike0034` are directly visible upstream and can be promoted without source-order guessing.
2. Enumerate OSF `DSB2X` and ingest the Bengali–English token-level F1/F2 trajectory table, retaining all five sampled points and all predictors.
3. Resolve licensed L2-ARCTIC acquisition and isolate the 3,599 manually annotated utterances/TextGrids from forced-aligned-only material.
4. Enumerate AUSpeech ScienceDB and isolate the fully manual dysarthric subset plus exhaustively human-checked normal-speaker annotations.
5. Resume accepted Chenghai Southern Min TextGrid/XLSX and real-time MRI observation-level ingestion.
