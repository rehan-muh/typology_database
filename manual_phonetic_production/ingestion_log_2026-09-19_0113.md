# Manual phonetic production ingestion — 2026-09-19 01:13 PT

## Backlog ingestion

Advanced the accepted JSUT manual-label source from BASIC5000_1350 through BASIC5000_1390 using the authoritative upstream `e2e_symbol/phoneme.yaml` blob `ae8933677c082c22238a895012b6b6dcba1f6625`.

- New source-faithful rows: 40 (`BASIC5000_1351`–`BASIC5000_1390`).
- Source file: `e2e_symbol/phoneme.yaml`.
- Preserved original symbol sequences byte-for-byte at the observation field level.
- Annotation method retained as `manual_curated_symbol_sequence`, consistent with prior JSUT ingestion.
- No IPA mapping, timing, context, or automatic Julius alignment was invented or imported.
- Namespaced source identity remains `jsut::BASIC5000_xxxx` conceptually; source-faithful table retains `source_id` + `source_record_id` separately.
- Validation: 40 unique contiguous record IDs; first `BASIC5000_1351`; last `BASIC5000_1390`; no overlap with prior durable cursor ending at 1350; authoritative blob SHA unchanged.
- Next JSUT cursor: `BASIC5000_1391`.

File written: `data/source_faithful/jsut_basic5000_1351_1390_20260919_0113.csv`.

## Discovery / qualification work

### BELMASK — review candidate, restricted observation files

Re-audited the Berlin Dataset of Lombard and Masked Speech (BELMASK), Zenodo DOI `10.5281/zenodo.10730795` / concept record `10.5281/zenodo.10730794`, with the 2024 Data paper DOI `10.3390/data9080092`.

Qualification evidence is stronger than a generic annotated-corpus hit: this is primary German production from 10 speakers (128 min) in four mask/noise conditions. The paper states that recordings were visually/auditorily inspected and selected utterances were manually segmented in Praat using tier boundaries. Those manually created sentence boundaries are qualifying production annotations. The subsequent phone/word segmentation used the BAS G2P→MAUS→PHO2SYL automatic pipeline, so those phone boundaries must NOT be admitted as manual unless a source explicitly documents human correction of particular files/boundaries.

The Zenodo payload is restricted behind an EULA (reported data volume 24.3 GB), so no observation-level BELMASK rows were fabricated. Keep as review/access-blocked for manual sentence-boundary ingestion if access becomes available. This source is particularly useful for future style/Lombard/mask analyses because condition and speaker metadata are explicit.

## Deduplication / lineage safeguards

- PAVOQUE durable continuation remains `m0137`; do not resume from spike-number convenience/reverification files, which overlap the already represented durable recording lineage.
- JSUT row range in this pass begins exactly after the previously durable `BASIC5000_1350` cursor.
- No raw audio committed.
- No unrelated repository paths modified.

## Next priorities

1. JSUT `BASIC5000_1391+` from the same authoritative manual phoneme-symbol source.
2. Inspect Russian Fricatives public SWISSUbase files and ingest all acoustic/token variables if downloadable.
3. Inventory TaL manual short-segment boundary CSVs and map articulatory/acoustic metadata.
4. Isolate the L2-ARCTIC 3,599-utterance human-corrected subset from corpus-wide forced alignments.
5. Inspect RescueSpeech v2 annotation tiers and admit only demonstrably manual layers.
6. Continue CCOST and Chenghai source-faithful backlog.
7. BELMASK remains access-blocked; retain manual sentence-boundary qualification but exclude automatic MAUS phone boundaries.
