# Ingestion pass — 2026-09-18 19:08 PT

## Backlog ingestion

- Continued accepted `pavoque` from cursor `spike0013` through `spike0014` using the authoritative `marytts/pavoque-data/pavoque-angry.yaml` at immutable blob `bff79d978f740f6c2b09bc595ade03b6bdf33423`.
- Added 55 source-faithful manually corrected segment observations: 28 for `spike0013` and 27 for `spike0014`.
- Preserved prompt, utterance text, style, utterance start/end, original segment label, original cumulative segment endpoint, source path, and blob SHA. No IPA was invented.
- Validation: segment endpoints are monotonic and nonnegative. `spike0013` final endpoint 3.775 s vs utterance span 3.780 s; `spike0014` final endpoint 3.355 s vs span 3.360 s. Both leave the same 5 ms residual pattern seen in preceding PAVOQUE items.
- Next PAVOQUE cursor: `spike0015`.

## Discovery

Fresh repository/deposit search was run across manual segmentation/correction terms. RescueSpeech remains a strong review candidate: Zenodo v2 describes primary German SAR recordings as manually annotated and openly exposes the audio record, but the exact qualifying phonetic/timing annotation layer still needs file-level isolation before core admission. MRPL2 remains qualifying at the study level (4,500 manually annotated non-native English utterances) but its files are restricted, so no observation rows were fabricated. Searches also returned several NLP/text-only or third-party corpora; these were not promoted merely to increase source counts.

## Integrity / lineage

- Changes confined to `manual_phonetic_production/`.
- No audio committed.
- No duplicate PAVOQUE prompt IDs introduced relative to the established cursor through `spike0012`.
- Source-faithful values remain separate from any future harmonized/derived representation.

## Next priorities

1. PAVOQUE `spike0015+` observation-level ingestion.
2. Continue JSUT from the latest actual repository cursor (the source-faithful tree already extends well beyond the older 0981 cursor, so do not regress).
3. File-level enumeration/isolation for RescueSpeech, AusKidTalk, CORPRES, and other review candidates.
4. Continue accepted sources with public token/trajectory/annotation files before adding registry-only discoveries.
